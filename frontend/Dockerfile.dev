# Development Dockerfile для Next.js frontend с hot-reload
FROM node:22-alpine

# Метаданные образа
LABEL maintainer="FREESPORT Dev Team"
LABEL version="1.0-dev"
LABEL description="Next.js frontend для FREESPORT Platform (Development)"

# Установка libc6-compat для совместимости
RUN apk add --no-cache libc6-compat

WORKDIR /app

# Копирование файлов зависимостей
COPY package.json package-lock.json* ./

# Установка зависимостей
RUN npm ci --prefer-offline

# Копирование исходного кода
COPY . .

# Переменные окружения для разработки
ENV NODE_ENV=development
ENV NEXT_TELEMETRY_DISABLED=1
ENV PORT=3000
ENV HOST=0.0.0.0

# Открытие порта для Next.js
EXPOSE 3000

# Запуск dev сервера с Turbopack
CMD ["npm", "run", "dev"]
