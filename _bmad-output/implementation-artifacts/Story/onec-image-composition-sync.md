---
baseline_commit: 1c0fac85
spec: _bmad-output/specs/spec-image-sync-from-1c/SPEC.md
---

# Story: Состав изображений товара синхронизируется с 1С, а не только пополняется

Status: ready-for-dev

> 🟢 **Свежесть координат: все номера строк проверены чтением файлов на коммите `1c0fac85`** (27.08.2026). Совпадают с координатами в брифе и в `current-behavior.md`.
> 🔴 **Дефект подтверждён замерами прода, а не рассуждением.** Товар 9194 отдаёт `main_image` = картинка чужого товара 14 КБ, верное фото приехало 25.08 и легло вторым. По каталогу таких перекосов минимум **104** при 2 467 товарах с непустым `base_images`. Плюс **195 тысяч ложных `Image not found` в сутки** — 253 на каждый прогон goods, полтора месяца, ни одного сигнала.
> 🔴 **AC1 — предусловие AC2–AC6. Реализовывать строго первым.** Зеркалирование состава до AC1 обрежет каталог: 1С подчищает `import_files` после импорта, исходников на проде нет, «разрешить» картинку не по чему.
> 🟠 **Порядок методов важен не только между AC.** `_save_image_if_not_exists` сейчас проверяет наличие исходника **до** вычисления целевого пути (`:403-406` против `:416-422`). Чтобы узнать копию, путь надо вычислить раньше проверки. Это не косметика — на этом стоит весь AC1.
> 🟠 **Оба метода состава вызываются из ЧЕТЫРЁХ мест, и два из них зеркалить нельзя.** XML-путь (`variant_import.py:557`, `:615`, `:830`, `:917`) — зеркалим. Путь сканирования каталога (`integrations/tasks.py:301`, `:311`; `import_images_from_1c.py:199`, `:208`) — остаётся аддитивным. Механизм разделения задан в Dev Notes, не изобретать свой.
> 🟢 **Развилка «законно ли зеркалить» закрыта фактами 27.08.2026.** `СодержитТолькоИзменения="true"` стоит на `<Каталог>` и отбирает, какие **товары** попадут в файл; состав `<Картинка>` внутри товара полный — 187 из 500 товаров в одной инкрементальной выгрузке несут больше одной картинки, у одного девять. Разбор — в `evidence.md`.
> ⚠️ **Перед первым прод-прогоном с зеркалированием** — перехватить живой `goods.xml` товара с несколькими фото и убедиться, что список полный. Реализацию и тесты это не блокирует, выкат — блокирует.
> ⚠️ **`tech-debt.md` п. 24 описывает эту же симптоматику, но неверную причину.** Там корнем назван пропуск по имени файла в `_save_image_if_not_exists:426`. Версия опровергнута: у товара 9194 GUID нового фото другой, файл скопирован 25.08, оба пути лежат в `base_images`. Не переделывай работу по п. 24 — верное зерно из него живёт как отложенный AC (см. «Вне объёма»).
> 🟠 **Индекс GitNexus на момент написания стори устарел** (проиндексирован `e583d43`, текущий `1c0fac85`), причём в диапазоне менялся сам `variant_import.py`. **До первой правки выполнить `! npx gitnexus analyze`**, иначе `impact` вернёт координаты несуществующего кода.

## Story

As a **владелец каталога**,
I want **чтобы состав и порядок фотографий товара на сайте повторяли то, что заведено в 1С, а уже перенесённые картинки не считались потерянными**,
so that **обновлённое в 1С фото становилось главным на карточке, снятое — исчезало, а счётчик ошибок импорта означал реальные потери файлов**.

## Acceptance Criteria

1. **AC1 — уже перенесённая копия не считается потерей.** При отсутствии исходного файла `_save_image_if_not_exists` проверяет целевой путь в хранилище: если копия есть — возвращает её путь и учитывает как `images_skipped`. `images_errors` растёт только когда нет ни исходника, ни копии.
2. **AC2 — `Product.base_images` зеркалит `goods.xml`.** При импорте из XML состав и порядок берутся из выгрузки: картинки, которой в выгрузке нет, в списке не остаётся; порядок списка совпадает с порядком тегов `<Картинка>`; первой в списке становится первая картинка 1С.
3. **AC3 — нулевое разрешение состав не меняет.** Если при зеркалировании не разрешилась **ни одна** картинка выгрузки (нет ни исходников, ни копий, либо все отсеяны порогом), `base_images` и состав варианта остаются прежними. Отдельный тест обязателен — это единственная защита от потери каталога при сбое обмена.
4. **AC4 — отсутствие тега `<Картинка>` состав не меняет.** Товар, у которого в `goods_data` нет ключа `images`, проходит импорт с нетронутым `base_images`. Поведение уже обеспечено guard'ом `"images" in goods_data` (`:556`, `:614`) — тест его фиксирует, чтобы правка не сняла.
5. **AC5 — `ProductVariant.main_image` переназначается по `offers.xml`.** При импорте из XML первая картинка выгрузки становится `main_image`, остальные — `gallery_images` в порядке 1С; уже заполненное `main_image` заменяется. Защиты AC3 и AC4 действуют так же.
6. **AC6 — порог размера считается и по копиям в `media`.** `_get_effective_min_size` определяет эффективный порог по фактически доступному файлу: исходнику либо перенесённой копии. Мелкое превью, ранее отсеянное основным порогом 100 КБ, не возвращается в состав после того, как исходники подчищены.
7. **AC7 — режим сканирования каталога остался аддитивным.** Вызовы из `integrations/tasks.py` и `import_images_from_1c` состав не режут и порядок не переставляют. Тест фиксирует: у товара с существующим `base_images` прогон сканирования только дописывает в хвост.
8. **AC8 — `import_images_from_1c` не падает на заполненном `main_image`.** Ветка дедупликации галереи режет путь по `.name`; `TypeError: argument should be a str or an os.PathLike object` не возникает.
9. **AC9 — каждый новый тест падает до правки.** Прогнать и показать RED в Debug Log, иначе тест ничего не ловит.
10. **AC10 — проверено на проде после выката.** ⚠️ Закрывается **после** ручного деплоя и следующей выгрузки 1С, не мержем: статус `done` ставится по прод-замеру, а не по зелёным тестам. `images_errors` в goods-сессии падает с 253 до величины, объяснимой реально отсутствующими файлами; товар 9194 отдаёт `main_image` = `…_fb2f7292-….jpg` (розовая гантель 1,0 кг). Запросы — в `verification.md`.

## Tasks / Subtasks

- [ ] **T0. Пре-флайт (до первой правки)**
  - [ ] `! npx gitnexus analyze` — индекс устарел, в диапазоне менялся `variant_import.py`
  - [ ] `npx gitnexus impact _save_image_if_not_exists --direction upstream`, то же для `_import_base_images`, `_import_variant_images`, `_get_effective_min_size`; сообщить blast radius Alex (ожидание — LOW; на 27.08 по обоим методам состава 9 узлов, 4 прямых вызывающих)

- [ ] **T1. AC1 — копия в хранилище разрешает картинку** (`variant_import.py:380-445`)
  - [ ] Вынести вычисление `destination_path` в helper `_build_destination_path(image_path, destination_prefix)` и вызвать его **до** проверки существования исходника
  - [ ] ⚠️ Helper обязан получать **нормализованный** путь (тот же, что уходит в `_save_image_if_not_exists` из `:684` и `:977`). Подставишь сырой путь из XML — `subdir` станет `import_files`, целевые пути разъедутся у всего каталога: копии перестанут находиться, а импорт начнёт плодить дубли рядом
  - [ ] Ветка «исходника нет»: если `default_storage.exists(destination_path)` — вернуть путь, `images_skipped += 1`; иначе прежнее поведение (`images_errors += 1`, `None`)
  - [ ] Проверку размера применить и к копии (см. T4) — картинка, разрешённая через копию, обязана проходить тот же порог
  - [ ] Тест: исходник удалён, копия в `media` есть → возвращён путь, `images_skipped` вырос, `images_errors` не вырос
  - [ ] Тест-регрессия: нет ни исходника, ни копии → `images_errors` растёт (существующий `test_image_not_found_logs_error` должен остаться зелёным)

- [ ] **T2. AC2 + AC3 + AC4 — зеркалирование `base_images`** (`variant_import.py:647-712`)
  - [ ] Добавить keyword-only параметр `mirror_composition: bool = False` в `_import_base_images`
  - [ ] В режиме зеркала собрать `resolved: list[str]` строго в порядке `image_paths`; при `not resolved` — выйти **не сохраняя** (AC3)
  - [ ] При непустом `resolved` — `product.base_images = resolved`, сохранить только при фактическом отличии (сохранить текущее поведение `if base_images != list(product.base_images or [])`)
  - [ ] Дедупликацию по filename сохранить внутри `resolved` (1С может прислать один файл дважды)
  - [ ] Проставить `mirror_composition=True` в вызовах `:557` и `:615`. Вызовы из `tasks.py` и `import_images_from_1c` **не трогать** — default `False` оставляет их аддитивными (AC7)
  - [ ] Тесты: снятая картинка уходит; порядок совпадает с XML; нулевое разрешение состав не трогает; нет ключа `images` — состав не трогается

- [ ] **T3. AC5 — переназначение `main_image` варианта** (`variant_import.py:934-1002`)
  - [ ] Тот же `mirror_composition: bool = False`; в режиме зеркала снять конструктивный запрет `if not main_image_set` (`:987`)
  - [ ] `resolved[0]` → `main_image`, `resolved[1:]` → `gallery_images`; при `not resolved` — выйти не сохраняя (AC3)
  - [ ] Проставить `mirror_composition=True` в вызовах `:830` и `:917`
  - [ ] Тесты: у варианта с непустым `main_image` первая картинка выгрузки становится главной, прежняя уходит в галерею; нулевое разрешение состав варианта не меняет

- [ ] **T4. AC6 — порог по фактически доступному файлу** (`variant_import.py:362-378`)
  - [ ] Добавить в `_get_effective_min_size` параметр `destination_prefix: str` — без него целевой путь копии не вычислить
  - [ ] Для каждой картинки: если исходника нет — взять размер копии через `default_storage.size(destination_path)`; ошибки хранилища глушить как `OSError` сейчас
  - [ ] `default_storage.size()` вызывать **только** в ветке «исходника нет». Существующий `test_stats_updated_on_image_import` (`test_image_import.py:174`) подменяет весь `default_storage` на `MagicMock` — безусловный вызов `size()` вернёт мок вместо числа и уронит сравнение с порогом
  - [ ] Обновить оба вызова: `:677` → `"base"`, `:970` → `"variants"`
  - [ ] Тест: исходников нет, копии крупные → порог остаётся 100 КБ, мелкая копия в состав не попадает

- [ ] **T5. AC8 — `Path` от `ImageFieldFile`** (`variant_import.py:958`)
  - [ ] `Path(variant.main_image).name` → `Path(variant.main_image.name).name`
  - [ ] Тест: `_import_variant_images` в аддитивном режиме на варианте с непустым `main_image` не падает

- [ ] **T6. AC7 — аддитивность режима сканирования**
  - [ ] Тест: `_import_base_images` без `mirror_composition` у товара с непустым `base_images` только дописывает в хвост и не переставляет порядок

- [ ] **T7. Прогон и проверка**
  - [ ] Показать RED каждого нового теста **до** правки (AC9), зафиксировать в Debug Log
  - [ ] Полный прогон тестов импорта 1С — регрессий нет
  - [ ] `npx gitnexus detect-changes --scope all` перед коммитом
  - [ ] После выката — проверки из `verification.md` (AC10)

## Dev Notes

### Как работает сейчас (проверено чтением на `1c0fac85`)

`_save_image_if_not_exists` (`:380`) — порядок операций: `source_path.exists()` (`:403`) → размер против `effective_min` (`:409-414`) → сборка `destination_path` (`:416-422`) → `default_storage.exists(destination_path)` (`:425-427`, `images_skipped`, возврат пути) → копирование (`:435-441`, `images_copied`). Именно на первой проверке всё и ломается: 1С подчищает `import_files` после импорта (`import_products_from_1c.py:878-904`), а `goods.xml` с теми же товарами приходит снова — и каждая уже перенесённая картинка засчитывается ошибкой.

`_import_base_images` (`:647`) аддитивен: существующий список предзагружается в `base_images`/`seen_filenames` (`:665-675`), новые пути дописываются в хвост (`:694`). `_import_variant_images` (`:934`) аддитивен и не переназначает главное: `main_image_set = bool(variant.main_image)` (`:949`), далее `if not main_image_set` (`:987`).

`_get_effective_min_size` (`:362`) — приватный метод, вызывается только из `:677` и `:970`, внешних вызывающих нет (проверено grep по `backend/`), поэтому смена сигнатуры безопасна. Смотрит только на исходники. После T1 исходников на проде обычно нет — без T4 порог упадёт до резервных 8 КБ и мелкие превью вернутся в состав. **T4 нельзя откладывать: он не улучшение, а компенсация побочного эффекта T1.**

Полная карта кода — в `_bmad-output/specs/spec-image-sync-from-1c/current-behavior.md`.

### Разделение режимов — критично

Оба метода состава вызываются из четырёх мест:

| Вызов | Источник состава | Режим |
|---|---|---|
| `variant_import.py:557`, `:615` | `goods.xml` | **зеркало** |
| `variant_import.py:830`, `:917` | `offers.xml` | **зеркало** |
| `integrations/tasks.py:301`, `:311` | glob по каталогу | аддитивный |
| `import_images_from_1c.py:199`, `:208` | glob по каталогу | аддитивный |

Сканирование каталога источником истины не является — там список собран по остаткам на диске, зеркалирование по нему обрежет всё, чего на диске уже нет. Механизм: keyword-only `mirror_composition: bool = False`. Оба сканирующих вызова передают аргументы по ключевым словам и default подхватят автоматически — их править не нужно.

### Гарантии, которые нельзя потерять

- **Нулевое разрешение ≠ «фото удалили»** (AC3). Сбой обмена, подчищенный каталог, отсеявший всё порог — любой из этих случаев даёт пустой `resolved`, и состав обязан остаться прежним.
- **Нет тега `<Картинка>` ≠ «фото удалили»** (AC4). Guard `"images" in goods_data` уже стоит на `:556` и `:614` — не снимать. Удаление всех фото товара через обмен не поддерживается осознанно: отличить «в 1С сняли всё» от «выгрузка сломалась» нечем.
- **Инкрементальный режим сам защищает остальное:** товара нет в файле — состав не трогается.
- **Ожидаемый побочный эффект, не дефект:** картинка, попавшая в состав только сканированием каталога и отсутствующая в `goods.xml`, после зеркалирующего прогона из списка уйдёт. XML — источник истины, так и задумано.
- **В аддитивном режиме сохранение остаётся безусловным.** `_import_variant_images` сегодня всегда доходит до `variant.save(update_fields=[...])` (`:1000-1001`). Ранний выход добавляется **только** для режима зеркала при пустом `resolved` — не распространяй его на аддитивный путь.

### Тестирование

- Только через Docker с PostgreSQL:
  ```bash
  cd docker && docker compose -p freesport-test -f docker-compose.test.yml run --rm -T backend \
    pytest -xvs apps/products/tests/integration/test_image_composition_sync.py
  ```
  `--env-file` тестовому compose не передаётся; `run --rm`, не `exec`. **Параллельные прогоны по одному compose-проекту невозможны** — одна БД на проект, две сессии дерутся на `TRUNCATE`.
- Новые тесты — в `backend/apps/products/tests/integration/test_image_composition_sync.py`. Маркер `integration` проставится автоматически по каталогу, руками не ставить.
- **`MEDIA_ROOT` обязателен через `override_settings`** — методы пишут в `default_storage`, без изоляции тест насорит в рабочий `media`.
- **Синтетические XML запрещены**, но здесь и не нужны: тестируются сами методы состава, XML в них не участвует. Достаточно временных файлов-заглушек нужного размера (`tmp_path`), см. фикстуру `temp_import_dir` в `test_image_import.py:82`. Учти пороги: заглушка в 17 байт не проходит даже резервный порог 8 КБ — для тестов состава писать файлы **больше 100 КБ**, иначе `resolved` окажется пустым и сработает guard AC3.
- **Существующий `test_image_not_found_logs_error`** (`test_image_import.py:193`) проверяет ровно ту ветку, которую меняет T1. Он обязан остаться зелёным: там нет ни исходника, ни копии, значит `images_errors` по-прежнему растёт. Если тест позеленел «не по той причине» — правка задела больше, чем нужно.

### Проект

- Комментарии и docstrings — **на русском** (стиль проекта).
- **Миграций не требуется** — меняется только логика заполнения существующих полей.
- **GitNexus обязателен**: `impact --direction upstream` до правки каждого из четырёх методов, `detect-changes --scope all` перед коммитом. Только CLI — MCP-инструменты `gitnexus_*` отключены.
- Деплой на прод ручной, CI-шаг всегда skipped; после пересборки backend **обязателен `restart nginx`**, иначе внешний API и обмен 1С уходят в 502.

### Вне объёма этой стори

Вынесено осознанно, каждый пункт — отдельная работа:

- **CAP-5 (латентный):** обновление содержимого при совпадении имени файла — сравнение размера или хэша исходника и копии. Не блокирует эту стори; заменяет собой неверную формулировку `tech-debt.md` п. 24, который после этой стори надо переписать.
- **CAP-6 (эксплуатация):** историческая чистка 104+ товаров. Решение Alex: сначала `python manage.py deduplicate_images --min-size 100 --dry-run`, затем боевой прогон — **после** выката этой стори; следом полная выгрузка номенклатуры из 1С, но **не раньше**, чем остановлен зацикленный обмен, иначе утонет в очереди.
- **CAP-7 (наблюдаемость):** доля неразрешённых картинок в текстовом `report` сессии. `report_details` их пишет (`variant_import.py:2311`), `report` — нет: `report ILIKE '%изображен%'` по всей таблице даёт 0 строк.
- **Сторона 1С:** зацикленный обмен (129 товаров каждые ~5 минут, `created = 0`, ~1 900 сессий в сутки) и 253 картинки, ссылки на которые приходят без файлов. Сторона сайта из подозреваемых исключена: `mode=import` отвечает `success` (`onec_exchange/views.py:275-278`), `ImportOrchestrator.execute` возвращает `True` после постановки задачи (`import_orchestrator.py:54-85`).

### Project Structure Notes

Правки только в `backend/apps/products/services/variant_import.py` (4 метода + 6 вызовов). Ни `integrations/tasks.py`, ни `management/commands/import_images_from_1c.py` менять не требуется — default `mirror_composition=False` сохраняет их поведение. Новый тестовый файл — в существующем каталоге `backend/apps/products/tests/integration/`.

### References

- [Source: _bmad-output/specs/spec-image-sync-from-1c/SPEC.md] — контракт: CAP-1…CAP-8, ограничения, non-goals
- [Source: _bmad-output/specs/spec-image-sync-from-1c/current-behavior.md] — карта кода с `file:line`
- [Source: _bmad-output/specs/spec-image-sync-from-1c/evidence.md] — замеры прода, разбор реальных выгрузок, отклонённая гипотеза
- [Source: _bmad-output/specs/spec-image-sync-from-1c/verification.md] — прогон тестов и проверка на проде
- [Source: _bmad-output/implementation-artifacts/tasks/dev-task-image-sync-from-1c.md] — исходный бриф
- [Source: _bmad-output/planning-artifacts/tech-debt.md#24] — устаревшая формулировка причины, требует переписывания после этой стори
- [Source: project-context.md#4] — стандарты тестирования, автомаркеры, запрет синтетических XML

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
