---
baseline_commit: 13917d4a
---

# Story 41.3: Честный текст согласия в форме подписки

Status: review

> ♻️ **Редакция 2 (2026-08-30). Первая редакция отменена целиком.** Она делила согласие на два чекбокса и правила бэкенд. Разбор с владельцем показал, что посылка была неверной: **подписка в проекте одна и на всё** — деления на «информационную» и «рекламную» рассылки нет и не планируется. Человек, нажимающий «Подписаться» под заголовком «Подписаться на рассылку», приходит именно за рассылкой; спрашивать его отдельной опциональной галочкой, хочет ли он то, что только что запросил, — бессмыслица.
> 🔴 **Настоящий дефект — не в бэкенде, а в тексте.** `backend/apps/common/views.py:427-428` пишет две записи `UserConsent` — `pdp_contract` и `marketing_email`. Это **правильно**. Недостоверна не вторая запись, а **формулировка, которой получено согласие**: единственный чекбокс (`SubscribeForm.tsx:167-180`) говорит только про обработку ПДн и ни слова про рассылку. Согласие на рекламную коммуникацию (ФЗ-38 ст. 18) записывается, а текстом не подтверждено.
> ✅ **Починка — переписать текст чекбокса**, чтобы он покрывал оба смысла. Бэкенд, сериализатор, `openapi.yaml`, типы фронта и восемь backend-тестов **не трогаются**: поведение сервера остаётся прежним.
> 📌 **Происхождение `marketing_email`.** Тип согласия заведён вместе с моделью (`backend/apps/common/models.py:590-593`, миграция `0015_userconsent`) ради **регистрации**: там опциональный чекбокс (`frontend/src/components/auth/RegisterForm.tsx:468-480`) осмыслен — человек регистрируется, чтобы закупаться, реклама для него отдельное желание. Стори 35.3 переиспользовала тот же `consent_type` в подписке, где семантика другая. Регистрация в этой стори **не трогается** (подтверждено Alex 2026-08-30).
> 🗑️ **Хвост снят.** Задача `tasks/dev-task-marketing-consent-journal-cleanup.md` и жёсткий порядок «сначала стори, потом чистка» **неактуальны**: реальных пользователей в БД нет, накопленные записи принадлежат тестовым логинам и удаляются свободно (решение Alex, 2026-08-30).
> ⚠️ **Формы подписки две.** `SubscribeForm` (тема blue, `/home`) и `ElectricSubscribeForm` (тема electric, `/electric`, маршрут в `KNOWN_TOP_LEVEL_ROUTES`). Правится текст в **обеих**.
> ⚠️ **Один frontend-тест сломается от смены текста:** `SubscribeForm.test.tsx:12-15` держит доступное имя чекбокса точной строкой. Тесты electric-формы ищут по регулярке `/обработку моих персональных данных/i` и переживают правку — при условии, что эта подстрока в новом тексте сохранена (она сохранена).
> 🟢 **Blast radius LOW.** Правка текста и разметки в двух компонентах + удаление формы с `coming-soon`. Графовых рисков нет; риск один — доступное имя чекбокса, которым пользуются тесты.

## Story

As a **подписчик**,
I want **видеть в чекбоксе, на что именно я соглашаюсь — и на обработку данных, и на письма**,
so that **записанное в журнале согласие соответствовало тому, что я подтвердил**.

**Закрывает:** FR-41-04, FR-41-05 (в редакции 2).

## Acceptance Criteria

**AC1 (FR-41-05) — один чекбокс, покрывающий оба смысла.**
**Given** форма подписки на главной (`SubscribeForm`, тема blue) и форма подписки темы electric (`ElectricSubscribeForm`)
**When** форма отображается
**Then** в каждой присутствует **ровно один** чекбокс согласия, по умолчанию не отмеченный
**And** его текст: `Я даю согласие на обработку моих персональных данных в соответствии с «Политикой обработки персональных данных» и согласен(на) получать информационные и рекламные рассылки от OPTISPORT по электронной почте`
**And** фрагмент «Политикой обработки персональных данных» остаётся ссылкой на `/privacy-policy` с `target="_blank"` и `rel="noopener noreferrer"`
**And** второй, отдельный маркетинговый чекбокс **не** добавляется

**AC2 (FR-41-05) — чекбокс остаётся обязательным.**
**Given** любая из двух форм
**When** чекбокс не отмечен
**Then** кнопка отправки заблокирована — правило `disabled={isSubmitting || !pdpConsent}` не меняется
**And** при обходе клиентской валидации сервер отвечает `400` с ошибкой в поле `pdp_consent` и текстом «Необходимо согласие на обработку персональных данных.»
**And** ни `Newsletter`, ни записи `UserConsent` не создаются

**AC3 (FR-41-05) — поведение сервера не меняется.**
**Given** успешная подписка
**When** запрос обработан
**Then** создаются **две** записи `UserConsent` — `pdp_contract` и `marketing_email` — как и сейчас
**And** `backend/apps/common/views.py`, `backend/apps/common/serializers.py`, `docs/api/openapi.yaml`, `frontend/src/types/api.ts`, `frontend/src/types/api.generated.ts` и `frontend/src/services/subscribeService.ts` **не изменяются**
**And** ни один из восьми backend-тестов подписки, опирающихся на «две записи», не требует правки

**AC4 (NFR-41-04) — доказуемость согласия.**
**Given** любая успешная подписка
**When** проверяются записи
**Then** обе несут `given_at`, `policy_version`, `ip_address`, `user_agent` и одинаковую привязку (`user` либо `session_key`)
**And** сохраняется атомарность: сбой записи согласия откатывает `Newsletter` и возвращает `503` с `error: "consent_persistence_failed"`
**And** текст, которым согласие получено, теперь покрывает **обе** записи — это и есть предмет стори

**AC5 (FR-41-04) — форма подписки удалена с `coming-soon`.**
**Given** страница `/coming-soon`
**When** она отображается
**Then** формы подписки на ней нет — ни поля email, ни кнопки «Подписаться»
**And** оставшийся текст блока не обещает уведомление о запуске (строка «Узнайте первым о нашем запуске» удаляется вместе с формой)
**And** канал связи `info@optisport.ru` в подвале страницы сохраняется
**And** ставшие неиспользуемыми импорты и состояние удалены, `npx eslint --max-warnings=0` по файлу зелёный

**AC6 (NFR-41-06) — доступность не деградирует.**
**Given** изменённый чекбокс
**When** проверяется доступность
**Then** его доступное имя содержит **весь** текст согласия, включая новый фрагмент про рассылки: все части метки (в blue — префикс, ссылка и новый суффикс) перечислены в `aria-labelledby`
**And** привязка ошибки через `aria-describedby` и `aria-invalid` сохраняется
**And** `axe` не находит нарушений на форме

**AC7 (NFR-41-01) — тесты.**
**Given** изменённый текст
**When** прогоняются frontend-тесты
**Then** константа `PDP_CONSENT_NAME` в `SubscribeForm.test.tsx:12-15` приведена к новому тексту
**And** добавлена проверка, что метка чекбокса называет рассылку по электронной почте (обе формы)
**And** остальные тесты обеих форм зелёные **без правок** — поведение не менялось, менялся текст

**AC8 (границы) — что стори НЕ делает.**
**Then** **не** добавляется поле `marketing_consent` в `SubscribeSerializer` и не правится `views.py`
**And** **не** регенерируются `openapi.yaml` и типы фронта — контракт API не меняется
**And** **не** трогается чекбокс регистрации (`RegisterForm.tsx:468-480`, `B2BRegisterForm`): там опциональность корректна, человек регистрируется не ради рассылки (подтверждено Alex, 2026-08-30)
**And** **не** выносится общий хук `useSubscribeForm`: дублирование логики двух форм остаётся, оно к согласиям отношения не имеет и уходит в техдолг (см. Dev Notes → «Что ушло из стори»)
**And** **не** трогается cookie-баннер (стори 41.1), текст политики в БД (стори 41.7), модель `UserConsent` и её миграции
**And** **не** выполняется чистка журнала — она неактуальна, данные тестовые

## Tasks / Subtasks

- [x] **Task 1. Ветка и baseline**
  - [x] `git switch -c feature/story-41-3-subscribe-consent-text` от `develop` (прямые коммиты в `develop` запрещены)
  - [x] `git rev-parse --short HEAD` → сверить с `baseline_commit: 13917d4a`; при расхождении перечитать координаты из Dev Notes

- [x] **Task 2. `SubscribeForm` (тема blue): текст согласия** (AC1, AC6)
  - [x] `frontend/src/components/home/SubscribeForm.tsx`, блок `<span>` со строк 167-180. Сейчас метка состоит из двух частей: `<label>` (строка 169) + `<Link>` (178). Добавить **третью** — суффикс после ссылки:
        ```
        Я даю согласие на обработку моих персональных данных в соответствии с   ← существующий label
        «Политикой обработки персональных данных»                               ← существующий Link
        и согласен(на) получать информационные и рекламные рассылки от OPTISPORT ← новый label-суффикс
        по электронной почте
        ```
  - [x] Суффикс оформить вторым `<label htmlFor={pdpConsentId}>` с собственным `id` от существующего `consentBaseId` (`React.useId()`), по образцу `ElectricSubscribeForm.tsx:240-242` (`pdpConsentLabelSuffixId`)
  - [x] **Обязательно:** дописать новый id в `aria-labelledby` чекбокса (строка 159) — сейчас там две части, станет три. Без этого доступное имя не будет содержать текст про рассылку и AC6 не выполнен
  - [x] Заголовок формы (строка 129) и подпись (130-131) не трогать
  - [x] Кнопку и правило блокировки не трогать (AC2)

- [x] **Task 3. `ElectricSubscribeForm` (тема electric): текст согласия** (AC1, AC6)
  - [x] `frontend/src/components/home/ElectricSubscribeForm.tsx`, блок `<span>` со строк 227-243. Метка уже состоит из трёх частей (`label` + `Link` + `label`-суффикс «в соответствии с Политикой») — дописать текст про рассылку в **существующий** суффикс (строка 241) либо добавить четвёртую часть
  - [x] Порядок слов темы сохранить: ссылка стоит в середине фразы, регистр — `uppercase` через класс (строка 227), в исходнике текст пишется обычным регистром
  - [x] Если добавляется четвёртая часть — её `id` (от существующего `formBaseId`) дописать в `aria-labelledby` чекбокса
  - [x] Итоговое доступное имя обязано содержать подстроку «обработку моих персональных данных» — по ней ищут существующие тесты (`ElectricSubscribeForm.test.tsx:26-27`)

- [x] **Task 4. Удаление формы с `coming-soon`** (AC5, FR-41-04)
  - [x] `frontend/src/app/ComingSoonClient.tsx`: удалить блок `{/* Email Subscription Form */}` целиком (строки 124-154) вместе с текстом «Узнайте первым о нашем запуске»
  - [x] Удалить `handleSubmit` (строки 14-26) и состояние `email` / `isSubmitting` (строки 11-12)
  - [x] Удалить ставшие неиспользуемыми импорты: `useState`, `Mail`, `ArrowRight`, `toast`, `Toaster`, а также сам элемент `<Toaster />` (строка 32)
  - [x] Оставить: `ShoppingCart`, `Users`, `TrendingUp`, `motion`, переменную `progress`, подвал с `info@optisport.ru`
  - [x] Директиву `'use client'` **сохранить** — компонент продолжает использовать `motion/react`
  - [x] Проверить вёрстку карточки после удаления формы (нижний отступ блока прогресса)
  - [x] `cd frontend && npx eslint src/app/ComingSoonClient.tsx --max-warnings=0` → зелёный

- [x] **Task 5. Frontend-тесты** (AC7, AC6)
  - [x] **Сначала** прогнать оба тест-файла форм на неизменённом коде и зафиксировать число зелёных — правка текста не должна ронять ничего, кроме одной константы
  - [x] `frontend/src/components/home/__tests__/SubscribeForm.test.tsx:12-15`: привести `PDP_CONSENT_NAME` к новому тексту целиком. `PDP_CONSENT_POLICY_LINK_NAME` (строка 15) не меняется — ссылка та же
  - [x] Добавить в оба файла проверку: доступное имя чекбокса содержит `/рассылки от OPTISPORT по электронной почте/i`
  - [x] Проверить, что ссылка на политику по-прежнему находится и несёт `target="_blank"`, `rel="noopener noreferrer"` (тест уже есть — убедиться, что он зелёный)
  - [x] axe-тест на форму (AC6) — шаблон `frontend/src/components/cart/__tests__/accessibility.test.tsx:13-25`
  - [x] Прогон: `cd frontend && npm run test -- src/components/home/__tests__/SubscribeForm.test.tsx src/components/home/__tests__/ElectricSubscribeForm.test.tsx`

- [x] **Task 6. Проверка целиком** (AC7)
  - [x] Frontend: `npm run test`, `npm run lint`, `npx tsc --noEmit`
  - [x] Backend прогонять не обязательно — код бэкенда не менялся; для страховки один прогон `pytest tests/integration/test_common_subscribe_api.py` в тестовом контейнере должен быть зелёным **без единой правки тестов** (это и есть доказательство, что стори бэкенд не задела)
  - [x] Ручная проверка: `docker compose --env-file .env -f docker/docker-compose.yml restart frontend`; открыть `/home` — один чекбокс с полным текстом, ссылка на политику кликается; подписаться; убедиться **в БД**, что записаны две `UserConsent` (`pdp_contract` + `marketing_email`) с одинаковой привязкой; открыть `/electric` — тот же текст в стиле темы; открыть `/coming-soon` — формы нет

- [x] **Task 7. Разовая чистка тестовых согласий** (решение Alex, 2026-08-30)
  - [x] Данные в БД тестовые, реальных пользователей нет — накопленные записи `marketing_email` удаляются без церемоний. Выполняется владельцем или по его явной просьбе, **после** мержа
  - [x] Задача `tasks/dev-task-marketing-consent-journal-cleanup.md` закрыта как неактуальная 2026-08-30 (основания — в шапке файла)
  - [x] **Выполнено 2026-08-30.** В локальной БД `freesport` удалены все тестовые записи: `common_userconsent` — 11 строк (5 `marketing_email` + 6 `pdp_contract`, включая проверочную пару id 13/14 session `pc8mrem6…`), `common_newsletter` — 4 строки (включая `story413check@example.com` id 4 и три `manual-*-35-3@example.com` из стори 35.3). Обе таблицы пусты. Продакшен-БД была чиста изначально (0 записей в `common_userconsent` и `common_newsletter`). Пользователь выбрал полный объём чистки («Все тестовые»), а не только `marketing_email`

- [x] **Task 8. Перед коммитом**
  - [x] `npx gitnexus detect-changes --scope all --repo "C:\Users\1\DEV\FREESPORT"`
  - [x] `File List` сверить с `git diff --name-only 13917d4a..HEAD`, а не с памятью (находка ревью в 41.0 и 41.5 — дважды)
  - [x] Коммит и push — только по явной просьбе владельца

## Dev Notes

### Почему бэкенд правильный, а текст — нет

```python
# backend/apps/common/views.py:427-428 — остаётся как есть
UserConsent.objects.create(consent_type="pdp_contract", **consent_kwargs)
UserConsent.objects.create(consent_type="marketing_email", **consent_kwargs)
```

Человек открывает блок с заголовком «Подписаться на рассылку» (`SubscribeForm.tsx:129`), подписью «Получайте первыми информацию о новинках и акциях» (130-131), вводит email и жмёт «Подписаться» (199). Намерение получать письма выражено самим действием и всей окружающей его надписью. Обе записи ему соответствуют.

Не соответствует **чекбокс**: он говорит исключительно про обработку персональных данных (169-178). По ФЗ-152 ст. 9 и ФЗ-38 ст. 18 согласие подтверждается тем, что человек прочитал и отметил, а не тем, что логично вывести из контекста. Дырка ровно одна — и закрывается одной фразой в метке.

**Связанного согласия здесь нет.** Объединять галочкой разные цели («обработка И передача третьим лицам И профилирование») нельзя; здесь цель одна — отправить человеку письмо, о котором он попросил, а обработка ПДн является техническим условием этой отправки, а не самостоятельной второй целью. Поэтому один обязательный чекбокс корректен, а два (с опциональным вторым) создавали бы состояние «подписка есть, права писать нет».

### Что ушло из стори по сравнению с редакцией 1

| Было в редакции 1 | Судьба |
|---|---|
| Поле `marketing_consent` в `SubscribeSerializer` | Отменено — не нужно |
| Условная запись `marketing_email` в `views.py` | Отменено — текущее поведение верное |
| Строгий разбор булева (`is True`), AC про `"true"`, `1`, `"yes"` | Отменено вместе с полем |
| Регенерация `openapi.yaml` + `npm run generate:types`, CI-гейт контракта | Не задействуется — контракт не меняется |
| Правка восьми backend-тестов + пять новых | Не нужна — сервер ведёт себя как прежде |
| Второй чекбокс в обеих формах | Отменён (решение Alex, 2026-08-30) |
| Хук `useSubscribeForm` (прежний AC13) | Вынесен из стори: дублирование логики двух форм реально, но к согласиям не относится. **Занесено в `deferred-work.md`** (запись от 2026-08-30, первая в файле) — с контрактом хука, границами и критерием поведенческой нейтральности |
| Жёсткий порядок «стори → чистка журнала» | Снят: данные тестовые |
| Удаление формы с `coming-soon` | **Сохранено** — единственная часть редакции 1, пережившая пересмотр |

### Тесты: что сломается и что нет

| Файл | Как ищет чекбокс | Ломается? |
|---|---|---|
| `SubscribeForm.test.tsx:12-15` | точная строка `PDP_CONSENT_NAME` | **Да** — единственная обязательная правка |
| `ElectricSubscribeForm.test.tsx:26-27` | регулярка `/обработку моих персональных данных/i` | Нет — подстрока сохранена |
| `RegisterForm.test.tsx:16-19` | точная строка, но **своей** формы | Нет — регистрацию не трогаем |
| `B2BRegisterForm.test.tsx:334` | регулярка по своей форме | Нет |
| `backend/tests/integration/test_common_subscribe_api.py` (8 тестов на «две записи») | — | Нет — бэкенд не менялся |

Если после правки падает что-то помимо `PDP_CONSENT_NAME` — это сигнал, что затронуто поведение, а не текст. Остановиться и разобраться, а не подгонять тест.

### Доступное имя: где легко ошибиться

В blue-форме имя чекбокса собирается через `aria-labelledby` из **перечисленных** id (`SubscribeForm.tsx:159`), а не из вложенного текста. Добавленный `<label>`-суффикс **не попадёт** в доступное имя автоматически: его id надо дописать в `aria-labelledby`. Симптом ошибки — тест на новый фрагмент текста находит его через `getByText`, но не находит через `getByRole('checkbox', { name: ... })`. В electric-форме та же схема, там уже три части (`226-243`).

### `coming-soon`: что именно удаляется

`frontend/src/app/ComingSoonClient.tsx` — форма фиктивная целиком:

```tsx
// строки 20-25
// Simulate API call
await new Promise(resolve => setTimeout(resolve, 1000));
toast.success('Спасибо! Мы уведомим вас о запуске.');
```

Адрес нигде не сохраняется — обращения к API нет. Пользователь отдаёт email, видит подтверждение и не получает ничего: сбор ПДн без основания, без согласия и без ссылки на политику (FR-41-04, `tasks/intent-site-audit-2026-08-24.md`, строка A4).

После удаления неиспользуемыми становятся `useState`, `Mail`, `ArrowRight`, `toast`, `Toaster` и элемент `<Toaster />`. ESLint запускается с `--max-warnings=0` (`frontend/package.json:9`) — оставленный импорт валит гейт. `'use client'` **остаётся**: компонент использует `motion/react`.

Тестов на `ComingSoonClient` в проекте нет. Ссылки на `coming-soon` вне страницы: `middleware.ts:37` (allowlist), `robots.ts:29` (запрет индексации), `layout.tsx:51` (комментарий про кэш из 41.5) — правок не требуют. Страница остаётся, удаляется только форма.

### Формы: две, обе живые

| Компонент | Тема | Страница | Цепочка |
|---|---|---|---|
| `SubscribeForm` | blue | `/home` | `SubscribeNewsSection` → `HomePage` → `BlueHomePage` |
| `ElectricSubscribeForm` | electric | `/electric` | `ElectricSubscribeSection` → `ElectricHomePage` |

`ACTIVE_THEME=blue` (`.env:118`), корень редиректит на `/home` (`frontend/src/app/page.tsx:4-31`), но `/electric` прямо достижим — он в `KNOWN_TOP_LEVEL_ROUTES` (`frontend/src/middleware.ts:40`) и после 41.0 отдаёт страницу, а не `404`.

### Уроки предыдущих стори эпика

- **Ручная проверка обязательна.** Тест на форму не отличает «текст отрисован» от «текст попал в доступное имя» и тем более от «в БД легли две записи».
- **File List недобирал реально изменённые файлы** и дважды становился находкой ревью — сверять с `git diff --name-only`.
- **Нормативный текст расходился с реализацией.** Если по ходу работы окажется, что AC неверен — править AC отдельным пунктом Change Log, а не «по факту реализации».
- **Стори 35.3 — предшественник этого кода.** Её решение «две записи из одного чекбокса» здесь **подтверждено в части записей** и **отменено в части текста**: две записи остаются, текст согласия переписывается.

### Решения по второму ревью (2026-08-30)

Второй раунд ревью поднял два вопроса, которые **не были дефектами реализации**, а требовали решения владельца о границах. Оба решены Alex 2026-08-30 и зафиксированы здесь, чтобы не всплывать заново.

| Вопрос ревью | Решение | Куда ушло |
|---|---|---|
| NFR-41-04 требует версию текста согласия и источник — их в `UserConsent` нет | Признать NFR покрытым **частично**, недостающее вынести отдельной стори | **Стори 41.9** в `epic-41-site-audit.md` + ключ `41-9-consent-journal-text-version-and-source: backlog` в `sprint-status.yaml`; к NFR-41-04 добавлена пометка «покрыт частично» |
| `pdp_contract` семантически не про подписку | Поведение **не менять**, семантику задокументировать | Комментарий над `CONSENT_TYPE_CHOICES` в `backend/apps/common/models.py` |

**Почему `pdp_contract` остаётся.** Тип читается шире исторической метки — «согласие на обработку ПДн на заявленном правовом основании». Подписка попадает в него законно: обработка адреса является техническим условием отправки письма, о котором человек попросил сам. Основания у двух записей **разные** — ФЗ-152 ст. 9 для обработки и ФЗ-38 ст. 18 для рекламной коммуникации — и отзываются независимо, поэтому свести подписку к одной записи `marketing_email` нельзя: отзыв рекламного согласия не должен обнулять правовое основание хранить адрес до фактического удаления. Переименовывать метку `choices` не стали — это миграция без выигрыша в поведении.

**Отступление от AC8.** AC8 запрещает трогать модель `UserConsent` и её миграции. Добавлен **комментарий** над `CONSENT_TYPE_CHOICES` — ни поля, ни `choices`, ни поведения он не меняет; `makemigrations --check --dry-run` отвечает `No changes detected`, 42 backend-теста подписки зелёные без правок. Отступление сознательное и минимальное: место, где разработчик читает семантику типа, — сама модель, а не файл стори. Зафиксировано отдельной строкой Change Log, как требуют Dev Notes («править AC отдельным пунктом Change Log, а не „по факту реализации“»).

### Project Structure Notes

- Правки `frontend/src/` применяются рестартом контейнера: `docker compose --env-file .env -f docker/docker-compose.yml restart frontend`. Пересбор не нужен — зависимости и `next.config.ts` не трогаются.
- Frontend-тесты — Vitest (`npm run test`), путь поиска `src/**/__tests__/**/*.{test,spec}.{ts,tsx}` (`vitest.config.mts:30`). Пороги покрытия — 65 % по четырём метрикам.
- Backend-тесты — pytest, только в Docker с PostgreSQL: `cd docker && docker compose -p freesport-test -f docker-compose.test.yml run --rm -T backend pytest <путь>` — без `--env-file`, `run --rm`, а не `exec`.
- Комментарии и docstrings нового кода — на русском (NFR-41-03).
- Ветка `feature/*` от `develop`; прямые коммиты в `develop` запрещены. Коммит и push — только по явной просьбе владельца.
- Новых зависимостей и миграций стори не вводит.

### References

- [Source: _bmad-output/planning-artifacts/epic-41-site-audit.md#Functional Requirements] — FR-41-04, FR-41-05 (редакция 2)
- [Source: _bmad-output/planning-artifacts/epic-41-site-audit.md#Story 41.3] — user story и AC-скелет
- [Source: backend/apps/common/views.py:392-465] — эндпоинт подписки, `consent_kwargs`, две записи (427-428) — **не меняется**
- [Source: backend/apps/common/models.py:587-653] — модель `UserConsent`, типы согласий (590-593)
- [Source: frontend/src/components/home/SubscribeForm.tsx:127-201] — форма blue: `aria-labelledby` (159), метка (167-180), кнопка (192-200)
- [Source: frontend/src/components/home/ElectricSubscribeForm.tsx:226-243] — метка формы electric из трёх частей
- [Source: frontend/src/components/home/__tests__/SubscribeForm.test.tsx:12-15] — константа доступного имени, требующая правки
- [Source: frontend/src/components/home/__tests__/ElectricSubscribeForm.test.tsx:26-27] — поиск чекбокса регуляркой
- [Source: frontend/src/components/auth/RegisterForm.tsx:468-480] — опциональный чекбокс регистрации — **вне объёма**
- [Source: frontend/src/app/ComingSoonClient.tsx:11-26,124-154] — фиктивная форма к удалению
- [Source: frontend/src/middleware.ts:30-53] — `KNOWN_TOP_LEVEL_ROUTES`
- [Source: _bmad-output/implementation-artifacts/Story/35-3-consent-checkbox-in-subscribe-form.md:764,804] — обоснование двойной записи (подтверждено) и причина правки обеих форм
- [Source: _bmad-output/implementation-artifacts/tasks/intent-site-audit-2026-08-24.md:21,40] — триаж A4
- [Source: project-context.md] — среда, тестовые команды, язык, frontend-специфика

## Change Log

| Дата | Изменение | Основание |
|---|---|---|
| 2026-08-30 | Стори создана | `bmad-create-story` по эпику 41, FR-41-04, FR-41-05 |
| 2026-08-30 | Добавлен AC13 «единый источник логики подписки» (`useSubscribeForm`) | Решение Alex, 2026-08-30 |
| 2026-08-30 | В AC4 зафиксировано: подписка гейтится только `pdp_consent` | Решение Alex, 2026-08-30 |
| 2026-08-30 | **Редакция 2 — стори переписана целиком.** Отменено деление на два чекбокса и вся бэкендовая часть; вместо этого переписывается текст единственного чекбокса, покрывая и ПДн, и рассылку. Отменены: поле `marketing_consent`, правка `views.py`, регенерация контракта API, правка восьми backend-тестов, хук `useSubscribeForm`. Сохранено: удаление формы с `coming-soon`. Снят жёсткий порядок «стори → чистка журнала» | Круглый стол 2026-08-30: подписка в проекте одна и на всё, отдельное маркетинговое согласие в форме подписки лишено смысла; реальных пользователей в БД нет. Три решения подтверждены Alex: один чекбокс с объединённым текстом, формулировка, регистрация не трогается |
| 2026-08-30 | **Реализация завершена.** Текст чекбокса согласия переписан в обеих формах подписки (blue `SubscribeForm`, electric `ElectricSubscribeForm`) — теперь покрывает и обработку ПДн, и рассылку; в blue добавлена третья часть метки и дописана в `aria-labelledby`. Фиктивная форма подписки удалена с `/coming-soon`. Тесты: `PDP_CONSENT_NAME` приведена к новому тексту, добавлено по два теста в каждый файл (упоминание рассылки в доступном имени + axe). Бэкенд не тронут — 42 backend-теста подписки зелёные без правок | Story 41.3, AC1–AC8 |
| 2026-08-30 | **Task 7 выполнен.** Чистка тестовых согласий в локальной БД `freesport`: удалены 11 строк `common_userconsent` (5 `marketing_email` + 6 `pdp_contract`, включая проверочную пару `story413check@example.com`) и 4 строки `common_newsletter` (включая `story413check@example.com` и три тестовых из стори 35.3). Обе таблицы пусты. Продакшен-БД была чиста изначально | Решение Alex 2026-08-30, явная просьба владельца |
| 2026-08-30 | **Закрыты находки код-ревью — 2 пункта.** (1) Метка electric-формы приведена к дословному тексту AC1: ссылка теперь охватывает фрагмент «Политикой обработки персональных данных», а не «обработку моих персональных данных»; тесты electric-формы получили константы точного доступного имени и отдельную проверку утверждённой формулировки. (2) File List пересобран по полному diff — добавлены `deferred-work.md`, `tasks/dev-task-marketing-consent-journal-cleanup.md`, `planning-artifacts/epic-41-site-audit.md`, `party-mode/.memlog.md`, а также `AGENTS.md` и `CLAUDE.md` (автогенерируемый счётчик GitNexus) | Код-ревью стори 41.3, `Review Findings` |
| 2026-08-30 | **Закрыт второй раунд код-ревью — 4 находки.** Два `[Patch]`: (1) сводная таблица эпика приведена к редакции 2, заодно сняты обоснование приоритета и пункт «вынесено за пределы» с той же отменённой посылкой; (2) Dev Agent Record приведён к состоянию после первого ревью — Completion Notes описывают итоговый текст electric-метки, счётчики тестов исправлены на blue +2 / electric +3 (19 и 15, сверено `grep -c`). Два `[Decision]` закрыты решениями Alex | Второй раунд код-ревью стори 41.3, `Review Findings` |
| 2026-08-30 | **Решение по NFR-41-04: покрыт частично, остаток вынесен.** `UserConsent` не хранит версию текста согласия и источник; правка модели исключена AC8. Заведена **стори 41.9** «Аудитируемость журнала согласий — версия текста и источник» в `epic-41-site-audit.md` и ключ `41-9-consent-journal-text-version-and-source: backlog` в `sprint-status.yaml`; к NFR-41-04 добавлена пометка «покрыт частично» | Решение Alex, 2026-08-30 |
| 2026-08-30 | **Решение по `pdp_contract`: поведение не менять, семантику задокументировать.** Двойная запись подписки подтверждена как корректная — основания разные (ФЗ-152 ст. 9 и ФЗ-38 ст. 18) и отзываются независимо. Отдельный тип `pdp_newsletter` и схлопывание к одной записи `marketing_email` рассмотрены и отклонены | Решение Alex, 2026-08-30 |
| 2026-08-30 | **Отступление от AC8 (зафиксировано осознанно).** AC8 запрещает трогать модель `UserConsent`; добавлен комментарий над `CONSENT_TYPE_CHOICES` в `backend/apps/common/models.py`. Ни поля, ни `choices`, ни поведение не менялись: `makemigrations --check --dry-run` → `No changes detected`, `pytest tests/integration/test_common_subscribe_api.py` → 42 passed без правок, `black`/`flake8` зелёные. AC8 в остальном соблюдён | Решение Alex «задокументировать текущее»; Dev Notes → «Решения по второму ревью» |

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (`claude-opus-5`), workflow `bmad-dev-story`.

### Debug Log References

| Проверка | Команда | Результат |
|---|---|---|
| Baseline обеих форм до правок | `npx vitest run src/components/home/__tests__/SubscribeForm.test.tsx src/components/home/__tests__/ElectricSubscribeForm.test.tsx` | 29 passed (17 blue + 12 electric) |
| RED после правки тестов | та же | 18 failed / 15 passed — падают ровно те, что зависят от текста метки |
| GREEN после правки компонентов | та же | 33 passed (29 baseline + 4 новых) |
| Полный прогон frontend | `npm run test` | 149 файлов, 2602 passed, 16 skipped — регрессий нет |
| Типы | `npx tsc --noEmit` | exit 0 |
| Линт | `npm run lint` (`eslint . --max-warnings=0`) | exit 0 |
| Линт `coming-soon` | `npx eslint src/app/ComingSoonClient.tsx --max-warnings=0` | exit 0 |
| Бэкенд (страховка, AC3) | `docker compose -p freesport-test -f docker-compose.test.yml run --rm -T backend pytest tests/integration/test_common_subscribe_api.py` | **42 passed** без единой правки тестов |
| Живая подписка | `POST /api/v1/subscribe/` (`story413check@example.com`) | `200`, обе записи `UserConsent` в БД |
| Blast radius | `npx gitnexus detect-changes --scope all` | 6 файлов, 5 символов, 0 процессов, **risk: low** |
| **Правки по ревью** — impact electric-формы | `npx gitnexus impact "Function:frontend/src/components/home/ElectricSubscribeForm.tsx:ElectricSubscribeForm" --direction upstream` | 2 затронутых символа (`ElectricSubscribeSection` → `ElectricHomePage`), **risk: LOW** |
| RED после правки electric-теста | `npx vitest run src/components/home/__tests__/ElectricSubscribeForm.test.tsx` | 12 failed / 3 passed — падают ровно тесты, зависящие от текста метки |
| GREEN после правки electric-компонента | `npx vitest run .../ElectricSubscribeForm.test.tsx .../SubscribeForm.test.tsx` | **34 passed** (33 до правок + 1 новый тест на дословность) |
| Полный прогон frontend после правок ревью | `npm run test` | 149 файлов, **2603 passed**, 16 skipped — регрессий нет |
| Типы после правок ревью | `npx tsc --noEmit` | exit 0 |
| Линт после правок ревью | `npm run lint` | exit 0 |
| Живая сборка `/electric` | restart `frontend` + `curl /electric`, grep по `.next/static/chunks` | новый текст в чанке есть, старая формулировка «в соответствии с Политикой и согласен» — **0 вхождений** |
| **Второй раунд ревью** — impact `UserConsent` | `npx gitnexus impact UserConsent --direction upstream --repo <корень>` | 30 затронутых, 18 прямых, 0 процессов, **risk: HIGH** — предупреждение выдано; фактическая правка комментарий-only |
| Миграция от комментария к `CONSENT_TYPE_CHOICES` | `manage.py makemigrations --check --dry-run` | **No changes detected** |
| Бэкенд после правки комментария | `pytest tests/integration/test_common_subscribe_api.py` | **42 passed** без единой правки тестов |
| Линт бэкенда | `black --check apps/common/models.py`, `flake8 apps/common/models.py` | оба зелёные |
| Счётчики тестов форм (сверка File List) | `grep -c "it(" .../SubscribeForm.test.tsx .../ElectricSubscribeForm.test.tsx` | **19** и **15** — подтверждают +2 и +3 |

### Completion Notes List

**Что сделано**

- **AC1, AC6 — `SubscribeForm` (blue).** Метка чекбокса собиралась из двух частей (`label`-префикс + `Link` на политику). Добавлена третья — `label`-суффикс с новым `pdpConsentLabelSuffixId` от того же `React.useId()`, и **его id дописан в `aria-labelledby`** (без этого доступное имя не содержало бы текст про рассылку). Итоговое доступное имя дословно совпадает со строкой из AC1.
- **AC1, AC6 — `ElectricSubscribeForm` (electric).** Метка состоит из трёх частей, `aria-labelledby` не менялся ни разу. Текст доводился в два приёма: сперва в существующий суффикс (`pdpConsentLabelSuffixId`) дописана фраза про рассылку, затем — по находке ревью — перестроены префикс и ссылка, потому что ссылка охватывала фрагмент «обработку моих персональных данных» вместо требуемого AC1 «Политикой обработки персональных данных». **Итоговое состояние:** префикс «Я даю согласие на обработку моих персональных данных в соответствии с», ссылка на `/privacy-policy` — ««Политикой обработки персональных данных»», суффикс «и согласен(на) получать информационные и рекламные рассылки от OPTISPORT по электронной почте». Ссылка осталась в середине фразы (порядок слов темы сохранён), `uppercase` — классом, на доступное имя не влияет. Доступное имя побайтово совпадает с blue-формой и с текстом AC1.
- **AC5 — `/coming-soon`.** Удалены форма целиком, строка «Узнайте первым о нашем запуске», `handleSubmit`, состояние `email`/`isSubmitting`, импорты `useState`, `Mail`, `ArrowRight`, `toast`, `Toaster` и элемент `<Toaster />`. `'use client'` сохранена (компонент использует `motion/react`). Блок прогресса стал последним в карточке — снят его нижний отступ `mb-8`, чтобы паддинг карточки остался симметричным. `info@optisport.ru` в подвале на месте.
- **AC7 — тесты.** В `SubscribeForm.test.tsx` `PDP_CONSENT_NAME` приведена к новому тексту; `PDP_CONSENT_POLICY_LINK_NAME` не менялась. В **оба** файла добавлены тест на упоминание рассылки в доступном имени (`getByRole('checkbox', { name: /рассылки от OPTISPORT по электронной почте/i })`) и axe-тест по шаблону `cart/__tests__/accessibility.test.tsx`. По находке ревью в `ElectricSubscribeForm.test.tsx` дополнительно заведены собственные `PDP_CONSENT_NAME` / `PDP_CONSENT_POLICY_LINK_NAME` (зеркало blue-теста), `getPdpCheckbox` переведён с регулярки на **точное** доступное имя и добавлен третий тест `uses the approved consent wording verbatim`. **Итог: blue 17 → 19 тестов (+2), electric 12 → 15 (+3), всего 34.** Остальные тесты обеих форм зелёные без правок.
- **AC2, AC3, AC4, AC8 — не тронуто.** `views.py`, `serializers.py`, `openapi.yaml`, типы фронта, `subscribeService.ts`, чекбокс регистрации, cookie-баннер, модель `UserConsent` — без изменений. Правило `disabled={isSubmitting || !pdpConsent}` не менялось. Хук `useSubscribeForm` не выносился.

**Проверка на живой БД (AC3, AC4).** Реальная подписка через `POST /api/v1/subscribe/` вернула `200`; в `UserConsent` легли **две** записи — `pdp_contract` и `marketing_email` — с одинаковой привязкой (`session_key=pc8mre…`), `policy_version=1.0`, `ip_address` и `user_agent`. Поведение сервера прежнее.

**Проверка сборки.** Новый текст присутствует в клиентских чанках обеих форм (`.next/static/chunks/src_02b20641._.js` — суффикс blue, `src_f348aca5._.js` — объединённый суффикс electric); строка «Узнайте первым о нашем запуске» из сборки исчезла (0 вхождений).

**Ограничение проверки.** Визуальный осмотр `/home` и `/electric` в браузере выполнить не удалось: расширение Claude in Chrome в этой сессии не подключено, а SSR-HTML этих страниц контент форм не содержит (`BlueHomePage` — клиентский компонент). Текст и доступное имя подтверждены jsdom-тестами (то же дерево доступности, что читает скринридер), присутствием строк в собранных чанках и живой записью в БД. `/coming-soon` отдаётся в SSR и проверен напрямую через `curl`: формы нет, `info@optisport.ru` на месте. **Визуальный осмотр вёрстки двух форм остаётся за владельцем.**

**Task 7 выполнена 2026-08-30.** Разовая чистка тестовых согласий проведена в локальной БД `freesport` после явной просьбы владельца. Удалены все тестовые записи: `common_userconsent` — 11 строк (5 `marketing_email` + 6 `pdp_contract`, включая проверочную пару id 13/14 session `pc8mrem6…` из ручной проверки Task 6), `common_newsletter` — 4 строки (включая `story413check@example.com` id 4 и три `manual-*-35-3@example.com` из стори 35.3). Обе таблицы пусты. Продакшен-БД проверена — была чиста изначально (0 записей). Владелец выбрал полный объём чистки («Все тестовые»), а не только `marketing_email`.

**✅ Устранена находка ревью [Patch]: electric-форма не соответствовала дословному тексту AC1.** До правки метка читалась «Я даю согласие на **обработку моих персональных данных** *(ссылка)* в соответствии с Политикой и согласен(на)…» — ссылка охватывала не тот фрагмент, а слово «Политикой» шло без раскрытия. AC1 требует ссылкой именно «Политикой обработки персональных данных». Метка перестроена по образцу blue-формы: префикс «Я даю согласие на обработку моих персональных данных в соответствии с», ссылка ««Политикой обработки персональных данных»», суффикс «и согласен(на) получать информационные и рекламные рассылки от OPTISPORT по электронной почте». Порядок слов темы сохранён — ссылка по-прежнему в середине фразы; `aria-labelledby` не менялся (те же три части, тот же порядок); `uppercase` остаётся классом, на доступное имя не влияет. Тесты electric-формы усилены: заведены `PDP_CONSENT_NAME` и `PDP_CONSENT_POLICY_LINK_NAME` (зеркало blue-теста), `getPdpCheckbox` ищет чекбокс по **точному** доступному имени вместо регулярки, ссылка проверяется по точному имени, добавлен тест `uses the approved consent wording verbatim`. Обе формы теперь дают побайтово одинаковый текст согласия. Проверено и на живой сборке: старая формулировка исчезла из клиентских чанков (0 вхождений).

**✅ Устранена находка ревью [Patch]: неполный File List.** Список пересобран по `git diff --name-only 13917d4a..HEAD` вместе с `git status --porcelain`, а не по памяти. Добавлены четыре названных ревью файла и ещё два, которые ревью не назвало: `AGENTS.md` и `CLAUDE.md` — там менялся только автогенерируемый блок GitNexus (счётчик символов), к содержанию стори это отношения не имеет, но в diff попадает и потому в списке отражено.

### File List

| Файл | Изменение |
|---|---|
| `frontend/src/components/home/SubscribeForm.tsx` | Изменён — третья часть метки согласия + новый id в `aria-labelledby` |
| `frontend/src/components/home/ElectricSubscribeForm.tsx` | Изменён — текст про рассылку дописан в суффикс метки; по находке ревью префикс и ссылка перестроены под дословный текст AC1 (ссылка охватывает «Политикой обработки персональных данных») |
| `frontend/src/app/ComingSoonClient.tsx` | Изменён — удалена фиктивная форма подписки, её состояние, обработчик и ставшие лишними импорты |
| `frontend/src/components/home/__tests__/SubscribeForm.test.tsx` | Изменён — `PDP_CONSENT_NAME` под новый текст, +2 теста (рассылка в доступном имени, axe) |
| `frontend/src/components/home/__tests__/ElectricSubscribeForm.test.tsx` | Изменён — +3 теста (рассылка в доступном имени, axe, дословность формулировки); поиск чекбокса и ссылки переведён на точные доступные имена через константы `PDP_CONSENT_NAME` / `PDP_CONSENT_POLICY_LINK_NAME` |
| `_bmad-output/implementation-artifacts/sprint-status.yaml` | Изменён — статус стори `ready-for-dev` → `in-progress` → `review` → `in-progress` → `review` → `done` → `in-progress` (второй раунд ревью) → `review`; заведён ключ `41-9-consent-journal-text-version-and-source: backlog`; в журнал добавлена запись о втором раунде |
| `_bmad-output/implementation-artifacts/Story/41-3-separate-pdn-and-marketing-consents.md` | Изменён — чекбоксы задач, Dev Agent Record, File List, Change Log, Review Findings, Status |
| `_bmad-output/implementation-artifacts/deferred-work.md` | Изменён — заведена первая запись техдолга: общий хук `useSubscribeForm` (вынесен из стори) |
| `_bmad-output/implementation-artifacts/tasks/dev-task-marketing-consent-journal-cleanup.md` | Изменён — задача закрыта как неактуальная (данные тестовые, реальных пользователей нет) |
| `backend/apps/common/models.py` | Изменён — **комментарий-only**: семантика `CONSENT_TYPE_CHOICES` и обоснование двойной записи подписки. Поля, `choices` и поведение не менялись, миграции нет |
| `_bmad-output/planning-artifacts/epic-41-site-audit.md` | Изменён — FR-41-05 переписан в редакцию 2, снят «хвост» про жёсткий порядок «стори → чистка»; по второму ревью: строка 41.3 в сводной таблице, обоснование приоритета и пункт «вынесено за пределы» приведены к редакции 2; к NFR-41-04 добавлена пометка «покрыт частично»; **заведена Story 41.9** |
| `_bmad-output/party-mode/memories/installed/.memlog.md` | Изменён — записи круглого стола по стори 41.3 (автоведение party-mode) |
| `AGENTS.md` | Изменён — **не относится к стори**: автогенерируемый блок GitNexus между маркерами `<!-- gitnexus:start -->`, счётчик символов обновился при `npx gitnexus analyze` |
| `CLAUDE.md` | Изменён — то же, что `AGENTS.md`: автогенерируемый счётчик GitNexus |

Список пересобран по `git diff --name-only 13917d4a..HEAD` + `git status --porcelain`, а не по памяти. После второго раунда ревью — **14 файлов**. Файлы кода — 6: пять фронтовых (`SubscribeForm.tsx`, `ElectricSubscribeForm.tsx`, `ComingSoonClient.tsx` и два тест-файла) и один бэкендовый, изменённый **только комментарием** (`apps/common/models.py`). Остальные — артефакты BMAD и автогенерируемые блоки. В `git diff 13917d4a..HEAD` присутствует ещё один файл — `_bmad-output/implementation-artifacts/Story/41-1-cookie-decline-and-change-choice.md`; он **не относится к 41.3** (создан отдельным прогоном `create-story` по стори 41.1, коммит `b23098ec`) и в списке сознательно не значится. Untracked `backend/uv.lock` также не от этой стори — существовал до начала работы.

### Review Findings

- [x] [Review][Decision] NFR-41-04 не обеспечен текущим журналом согласий — эпик требует сохранять дату, **версию текста согласия и источник**, но `UserConsent` содержит только `policy_version` с постоянным default `1.0`, а `subscribe()` не передаёт ни версию фактической формулировки чекбокса, ни источник `newsletter`; поле с текстом/версией UI отсутствует (`epic-41-site-audit.md:145`, `models.py:616-631`, `views.py:421-428`). Нужно решить: расширить backend-аудит либо явно пересмотреть NFR и заявления AC4 о доказуемости.
  - **Решение Alex 2026-08-30: отдельная стори в эпике 41.** Находка подтверждена — `policy_version` действительно константа `1.0`, версии формулировки чекбокса и источника согласия в модели нет. В 41.3 это не чинится: правка `UserConsent` и `views.py` прямо исключена AC8, а миграция модели согласий — не «хвост» текстовой стори. Заведена **стори 41.9 «Аудитируемость журнала согласий — версия текста и источник»** (`epic-41-site-audit.md`, раздел Story 41.9; ключ `41-9-consent-journal-text-version-and-source: backlog` в `sprint-status.yaml`). К NFR-41-04 в эпике добавлена пометка «покрыт частично» со ссылкой на 41.9. **AC4 стори 41.3 остаётся верным как написан** — он утверждает наличие `given_at`, `policy_version`, `ip_address`, `user_agent` и атомарность, и всё это выполняется; он не утверждает, что `policy_version` содержательно версионирует текст чекбокса.
- [x] [Review][Decision] `pdp_contract` семантически не соответствует подписке — модель расшифровывает тип как «обработка ПДн для исполнения договора», тогда как story утверждает, что цель одна — рассылка и обработка ПДн является её техническим условием; подписка всё равно записывает `pdp_contract` (`models.py:590-593`, `views.py:427-428`, эта story:158). Нужно выбрать корректную модель: отдельный тип ПДн для рассылки, одна запись `marketing_email` либо документированное обоснование существующей двойной записи.
  - **Решение Alex 2026-08-30: задокументировать текущее поведение.** Двойная запись признана корректной: `pdp_contract` читается шире своей исторической метки — как «согласие на обработку ПДн на заявленном правовом основании», а подписка является его законным частным случаем (обработка адреса — техническое условие отправки письма, о котором человек попросил сам). Два основания разные (ФЗ-152 ст. 9 и ФЗ-38 ст. 18) и отзываются независимо, поэтому свести их к одной записи `marketing_email` нельзя. Отдельный тип `pdp_newsletter` рассмотрен и отклонён как избыточный — он тянет миграцию `choices` и правку восьми backend-тестов без выигрыша в правовой позиции. **Устранено 2026-08-30:** семантика зафиксирована комментарием над `CONSENT_TYPE_CHOICES` (`backend/apps/common/models.py:588-599`) — там, где её читает разработчик. Метки `choices` не менялись: `makemigrations --check` → `No changes detected`, `pytest tests/integration/test_common_subscribe_api.py` → **42 passed** без правок, `black --check` и `flake8` зелёные. Поведение сервера прежнее, AC3 не нарушен.

- [x] [Review][Patch] Electric-форма не соответствует дословному тексту и linked-фрагменту AC1 [frontend/src/components/home/ElectricSubscribeForm.tsx:228] — ссылка охватывает только «обработку моих персональных данных», а не требуемый фрагмент «Политикой обработки персональных данных»; тест не фиксирует утверждённую формулировку.
  - **Устранено 2026-08-30.** Метка electric-формы приведена к дословному тексту AC1: префикс — «Я даю согласие на обработку моих персональных данных в соответствии с», ссылка на `/privacy-policy` — «Политикой обработки персональных данных», суффикс — «и согласен(на) получать информационные и рекламные рассылки от OPTISPORT по электронной почте». Ссылка осталась в середине фразы (порядок слов темы сохранён), `aria-labelledby` не менялся — части те же три. В `ElectricSubscribeForm.test.tsx` заведены константы `PDP_CONSENT_NAME` и `PDP_CONSENT_POLICY_LINK_NAME` (зеркало blue-теста), `getPdpCheckbox` ищет чекбокс по **точному** доступному имени, проверка ссылки — по точному имени фрагмента политики, добавлен отдельный тест `uses the approved consent wording verbatim`.
- [x] [Review][Patch] File List стори неполон относительно заявленной сверки с полным diff [_bmad-output/implementation-artifacts/Story/41-3-separate-pdn-and-marketing-consents.md:311] — не указаны изменения `deferred-work.md`, `tasks/dev-task-marketing-consent-journal-cleanup.md`, `party-mode/memories/installed/.memlog.md` и `planning-artifacts/epic-41-site-audit.md`.
  - **Устранено 2026-08-30.** File List пересобран по `git diff --name-only 13917d4a..HEAD` + `git status --porcelain`; добавлены все четыре названных файла, а также `AGENTS.md` и `CLAUDE.md` (автогенерируемый счётчик GitNexus), которые ревью не назвало, но которые тоже попадают в diff.
- [x] [Review][Patch] Сводная таблица эпика сохранила отменённое описание «Раздельные согласия подписки», хотя редакция 2 реализует один объединённый чекбокс [_bmad-output/planning-artifacts/epic-41-site-audit.md:219].
  - **Устранено 2026-08-30.** Строка 41.3 в сводной таблице переписана под редакцию 2: «Текст чекбокса подписки покрывает и ПДн, и рассылку (редакция 2); удаление формы с `coming-soon`», слой исправлен с «фронт + бэк» на «фронт» — бэкенд стори не трогает. Сверх названного ревью в том же файле сняты ещё два места с той же отменённой посылкой, противоречившие примечанию «Хвост снят» к FR-41-05: обоснование приоритета («41.3 третьей — останавливает порчу журнала согласий. Блокирующий предшественник для внешней задачи…») и пункт «Вынесено за пределы эпика» про `dev-task-marketing-consent-journal-cleanup.md`. Оба приведены к фактическому состоянию: блокирующей связи нет, задача закрыта как неактуальная, чистка выполнена в Task 7.
- [x] [Review][Patch] Финальный Dev Agent Record не приведён к состоянию после первого ревью: Completion Notes всё ещё приписывают electric-ссылке старый текст и говорят о двух новых тестах, а File List также указывает `+2`, хотя фактически добавлен третий тест на дословность [эта story:294,296,319].
  - **Устранено 2026-08-30.** Completion Notes: пункт про electric-форму переписан — теперь он описывает **итоговое** состояние метки (ссылка охватывает «Политикой обработки персональных данных») и явно говорит, что текст доводился в два приёма, второй — по находке первого ревью. Пункт AC7 дополнен фактическими счётчиками: **blue 17 → 19 (+2), electric 12 → 15 (+3), всего 34**; названы третий тест `uses the approved consent wording verbatim` и перевод `getPdpCheckbox` с регулярки на точное доступное имя. File List: строка `ElectricSubscribeForm.test.tsx` исправлена с `+2` на `+3` с перечислением, строка `ElectricSubscribeForm.tsx` — с упоминанием правки по ревью. Счётчики сверены с кодом (`grep -c "it("` → 19 и 15), а не с памятью.
