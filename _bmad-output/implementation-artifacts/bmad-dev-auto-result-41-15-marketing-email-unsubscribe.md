---
status: blocked
---

# BMad Dev Auto Result

Status: blocked
Blocking condition: intent gaps — условия G1–G5 не закрыты, реализация прямо запрещена постановкой до решений владельца (отправщик не выбран, контракт токена «не согласовывать молча»)

## Вход

- Workflow: `bmad-dev-auto`
- Намерение (invocation): `_bmad-output/implementation-artifacts/Story/41-15-marketing-email-unsubscribe.md` — Story 41.15, требование FR-41-31
- Контекст эпика: `_bmad-output/implementation-artifacts/epic-41-context.md` (кэш невалиден — `epic-41-site-audit.md` новее; не перекомпилировался, так как работа остановлена в шаге 1)
- Предыдущая история эпика: 41.14 — `done` (`Story/41-14-metrika-consent-and-transparency.md`)
- HEAD: `9dc2e5b8b75328763a3b4ba27f8ff4a068b49d27`, ветка `develop`

## Почему остановлено

Постановка 41.15 — это **backlog-заготовка с явным блокером**, а не спецификация, готовая к реализации:

- «**Готовность реализации:** заблокирована — рассылка ещё не запущена».
- «Общий шаг BMAD «ready-for-dev» отложен до закрытия условий ниже».
- «Не добавлять декоративную ссылку в форму подписки и не считать существующий API доказательством завершённого сценария».
- G2 (контракт токена): «Не согласовано; **не выбирать молча**».

Требования READY FOR DEVELOPMENT («No known requirement, acceptance, dependency, or implementation gaps remain unresolved») не выполняются: AC1 и AC4 требуют **реально доставленного** маркетингового письма через реальный путь отправщика, которого в проекте нет. Домысливать отправщика и контракт токена вместо владельца — прямое нарушение постановки.

## Неотвеченные вопросы (условия G1–G5)

| Условие | Что требуется от владельца | Состояние |
|---|---|---|
| G1. Отправщик | Приложение/CRM/сервис, владелец, место шаблона письма, источник получателей, тестовый контур | Рассылка не запущена; отправщик не выбран |
| G2. Токен | Формат, генерация/проверка, срок действия, ротация, повторное использование, поведение старой ссылки после повторной подписки | Не согласовано; постановка запрещает выбирать самостоятельно |
| G3. Синхронизация | Как `Newsletter.is_active=False` исключает адрес из отправок, включая очереди и внешний список; обработка отказов | Зависит от G1 |
| G4. Публичный сценарий | Адрес и тема экрана, состояния, доступ без входа во всех режимах, схема API | UX-checkpoint возможен только после G1–G3 |
| G5. Проверка контекста | Свежий GitNexus, полное чтение окончательных файлов UPDATE, версии зависимостей, регрессионные тесты | Повторять после снятия блокировки |

## Собранные доказательства

- **Отправщика маркетинговой рассылки в репозитории нет.** Поиск по `backend/apps` не находит ни массовой отправки, ни шаблона маркетингового письма: `send_mail`/`EmailMessage` встречаются только в транзакционных путях — `apps/orders/tasks.py`, `apps/users/tasks.py`, `apps/users/services/conflict_resolution.py`, `apps/common/services/reporting.py`, `apps/common/management/commands/test_email.py`.
- **`Newsletter` используется только как хранилище подписки**, без исходящих рассылок: `backend/apps/common/models.py` (модель), `serializers.py`, `views.py`, `admin.py`, `urls.py`.
- **Публичного экрана отписки во frontend нет.** В `frontend/src` совпадения по `unsubscribe`/`отпис` есть только в сгенерированных типах `types/api.generated.ts`; ни страницы, ни сервиса, ни роута.
- **Планирующие артефакты это подтверждают.** `sprint-change-proposal-2026-09-13-remaining-audit-items.md`: «В репозитории нет массового отправщика маркетинговой рассылки, шаблона письма или пользовательского экрана отписки»; «Если рассылка пока не отправляется, Story фиксирует это доказательством и остаётся blocked до появления отправщика; она не создаёт декоративную ссылку в форме подписки».
- **Текущий API — только email-based.** `backend/apps/common/views.py` (`unsubscribe`, POST, `AllowAny`, `UnsubscribeRateThrottle`), `serializers.py` (`UnsubscribeSerializer`), `models.py` (`Newsletter`). Новый token-параметр сам по себе не заработает; старый контракт сохраняется отдельным решением.

## Состояние версий (дополнительный блокирующий фактор)

- Рабочее дерево **грязное**: изменён один файл — сама постановка `_bmad-output/implementation-artifacts/Story/41-15-marketing-email-unsubscribe.md` (+178/−47, не закоммичено с 14.09.2026).
- Текущая ветка — `develop` (защищённая интеграционная). Предыдущие истории эпика велись в `feature/story-41-*`; прямое ведение работы на `develop` не соответствует конвенции проекта.

## Рекомендация

1. Владельцу закрыть G1 и G2 (отправщик + контракт токена) — это внешние решения, агент их не принимает.
2. После G1–G3 провести UX-checkpoint (G4) и только затем переводить 41.15 в `ready-for-dev`.
3. Перед запуском реализации — закоммитить/убрать правку постановки и создать ветку `feature/story-41-15-*`.
4. Пока рассылка не запущена, корректный трекер-статус — `backlog` (схема tracker не поддерживает `blocked`), что и зафиксировано в постановке.
