Step Id: 59

# Технический долг и несоответствия документации (FREESPORT)

Дата: 2026-01-18

### 📋 Список выявленных проблем

1.  **Противоречие в командах импорта 1С:**
    - **Где:** `docs/architecture/20-1c-integration.md` vs `docs/architecture/import-architecture.md`.
    - **Суть:** В одном месте предлагаются команды `import_catalog_from_1c`/`import_offers_from_1c`, в другом используется реализованная `import_products_from_1c`.
    - **Рекомендация:** Стандартизировать документацию под `import_products_from_1c`.

2.  **Неактуальный индекс документации:**
    - **Где:** `docs/index.md`.
    - **Суть:** Множество документов помечены как "To be generated", хотя они уже существуют в папке `docs/architecture/`.
    - **Рекомендация:** Обновить ссылки в `docs/index.md`.

3.  **Безопасность JWT: Stateless Access Tokens:**
    - **Где:** `backend/apps/users/views/authentication.py`.
    - **Суть:** Access токены не инвалидируются немедленно при выходе (действуют до 60 мин).
    - **Рекомендация:** Добавить Redis blacklist для access токенов или сократить TTL до 15-30 мин.

4.  **Безопасность JWT: Race Condition при ротации:**
    - **Где:** `frontend/src/services/api-client.ts`.
    - **Суть:** Одновременный refresh с разных устройств может привести к разлогину из-за `ROTATE_REFRESH_TOKENS=True`.
    - **Рекомендация:** Документировать поведение или рассмотреть sliding sessions.

5.  **Отсутствие функции "Выйти со всех устройств":**
    - **Где:** `backend`.
    - **Суть:** Нет endpoint для массовой инвалидации всех сессий пользователя.
    - **Рекомендация:** Реализовать endpoint `/auth/logout-all/` через очистку `OutstandingToken`.

6.  **Дублирование логики очистки сессии (Frontend):**
    - **Где:** `authStore.ts`, `api-client.ts`, `AuthProvider.tsx`.
    - **Суть:** Логика удаления токенов и очистки состояния дублируется в трех местах.
    - **Рекомендация:** Централизовать в общую функцию `clearAuthState()`.

7.  **Хардкод SITE_URL в Backend:**
    - **Где:** `apps/users/views/authentication.py` (Password Reset).
    - **Суть:** Используется `localhost:3000` в коде вместо `settings.SITE_URL`.
    - **Рекомендация:** Использовать значение из `settings`.

8.  **Middleware: Проверка токена без валидации:**
    - **Где:** `frontend/src/middleware.ts`.
    - **Суть:** Middleware проверяет только наличие cookie `refreshToken`, но не его валидность или срок действия.
    - **Рекомендация:** Добавить проверку JWT (без верификации подписи) на стороне Middleware для UX-улучшения.

9.  **Отсутствие атомарного "Переключения профиля":**
    - **Где:** `frontend/src/stores/authStore.ts`.
    - **Суть:** Нет метода для надежного переключения между аккаунтами без риска оставить старые токены валидными.
    - **Рекомендация:** Реализовать метод `switchAccount()`, обеспечивающий полную очистку старой сессии перед входом в новую.

10. **Безопасность: Cookies не HttpOnly:**
    - **Где:** `frontend/src/stores/authStore.ts`.
    - **Суть:** Cookies доступны через JavaScript, что повышает риск при XSS (хотя это нужно для Middleware).
    - **Рекомендация:** Исследовать возможность использования HttpOnly cookies с проксированием через Next.js API Routes или признать этот риск документированным.

11. **Логирование: console.error в Production:**
    - **Где:** `frontend/src/stores/authStore.ts:106`.
    - **Суть:** Ошибки логаута выводятся напрямую в консоль браузера.
    - **Рекомендация:** Подключить сервис сбора ошибок (Sentry или аналоги) и убрать прямые вызовы `console.error`.

12. **Дублирование логики проверки B2B-ролей (Низкая важность):**
    - **Где:** `frontend/src/components/product/ProductInfo.tsx`, `frontend/src/components/product/ProductSummary.tsx`.
    - **Суть:** Логика `['wholesale_level1', 'wholesale_level2', 'wholesale_level3', 'trainer', 'admin'].includes(userRole)` повторяется в нескольких файлах.
    - **Влияние:** Потенциальная проблема поддержки: при добавлении новой роли придётся обновлять логику в нескольких местах.
    - **Рекомендация:** Вынести в общую утилиту, например `canSeeRrp(userRole)` в `utils/pricing.ts`.

13. **1C Integration: Temp File Cleanup (Garbage Collection):**
    - **Где:** `backend/apps/products/management/commands/cleanup_1c_temp.py` (To be created).
    - **Суть:** Временные файлы в `MEDIA_ROOT/1c_temp/` после завершения импорта или при ошибке остаются на диске. Это может привести к переполнению хранилища.
    - **Рекомендация:** Реализовать management command `cleanup_1c_temp`, который удаляет файлы старше 24 часов (TTL). Запустить через Celery Beat.

14. **1C Import Protocol: Strict Session Creation:**
    - **Где:** `backend/apps/integrations/onec_exchange/views.py`.
    - **Суть:** Нарушение строгой семантики протокола обмена. Сейчас сессия создаётся "лениво" (при `import` или `complete`), вместо строгого создания при `mode=init`. Это приводит к проблемам с пустыми сессиями и требует workaround'ов.
    - **Рекомендация:** Переписать логику `ICExchangeView`: создавать сессию строго в `handle_init`, а в остальных методах (`file`, `import`, `complete`) только использовать существующую или возвращать ошибку, если сессии нет.

15. **1C Import Security: Public Media Root Exposure:**
    - **Где:** `backend/apps/integrations/onec_exchange/import_orchestrator.py`.
    - **Суть:** Файлы импорта сохраняются в `ImportOrchestratorService.import_dir = settings.MEDIA_ROOT / "1c_import"`. `MEDIA_ROOT`, как правило, доступен через Nginx как статика. Это значит, что любой может скачать ваши файлы импорта (цены, остатки, клиенты), просто подобрав URL.
    - **Рекомендация:** Немедленно перенесите хранение файлов импорта в приватную директорию `var/` или другую защищенную локацию за пределами web-root.

16. **Несоответствие логики/данных (Стоимость доставки):**
    - **Где:** `OrderExportService`.
    - **Суть:** Сервис устанавливает сумму документа XML равной `order.total_amount` (которая включает доставку), но список `<Товары>` содержит только физические товары. Если `delivery_cost > 0`, сумма строк не совпадет с суммой документа. Интеграция с 1С упадет при валидации.
    - **Рекомендация:** Добавить виртуальную позицию "Доставка" в список товаров, если стоимость доставки > 0.

17. **Frontend: список оптовых ролей размножен по восьми файлам (расширение п. 12):**
    - **Дата внесения:** 2026-08-01, при планировании эпика 39 («Опт 4»).
    - **Где:** `frontend/src/components/product/ProductInfo.tsx`, `app/(blue)/catalog/page.tsx`, `components/home/{HeroSection,ElectricHeroSection,HitsSection,NewArrivalsSection,PromoSection,SaleSection}.tsx` — плюс `utils/pricing.ts`, `utils/server-auth.ts`, `stores/authStore.ts`, `schemas/authSchemas.ts` как отдельный слой списков B2B-ролей.
    - **Суть:** Список оптовых ролей — копипаста в восьми компонентах. Добавление роли `wholesale_level4` в эпике 39 стоит **восьми одинаковых правок**; пятый уровень будет стоить девяти. Пункт 12 фиксировал ту же проблему на двух файлах — фактический масштаб больше.
    - **Влияние:** Цена каждого нового ценового уровня линейно растёт; пропуск одного файла даёт молчаливый дефект — роль перестаёт считаться оптовой ровно в одном блоке главной страницы, без ошибки сборки.
    - **Решение по эпику 39 (Alex, 2026-08-01):** рефакторинг **не входит** в объём эпика — роль дописывается во все одиннадцать точек как есть. Долг зафиксирован здесь.
    - **Рекомендация:** Свести к одному источнику — предикат в `utils/pricing.ts` (например, `isWholesaleRole(userRole)`), через который выражаются все восемь компонентов; там же закрыть п. 12.

18. ~~**Публичный каталог отдаёт оптовые цены анонимным запросам (непринятая коммерческая политика):**~~ ✅ **ЗАКРЫТ 2026-08-03**
    - **Закрыт стори:** `_bmad-output/implementation-artifacts/Story/security-wholesale-price-visibility.md` (baseline `61e10805`, реализация от `67045968`).
    - **Фактическая форма исправления:** вариант B, как и решено. Появился модуль `backend/apps/products/pricing_policy.py` — единственный источник истины по видимости цен: `resolve_pricing_role(user)`, `can_see_wholesale_prices(user)`, `can_see_info_prices(user)`, константы `WHOLESALE_PRICE_FIELDS` / `INFO_PRICE_FIELDS` / `INFO_PRICE_ROLES`. Через него выражены: гейт обнуления `opt*_price` в `ProductListSerializer.to_representation`, скрытие `rrp`/`msrp` в обоих сериализаторах (дублирующиеся литеральные списки `allowed_roles` удалены — п. (2) рекомендации закрыт), `ProductVariant.get_price_for_user` и ветвление по роли в `ProductFilter.filter_min_price` / `filter_max_price`.
    - **Закрыты обе половины инварианта.** Неверифицированный B2B понижается до `retail` не только в каталоге, но и в `CartItem.price_snapshot`, а через него — в `unit_price` заказа. `cart/` и `orders/` правок не потребовали: цена приходит транзитом.
    - **Тест-сторож (условие Sally) поставлен:** `backend/tests/integration/test_catalog_price_visibility.py` — проверка сформулирована по всем ключам вида `opt*_price`, а не перечислением трёх имён, поэтому `opt4_price` из стори 39.3 попадёт под неё автоматически. Предикаты покрыты `backend/apps/products/tests/unit/test_pricing_policy.py`.
    - **Контракт не изменился (п. (3) рекомендации не потребовался):** схемы `ProductList`/`ProductDetail`/`ProductVariant` в генерации drf-spectacular до и после правки идентичны, `opt1/2/3_price` остались в `properties` и `required`. `docs/api/openapi.yaml` не трогался, типы фронта не пересобирались — ровно та экономия, ради которой долг закрывался до стори 39.3.
    - **Побочный эффект для 39.3 отработан:** координаты Dev Notes и `baseline_commit` в story-файле 39.3 освежены, Task 2.5 и AC4 переформулированы на `pricing_policy.py`, предупреждение в шапке снято.
    - **Остаточное наблюдение (в объём не входило):** `rrp`/`msrp` по-прежнему стоят в `required` схем, хотя вырезаются из ответа для ролей без права — предсуществующий разрыв контракта с ответом. Вариант B выбран в том числе чтобы **не тиражировать** его на оптовые поля. Отдельно: закоммиченный `docs/api/openapi.yaml` в целом отстал от кода (последнее обновление — `73992820`, 2026-07-26), а сам генератор недетерминирован в порядке HTTP-методов внутри путей — побайтовое сравнение регенерации с коммитом бессмысленно, сравнивать нужно `components.schemas`. Это относится к Task 8 стори 39.3.
    - **Дата внесения:** 2026-08-03, при разборе стори 39.3 («Каталог, админка и API отдают цену уровня 4»).
    - **Формулировка:** это не дефект сериализатора, а **коммерческая политика, которую никто не принимал**: оптовая сетка — предмет отдельной договорённости с каждым клиентом, и портал публикует её бесплатно всем. Решение о том, какие ценовые поля видит публичный каталог, до сих пор не принималось явно ни в PRD, ни в архитектуре.
    - **Где:**
      - `backend/apps/products/views.py:55` — `ProductViewSet.permission_classes = [permissions.AllowAny]` («Каталог доступен всем»).
      - `backend/apps/products/serializers.py:488-523` — `ProductListSerializer.Meta.fields` содержит `retail_price`, `opt1_price`, `opt2_price`, `opt3_price` без всяких условий; `ProductDetailSerializer` наследует этот список.
      - `backend/apps/products/serializers.py:575-597` и `:104-115` — `to_representation` вырезает из ответа **только** `rrp` и `msrp` по белому списку `allowed_roles`. Оптовые цены не вырезаются нигде.
      - Проверки верификации B2B нет ни в одном сериализаторе товара: `grep -rn "is_verified" apps/products/` даёт пустой результат.
      - `backend/apps/products/models.py:1188-1203` — `ProductVariant.get_price_for_user` строит `role_price_mapping` только по `user.role`, **флаг верификации не проверяется вовсе**. Значит вторая половина инварианта («неверифицированный B2B видит retail-цены») не реализована тоже: неверифицированный оптовик получает оптовую цену в `current_price`. Гейт только на сырых полях `opt*_price` закроет утечку анонимам, но не этот случай — обе половины должны попасть в AC будущей стори.
    - **Суть:** `GET /api/v1/products/` и `GET /api/v1/products/{slug}/` без токена возвращают все оптовые цены (`opt1_price`, `opt2_price`, `opt3_price`). Это прямо противоречит доменному инварианту `project-context.md` §3: «оптовые цены (`opt1/2/3_price`) и B2B-функции доступны **только верифицированным** пользователям с ролью wholesale/trainer/federation_rep. Неверифицированный B2B видит retail-цены. Проверять флаг верификации в сериализаторах/permissions.» Инвариант записан, но не реализован.
    - **Влияние:**
      - Полная оптовая матрица снимается конкурентом без аутентификации — один обход пагинации (`max_page_size=100`, рейт-лимита на каталоге нет).
      - После стори 39.3 к трём утекающим полям добавляется четвёртое (`opt4_price`, AC2), после стори 39.4 оно приезжает в типы фронта.
      - **Соседняя дверь заперта, а эта — нет.** Антипаттерны стори 39.3 прямо запрещают добавлять `opt4_price` в `ProductVariantSerializer.Meta.fields` со формулировкой «раскрыло бы оптовые цены всем ролям, включая гостей». Риск в стори назван, но защищён только один из двух сериализаторов — тот, который цен и так не отдаёт.
    - **Цена отложенного исправления (контракт):** `docs/api/openapi.yaml` объявляет `opt1_price`, `opt2_price`, `opt3_price` в `required` схемы `ProductList` (и `ProductDetail`). Task 8 стори 39.3 добавляет туда же `opt4_price`. Убрать поле из `required` позднее — **ломающее изменение контракта** с обязательной пересборкой `frontend/src/types/api.generated.ts`. Отдельно отметить: `rrp` и `msrp` **тоже стоят в `required`**, хотя фактически вырезаются для розницы и гостей, — то есть контракт уже расходится с реальным ответом, и приём «вырезать поле в `to_representation`» этот разрыв только тиражирует.
    - **Масштаб правки:** `opt1/2/3_price` упоминаются 95 раз в 20 тестовых файлах (`apps/products/tests/`, `tests/integration/`, `tests/unit/`, `tests/conftest.py`, `tests/factories.py`). Правка видимости затронет как минимум `test_catalog_api.py`, `test_product_detail_api.py`, `test_pricing_integration.py`, `test_b2b_workflow.py`, `test_serializers.py`.
    - **Решение (Alex, 2026-08-03):** закрывается **до** реализации стори 39.3, а не откладывается. Причина порядка: если гейт поставить первым, `opt4_price` попадёт в `openapi.yaml` уже в правильной форме — одна регенерация контракта и типов фронта вместо двух, без ломающего изменения.
    - **Форма исправления — решено (Alex, 2026-08-03): вариант B — отдавать `0.0`.** Роль без права получает `opt*_price: 0.0`, поля остаются в ответе и в `required`. Обоснование: `get_opt1/2/3_price` (`serializers.py:614-633`) **уже** возвращают `0.0` при пустой цене, а по проду `opt1_price` пуст у 52%, `opt2/opt3` — у 54% вариантов (замер 2026-08-03) — то есть ноль давно является штатным ответом «цены нет», фронт его переваривает и падает в `retail` по своему каскаду. Ноль правок `openapi.yaml`, ноль правок типов фронта, ноль ломающих изменений. Отклонён вариант A (`data.pop` по образцу `rrp`/`msrp`) — он требует вынуть поля из `required` и тиражирует уже существующий разрыв контракта с ответом.
    - **Условие принятия варианта B (Sally, принято):** «нет права» и «цены нет» становятся неразличимы, поэтому обязателен **тест-сторож в AC** — анонимный и розничный `GET /api/v1/products/` не содержит ни одного `opt*_price > 0` — плюс комментарий в коде, объясняющий, что `0.0` здесь означает «роль не имеет права», а не «данные не заполнены». Без сторожа гейт снимут обратно как «непонятный».
    - **⚠️ Побочный эффект для стори 39.3:** правка трогает `ProductListSerializer.to_representation` и `Meta.fields` — ровно те места, на которые Dev Notes стори 39.3 ссылаются точными номерами строк (`serializers.py:109`, `:478`, `:514`, `:549`, `:590`) при `baseline_commit: 61e10805`. После закрытия долга номера в стори нужно освежить, иначе дев пойдёт по сдвинутым координатам.
    - **Владелец шага «освежить координаты» (решено 2026-08-03):** **последним таском стори, закрывающей этот долг** — «освежить номера строк и `baseline_commit` в `_bmad-output/implementation-artifacts/Story/39-3-catalog-admin-api-opt4-price.md`». Дублирующая страховка уже стоит в шапке самой стори 39.3 (предупреждение о валидности координат на `61e10805`) — на случай, если таск в стори не попадёт.
    - **Рекомендация:** (1) принять и записать политику таблицей «роль → видимые ценовые поля», включая гостя и неверифицированного B2B; (2) выразить её одним предикатом в `apps/products/` — там же закрыть дублирование белого списка `allowed_roles` из двух сериализаторов; (3) регенерировать `docs/api/openapi.yaml` и типы фронта одним заходом; (4) добавить тест-сторож «анонимный `GET /api/v1/products/` не содержит ни одного `opt*_price`».
