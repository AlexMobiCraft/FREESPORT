---
topic: Синхронизация состава изображений товаров из 1С: импорт аддитивен и не поддерживает состав (удаление, порядок, главное фото)
updated: 2026-08-27T21:00
---

- (event) Создание спеки из dev-брифа _bmad-output/implementation-artifacts/tasks/dev-task-image-sync-from-1c.md (ветка main, коммит 1c0fac85, замеры на проде optisport.ru 2026-08-27); вход структурный и богатый — извлечение прямое, без элиситации
- (decision) slug = image-sync-from-1c, унаследован из имени входного файла dev-task-image-sync-from-1c.md
- (decision) Заголовки kernel остаются английскими (машинный контракт для downstream-скиллов), тело — русское по document_output_language: Русский
- (decision) Корневая причина зафиксирована как «импорт умеет добавлять картинки и не умеет поддерживать состав»: снятое в 1С фото не удаляется, порядок 1С не применяется, главное изображение не переназначается
- (capability) CAP-1 (бриф T1, предусловие остальных): _save_image_if_not_exists при отсутствии исходника признаёт уже перенесённую копию в хранилище валидным результатом и считает её skipped, а не errors; ошибка — только когда нет ни исходника, ни копии
- (capability) CAP-2 (T2): Product.base_images зеркалит состав и порядок тегов <Картинка> из goods.xml — снятое фото уходит, первым становится первое из 1С
- (capability) CAP-3 (T3): ProductVariant.main_image переназначается по offers.xml (первая картинка — главная, остальные — галерея в порядке 1С); снимается конструктивный запрет if not main_image_set
- (capability) CAP-4 (T4): _get_effective_min_size считает порог размера и по перенесённым копиям в media, иначе после CAP-1 порог падает до резервных 8 КБ и мелкие превью возвращаются в состав
- (capability) CAP-5 (T5, латентный, ниже приоритетом, не блокирует CAP-1..CAP-4): обновление содержимого при совпадении имени файла — сравнение размера или хэша исходника и копии
- (capability) CAP-6 (T6): историческая чистка состава у 104+ товаров, у которых первая картинка мельче последующих; способ — развилка Б (не решена)
- (capability) CAP-7 (T7): наблюдаемость — доля неразрешённых картинок выше порога попадает в текстовый report сессии и в лог как предупреждение (сейчас report ILIKE '%изображен%' даёт 0 строк при 195 тыс. ошибок в сутки)
- (capability) CAP-8 (T8, мелкий): variant_import.py:958 Path(variant.main_image) кидает TypeError на ImageFieldFile — резать путь по .name
- (constraint) Порядок обязателен: CAP-1 — предусловие CAP-2/CAP-3/CAP-4; зеркалирование до CAP-1 обрежет каталог, потому что исходников на проде нет и разрешать картинку не по чему
- (constraint) Защита от потери каталога: если ни одна картинка выгрузки не разрешилась — состав не трогать; если у товара нет тега <Картинка> вовсе — состав не трогать (удаление всех фото через обмен не поддерживается осознанно)
- (constraint) Зеркалить только список из XML; режим сканирования каталога (import_images_from_1c, integrations/tasks.py:192) обязан остаться аддитивным — источником истины он не является
- (constraint) Каждый тест обязан падать до правки (RED показать), иначе тест ничего не ловит
- (constraint) MEDIA_ROOT в тестах обязателен через override_settings — методы пишут в default_storage и без изоляции сорят в рабочий media
- (constraint) Тесты состава изображений обходятся временными файлами-заглушками нужного размера (XML в них не участвует); запрет синтетических XML касается тестов импорта 1С — там только реальные файлы из data/import_1c/
- (constraint) GitNexus impact --direction upstream обязателен до правки _import_base_images, _import_variant_images, _save_image_if_not_exists, _get_effective_min_size (на 27.08 — LOW, 9 узлов, 4 прямых вызывающих); перед коммитом detect-changes --scope all; только CLI, MCP отключён
- (constraint) Миграций задача не требует — меняется только логика заполнения существующих полей
- (constraint) Деплой на прод ручной (CI-шаг всегда skipped); после пересборки backend обязателен restart nginx, иначе весь внешний API и обмен 1С уходят в 502
- (decision) Non-goal: чинить сторону 1С (зацикленный инкрементальный обмен, 253 непереданных файла, ~1900 сессий в сутки) — вне бэкенда, вынесено в open questions как T9
- (decision) Non-goal: делать сканирование каталога источником истины и поддерживать удаление всех фото товара через обмен
- (decision) Non-goal: схема БД и миграции; отклонена гипотеза о skip-по-имени как корневой причине (опровергнута фактами, осталась как латентный CAP-5)
- (assumption) Принято, что <Картинка> в goods.xml перечисляет полный актуальный состав фото товара (так устроен CommerceML) — допущение под вопросом развилки А, до её проверки CAP-2 не реализуется
- (assumption) Принято, что 104 товара с мелкой первой картинкой — нижняя оценка масштаба: замена крупного фото на другое крупное этой эвристикой не ловится
- (question) Развилка А (блокирует CAP-2): шлёт ли выгрузка БУС весь состав <Картинка> или только изменённые картинки? Проверяется одним прогоном — зарегистрировать товар 9194 к обмену и перехватить goods.xml до подчистки каталога импортом
- (question) Развилка Б (способ для CAP-6): Б1 полная выгрузка номенклатуры из 1С (точно, но зависит от развилки А и утонет в зацикленном обмене) или Б2 существующая команда deduplicate_images --min-size 100 (быстро, но эвристика по размеру). Рекомендация брифа: Б2 сразу после CAP-1..CAP-4, Б1 следом; Б2 до CAP-1 бессмысленен
- (question) К стороне 1С (T9): почему goods.xml ссылается на 253 картинки, файлы которых не передаются — файлы теряются отдельным архивом или выгрузка картинок выключена, а ссылки остаются?
- (question) К стороне 1С (T9): почему одна и та же порция из 129 товаров уходит каждые 5 минут с created=0, updated=129 — регистрация изменений не снимается?
- (decision) Companions: evidence.md (замеры прода, зацикленный обмен, отклонённая гипотеза, прототип 27.08), current-behavior.md (карта кода с file:line), verification.md (проверка на проде после выката: SQL + curl, контрольный товар 9194); adopted — ../../../project-context.md
- (decision) sources: dev-task-image-sync-from-1c.md поглощён целиком — весь несущий контент перенесён в SPEC.md и три companion-файла
- (event) Self-validate проход 1 (Coherence): пройден. 8 capability, у каждой intent и success; success везде проверяем тестом или SQL/curl-замером; 5 non-goals; все constraints реально сужают решение (порядок CAP-1 первым, защиты от обнуления, аддитивность режима сканирования, RED-first, override_settings, GitNexus, ручной деплой); ID уникальны и не переиспользуются
- (event) Self-validate проход 2 (Preservation): пройден. Пункты брифа §1-§9 разнесены: симптом и замеры -> evidence.md, карта кода с file:line -> current-behavior.md, 10 пунктов контракта приёмки -> success-поля CAP-1..CAP-8 и constraints, §7 ограничения -> Constraints + verification.md + adopted project-context.md, §8 SQL/curl -> verification.md, §9 отклонённая гипотеза и прототип -> evidence.md. Несущего контента не потеряно; отброшены только оболочечные фразы преамбулы
- (event) Спека собрана: SPEC.md + evidence.md + current-behavior.md + verification.md; блокирующая развилка А остаётся открытой — CAP-2 не начинать до её проверки
- (direction) Alex по развилке А: режим выгрузки БУС зависит от настроек — можно полную, можно только изменения; в автоматическом (штатном) режиме выгружаются изменения
- (decision) Развилка А закрыта: фильтр «только изменения» отбирает, КАКИЕ товары попадут в файл, а не урезает состав <Картинка> внутри товара. Доказательство на реальных выгрузках data/import_1c/goods/: атрибут Каталога СодержитТолькоИзменения="true", при этом 187 из 500 товаров в одном файле несут более одной <Картинка> (до 9 у одного товара) — дельта картинок так не выглядит. Зеркалирование (CAP-2) законно на уровне товара
- (constraint) Инкрементальный режим сам обеспечивает главную защиту: товара нет в файле — состав не трогается. Это ровно та же гарантия, что дают два уже записанных guard-а
- (decision) Проверка живого goods.xml переведена из блокера реализации CAP-2 в гейт перед первым прод-прогоном с зеркалированием: перехватить выгрузку товара с несколькими фото и убедиться, что список полный. Реализацию и тесты можно начинать
- (assumption) Товары без тега <Картинка> в выгрузке (289 из 500 в проверенном файле) считаются реально бесфотными, а не «фото не изменились»: доля 211/500 с картинками согласуется с 2467 товарами с непустым base_images на проде. Прямой сверки с БД не делалось
- (direction) Alex по развилке Б: непонятно, что решать — требуется объяснение сути выбора
- (decision) Развилка Б решена в пользу порядка «Б2 затем Б1»: сначала deduplicate_images --dry-run и прогон на 104 товарах сразу после CAP-1..CAP-4 (быстро, без стороны 1С), затем полная выгрузка БУС как приведение к данным 1С — теперь доступна, Alex подтвердил её настройкой. Полную выгрузку не запускать, пока обмен зациклен: она утонет в очереди
- (direction) Alex по вопросу о 129 товарах каждые 5 минут: ответа нет, нужно разбираться — вопрос остаётся открытым как отдельное расследование
- (note) Сторона сайта из подозреваемых по зацикливанию исключена: mode=import возвращает 1С "success" (views.py:275-278), а ImportOrchestrator.execute (import_orchestrator.py:54-85) отдаёт True после постановки задачи — 253 ошибки изображений в failure-ответ не превращаются. Причина невыгрузки регистрации изменений лежит на стороне 1С
- (event) SPEC.md и evidence.md пере-выведены после ответов Alex: развилка А закрыта, развилка Б решена (Б2 -> Б1), открытых вопросов осталось два (зацикливание обмена и 253 непереданных файла)
- (event) Self-validate после обновления: Coherence — пройден (8 capability с intent+success, ID не тронуты, non-goals на месте); Preservation — пройден, ответы Alex и новые факты из data/import_1c/ и onec_exchange/ разнесены в Assumptions, Constraints, Open Questions и evidence.md
- (direction) Alex подтвердил порядок Б2 -> Б1 для CAP-6: сначала deduplicate_images (--dry-run, затем боевой прогон), после — полная выгрузка из 1С. Развилка Б закрыта, рекомендация становится решением
