# Проверка

## Прогон тестов

```bash
cd docker && docker compose -p freesport-test -f docker-compose.test.yml run --rm -T backend \
  pytest -xvs apps/products/tests/<файл>
```

`--env-file` тестовому compose не передаётся; `run --rm`, не `exec`. **Параллельные прогоны по одному compose-проекту невозможны** — одна БД на проект, две сессии дерутся на `TRUNCATE` и дают лавину ложных падений.

Каждый новый тест сначала прогоняется **до** правки и должен упасть (RED). Прогон без RED не доказывает ничего.

## Проверка на проде после выката

```sql
-- 1. Ошибки изображений упали (было 253 на каждую goods-сессию)
SELECT id, created_at,
       report_details->>'images_copied'  AS copied,
       report_details->>'images_skipped' AS skipped,
       report_details->>'images_errors'  AS errors
FROM import_sessions
WHERE report_details ? 'images_copied' ORDER BY created_at DESC LIMIT 10;

-- 2. Контрольный товар
SELECT base_images FROM products WHERE id = 9194;
```

Перекос «мелкая картинка первая» считать скриптом: путь → `os.path.getsize` в `MEDIA_ROOT`, было 104.

Главное изображение через API:

```bash
curl -s https://optisport.ru/api/v1/products/<slug>/ | jq .main_image
```

Для товара 9194 главным должен стать файл `…_fb2f7292-….jpg` (розовая гантель 1,0 кг).

## После выката

После пересборки backend обязателен `restart nginx` — иначе весь внешний API и обмен 1С уходят в 502. Деплой ручной, CI-шаг всегда skipped.

## Результат прод-замера (2026-08-28, после выката `0393e02c`)

Выкат: `git reset --hard origin/main` + пересборка `backend`, `celery`, `celery-beat` (один образ — воркер обязателен, импорт живёт в нём), миграций нет, `restart nginx`. Полная выгрузка номенклатуры из 1С — 20 частей, 9 361 товар обновлён.

### Гейт выката закрыт живой выгрузкой

Файлы 1С подчищаются после импорта, поэтому `goods.xml` перехвачен на проде watcher'ом (`/home/freesport/goods_snapshots`, копии сжаты). Замер по инкрементальному файлу от 05:20 UTC:

| Показатель | Значение |
|---|---|
| Товаров | 129 |
| Тегов `<Картинка>` | 253 |
| `СодержитТолькоИзменения` | `"true"` |
| Товаров с >1 картинкой | 107 (2–5 фото) |

Состав `<Картинка>` внутри товара полный — флаг отбирает товары, а не фотографии. Остаточный риск из `evidence.md` снят.

### AC10 — счётчики

| Метрика | До выката | После |
|---|---|---|
| `images_errors` в goods-сессии | 253 из 253 | 73 (инкрементальная), 1 896 за всю полную выгрузку |
| `images_skipped` | 0 | 180 (инкрементальная), 8 726 за полную выгрузку |
| Товар 9194, `main_image` через API | картинка 14 КБ чужого товара | `…_fb2f7292-….jpg`, 146 663 Б (розовая гантель) |
| Перекос «первая мелкая, дальше крупнее» | 104 | **0** |

Оставшиеся 1 896 ошибок — картинки, на которые 1С ссылается, но файла нет ни в `import_files`, ни в `media` (проверено поштучно). Это сторона 1С, вынесено за объём стори.

### Состав каталога — сверка с XML полной выгрузки

Скриптом сверен `base_images` каждого товара с ожидаемым составом (картинки XML, прошедшие порог, в порядке тегов):

| Проверка | Результат |
|---|---|
| Товаров с картинками в выгрузке | 4 176 |
| Состав и порядок точно совпали с XML | **4 158** |
| Порядок разошёлся | 0 |
| Состав разошёлся | 0 |
| AC3: ни одна картинка не разрешилась | 18 товаров, состав не изменился ни у одного |
| AC4: товаров без тега `<Картинка>` | 5 685, состав не изменился ни у одного |
| Товаров с непустым `base_images` | было 4 163 → стало 4 163 |
| Битых ссылок (файла нет на диске) | 0 |

### AC5 на проде не активируется

В `offers.xml` полной выгрузки — **0** тегов `<Картинка>` на 14 891 предложение: 1С картинки в предложениях не выгружает. Сверка 16 619 вариантов до/после: `main_image` изменился у 0, `gallery_images` — у 0. Код AC5 верен, но на этом обмене его ветка не вызывается — guard `"images" in offer_data` держит состав вариантов.
