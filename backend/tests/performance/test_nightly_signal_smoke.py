"""ВРЕМЕННЫЙ файл — проверка красного пути оповещения nightly.

Тест намеренно падает, чтобы прогон `performance-tests.yml` стал красным и job `notify`
завёл issue с меткой `nightly-failure`. Проверяется то, что нельзя проверить на зелёном
прогоне: создание issue, дедупликация комментарием и автозакрытие.

УДАЛИТЬ сразу после проверки. Маркер `performance` проставляется автоматически по каталогу
(`tests/performance/`), поэтому в PR-гейты этот файл не попадает — там фильтр
`not performance and not slow`.
"""


def test_nightly_failure_signal_smoke():
    """Всегда падает. Единственное назначение — покрасить nightly в красный."""
    assert False, "Намеренное падение: проверка оповещения о падении nightly."
