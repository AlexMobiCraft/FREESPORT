# Метод BMad — Руководство пользователя

Это руководство поможет вам понять и эффективно использовать метод BMad для гибкого планирования и разработки на основе искусственного интеллекта.

## Рабочий процесс BMad: планирование и выполнение

Сначала рассмотрим полный стандартный рабочий процесс планирования и выполнения Greenfield. Brownfield очень похож, но рекомендуется сначала разобраться с Greenfield, даже если речь идет о простом проекте, прежде чем приступать к проекту Brownfield. Метод BMad необходимо установить в корневую папку вашего нового проекта. На этапе планирования вы можете по желанию использовать мощные веб-агенты, что потенциально даст более качественные результаты при меньших затратах, чем если бы вы использовали свой собственный API-ключ или кредиты в некоторых инструментах Agentic. Для планирования мощные модели мышления и более широкий контекст, а также сотрудничество с агентами дадут наилучшие результаты.

Если вы собираетесь использовать метод BMad в проекте Brownfield (существующем проекте), ознакомьтесь с разделом **[Работа в Brownfield](./working-in-the-brownfield.md)**.

Если приведенные ниже диаграммы не отображаются, установите Markdown All in One вместе с плагинами Markdown Preview Mermaid Support в VSCode (или один из форков). С этими плагинами, если вы щелкните правой кнопкой мыши по вкладке при открытии, должна появиться опция «Открыть предварительный просмотр», или проверьте документацию IDE.

### Рабочий процесс планирования (веб-интерфейс или мощные агенты IDE)

Перед началом разработки BMad следует структурированному рабочему процессу планирования, который в идеале выполняется в веб-интерфейсе для обеспечения экономической эффективности:

```mermaid
graph TD
    A["Начало: идея проекта"] --&gt; B{"Дополнительно: исследование аналитика"}
    B --&gt;|Да| C["Аналитик: Мозговой штурм (необязательно)"]
    B --&gt;|Нет| G{"Доступно краткое описание проекта?"}
    C --&gt; C2["Аналитик: Исследование рынка (опционально)"]
    C2 --&gt; C3["Аналитик: Анализ конкурентов (необязательно)"]
    C3 --&gt; D["Аналитик: создание краткого описания проекта"]
    D --&gt; G
    G --&gt;|Да| E["PM: Создать PRD на основе описания (ускоренный процесс)"]
    G --&gt;|Нет| E2["PM: Создание интерактивного PRD (дополнительные вопросы)"]
    E --&gt; F["PRD создан с FR, NFR, этапами и историями"]
    E2 --&gt; F
    F --&gt; F2{"Требуется UX?"}
    F2 --&gt;|Да| F3["Эксперт по UX: создать спецификацию фронт-энда"]
    F2 --&gt;|Нет| H["Архитектор: создать архитектуру на основе PRD"]
    F3 --&gt; F4["Эксперт по UX: сгенерировать подсказку UI для Lovable/V0 (необязательно)"]
    F4 --&gt; H2["Архитектор: создать архитектуру на основе PRD + спецификации UX"]
    H --&gt; Q{"Стратегия раннего тестирования? (необязательно)"}
    H2 --&gt; Q
    Q --&gt;|Да| R["QA: ввод архитектуры раннего тестирования в областях с высоким риском"]
    Q --&gt;|Нет| I
    R --&gt; I["PO: Запустить главный контрольный список"]
    I --&gt; J{"Документы согласованы?"}
    J --&gt;|Да| K["Планирование завершено"]
    J --&gt;|Нет| L["PO: Обновить этапы и истории"]
    L --&gt; M["Обновить PRD/архитектуру по мере необходимости"]
    M --&gt; I
    K --&gt; N["📁 Переключиться на IDE (если используется веб-платформа агента)"]
    N --&gt; O["PO: Разделить документы"]
    O --&gt; P["Готовность к циклу SM/Dev"]

    стиль A заливка:#f5f5f5,цвет:#000
    стиль B заливка:#e3f2fd,цвет:#000
    стиль C заливка:#e8f5e9,цвет:#000
    стиль C2 заливка:#e8f5e9,цвет:#000
    стиль C3 заливка:#e8f5e9,цвет:#000
    стиль D заливка:#e8f5e9,цвет:#000
    стиль E заливка:#fff3e0,цвет:#000
    стиль E2 заливка:#fff3e0,цвет:#000
    стиль F заливка:#fff3e0,цвет:#000
    стиль F2 заливка:#e3f2fd,цвет:#000
    стиль F3 заливка:#e1f5fe,цвет:#000
    стиль F4 заливка:#e1f5fe,цвет:#000
    стиль G заливка:#e3f2fd,цвет:#000
    стиль H заливка:#f3e5f5,цвет:#000
    стиль H2 заливка:#f3e5f5,цвет:#000
    стиль Q заливка:#e3f2fd,цвет:#000
    стиль R заливка:#ffd54f,цвет:#000
    стиль I заливка:#f9ab00,цвет:#fff
    стиль J заливка:#e3f2fd,цвет:#000
    стиль K заливка:#34a853,цвет:#fff
    стиль L заливка:#f9ab00,цвет:#fff
    стиль M заливка:#fff3e0,цвет:#000
    стиль N заливка:#1a73e8,цвет:#fff
    стиль O заливка:#f9ab00,цвет:#fff
    стиль P заливка:#34a853,цвет:#fff
```

#### Переход от веб-интерфейса к IDE

**Критическая точка перехода**: после того как PO подтвердит согласование документа, необходимо перейти с веб-интерфейса на IDE, чтобы начать рабочий процесс разработки:

1. **Копирование документов в проект**: убедитесь, что файлы `docs/prd.md` и `docs/architecture.md` находятся в папке docs вашего проекта (или в пользовательском месте, которое вы можете указать во время установки).
2. **Переключитесь на IDE**: откройте свой проект в предпочитаемом IDE Agentic.
3. **Разбиение документов на фрагменты**: используйте агент PO для разбиения PRD, а затем архитектуры на фрагменты.
4. **Начните разработку**: запустите цикл разработки ядра, который следует ниже.

#### Планирование артефактов (стандартные пути)

```текст
PRD              → docs/prd.md
Архитектура     → docs/architecture.md
Разделенные этапы    → docs/epics/
Разделенные истории  → docs/stories/
Оценки QA   → docs/qa/assessments/
QA Gates         → docs/qa/gates/
```

### Основной цикл разработки (IDE)

После завершения планирования и распределения документов BMad следует структурированному рабочему процессу разработки:

```mermaid
graph TD
    A["Начало этапа разработки"] --&gt; B["SM: Просмотр предыдущих заметок по разработке/контролю качества"]
    B --&gt; B2["SM: Составление чернового варианта следующей истории из фрагментированного Этапа + архитектура"]
    B2 --&gt; S{"История с высоким риском? (Необязательно)"}
    S --&gt;|Да| T["QA: *риск + *дизайн чернового варианта истории"]
    S --&gt;|Нет| B3
    T --&gt; U["Создана стратегия тестирования и профиль рисков"]
    U --&gt; B3{"PO: Проверить черновик истории (необязательно)"}
    B3 --&gt;|Запрошена проверка| B4["PO: Проверить историю по артефактам"]
    B3 --&gt;|Пропустить проверку| C{"Утверждение пользователем"}
    B4 --&gt; C
    C --&gt;|Утверждено| D["Dev: Последовательное выполнение задач"]
    C --&gt;|Требуются изменения| B2
    D --&gt; E["Dev: Реализация задач + тестов"]
    E --&gt; V{"Проверка качества в середине разработки? (Необязательно)"}
    V --&gt;|Да| W["QA: *trace или *nfr для ранней валидации"]
    V --&gt;|Нет| F
    W --&gt; X["Разработка: устранение пробелов в покрытии/NFR"]
    X --&gt; F["Разработка: запустить все проверки"]
    F --&gt; G["Разработка: Пометить как готовое к проверке + Добавить примечания"]
    G --&gt; H{"Проверка пользователем"}
    H --&gt;|Запросить проверку QA| I["QA: Проверка архитектором тестов + контроль качества"]
    H --&gt;|Утвердить без QA| M["ВАЖНО: Проверить, что все регрессионные тесты и линтинг прошли успешно"]
    I --&gt; J["QA: Анализ архитектуры тестирования + Активная рефакторинг"]
    J --&gt; L{"Решение QA"}
    L --&gt;|Требуется работа разработчиков| D
    L --&gt;|Утверждено| M
    H --&gt;|Требуется исправление| D
    M --&gt; N["ВАЖНО: ПРЕДОСТАВЬТЕ СВОИ ИЗМЕНЕНИЯ ПЕРЕД ПРОДОЛЖЕНИЕМ!"]
    N --&gt; Y{"Требуется обновление шлюза?"}
    Y --&gt;|Да| Z["QA: *gate для обновления статуса"]
    Y --&gt;|Нет| K
    Z --&gt; K["Пометить историю как завершенную"]
    K --&gt; B

    стиль A заливка:#f5f5f5,цвет:#000
    стиль B заливка:#e8f5e9,цвет:#000
    стиль B2 заливка:#e8f5e9,цвет:#000
    стиль S заливка:#e3f2fd,цвет:#000
    стиль T заливка:#ffd54f,цвет:#000
    стиль U заливка:#ffd54f,цвет:#000
    стиль B3 заливка:#e3f2fd,цвет:#000
    стиль B4 заливка:#fce4ec,цвет:#000
    стиль C заливка:#e3f2fd,цвет:#000
    стиль D заливка:#e3f2fd,цвет:#000
    стиль E заливка:#e3f2fd,цвет:#000
    стиль V заливка:#e3f2fd,цвет:#000
    стиль W заливка:#ffd54f,цвет:#000
    стиль X заливка:#e3f2fd,цвет:#000
    стиль F заливка:#e3f2fd,цвет:#000
    стиль G заливка:#e3f2fd,цвет:#000
    стиль H заливка:#e3f2fd,цвет:#000
    стиль I заливка:#f9ab00,цвет:#fff
    стиль J заливка:#ffd54f,цвет:#000
    стиль K заливка:#34a853,цвет:#fff
    стиль L заливка:#e3f2fd,цвет:#000
    стиль M заливка:#ff5722,цвет:#fff
    стиль N заливка:#d32f2f,цвет:#fff
    стиль Y заливка:#e3f2fd,цвет:#000
    стиль Z заливка:#ffd54f,цвет:#000
```

## Необходимые условия

Перед установкой BMad Method убедитесь, что у вас есть:

- **Node.js** ≥ 18, **npm** ≥ 9
- **Git** установлен и настроен
- **(Необязательно)** VS Code с расширениями «Markdown All in One» + «Markdown Preview Mermaid Support»

## Установка

### Дополнительно

Если вы хотите выполнить планирование в Интернете с помощью Claude (Sonnet 4 или Opus), Gemini Gem (2.5 Pro) или пользовательских GPT:

1. Перейдите в `dist/teams/`
2. Скопируйте `team-fullstack.txt`
3. Создайте новый Gemini Gem или CustomGPT
4. Загрузите файл с инструкциями: «Ваши важные инструкции по эксплуатации прилагаются, не нарушайте характер, как указано»
5. Введите `/help`, чтобы увидеть доступные команды

### Настройка проекта IDE

```bash
# Интерактивная установка (рекомендуется)
npx bmad-method install
```

## Специальные агенты

Существует два агента BMad — в будущем они будут объединены в одного BMad-Master.

### BMad-Master

Этот агент может выполнять любые задачи и команды, которые могут выполнять все другие агенты, за исключением фактической реализации сценария. Кроме того, этот агент может помочь объяснить метод BMad в Интернете, обратившись к базе знаний и объяснив вам все, что касается этого процесса.

Если вы не хотите переключаться между разными агентами, кроме разработчика, этот агент для вас. Просто помните, что по мере роста контекста производительность агента снижается, поэтому важно дать агенту указание сжать разговор и начать новый разговор с сжатым разговором в качестве начального сообщения. Делайте это часто, желательно после реализации каждого сюжета.

### BMad-Orchestrator

Этот агент НЕ должен использоваться в IDE, поскольку он является тяжеловесным агентом специального назначения, который использует много контекста и может трансформироваться в любой другой агент. Он существует исключительно для облегчения работы команд в рамках веб-пакетов. Если вы используете веб-пакет, вас встретит BMad Orchestrator.

### Как работают агенты

#### Система зависимостей

Каждый агент имеет раздел YAML, который определяет его зависимости:

```yaml
зависимости:
  шаблоны:
    - prd-template.md
    - user-story-template.md
  задачи:
    - create-doc.md
    - shard-doc.md
  данные:
    - bmad-kb.md
```

**Ключевые моменты:**

- Агенты загружают только необходимые ресурсы (оптимизированный контекст)
- Зависимости автоматически разрешаются во время объединения
- Ресурсы совместно используются агентами для обеспечения согласованности

#### Взаимодействие агентов

**В IDE:**

```bash
# Некоторые IDE, такие как Cursor или Windsurf, используют ручные правила, поэтому взаимодействие осуществляется с помощью символа «@».
@pm Создать PRD для приложения по управлению задачами
@architect Разработать архитектуру системы
@dev Реализовать аутентификацию пользователей

# Некоторые IDE, такие как Claude Code, используют вместо этого косые черты
/pm Создание пользовательских историй
/dev Исправить ошибку входа в систему
```

#### Интерактивные режимы

- **Инкрементный режим**: пошаговый режим с вводом данных пользователем
- **Режим YOLO**: быстрое создание с минимальным взаимодействием

## Интеграция с IDE

### Лучшие практики IDE

- **Управление контекстом**: храните только те файлы, которые имеют отношение к контексту, и старайтесь, чтобы файлы были максимально лаконичными и целенаправленными.
- **Выбор агента**: используйте агент, подходящий для задачи
- **Итеративная разработка**: работайте над небольшими, целенаправленными задачами
- **Организация файлов**: поддерживайте чистую структуру проекта
- **Регулярное сохранение**: часто сохраняйте свою работу

## Тестовый архитектор (агент по контролю качества)

### Обзор

Агент QA в BMad — это не просто «старший разработчик-рецензент», а **архитектор тестирования** с глубокими знаниями в области стратегии тестирования, контроля качества и тестирования на основе рисков. Этот агент по имени Куинн предоставляет консультации по вопросам качества и активно улучшает код, когда это безопасно.

#### Быстрый старт (основные команды)

```bash
@qa *risk {story}       # Оценка рисков перед разработкой
@qa *design {story}     # Создать стратегию тестирования
@qa *trace {story}      # Проверка охвата тестированием во время разработки
@qa *nfr {story}        # Проверка атрибутов качества
@qa *review {story}     # Полная оценка → запись в шлюз
```

#### Псевдонимы команд (Test Architect)

В документации для удобства используются сокращенные формы. Оба стиля являются допустимыми:

```текст
*риск    → *профиль риска
*design  → *test-design
*nfr     → *оценка-nfr
*трассировка   → *требования к трассировке (или просто *трассировка)
*review  → *review
*шлюз    → *шлюз
```

### Основные возможности

#### 1. Профилирование рисков (`*risk`)

**Когда:** после составления чернового варианта сценария, до начала разработки (самый ранний момент вмешательства)

Выявляет и оценивает риски реализации:

- **Категории**: технические, безопасность, производительность, данные, бизнес, операционные
- **Оценка**: вероятность × анализ воздействия (шкала от 1 до 9)
- **Смягчение**: конкретные стратегии для каждого выявленного риска
- **Влияние на этап**: риски ≥9 вызывают FAIL, ≥6 вызывают CONCERNS (см. `tasks/risk-profile.md` для авторитетных правил)

#### 2. Разработка теста (`*design`)

**Когда:** после создания чернового варианта истории, до начала разработки (определяет, какие тесты необходимо написать)

Создает комплексные стратегии тестирования, включая:

- Тестовые сценарии для каждого критерия приемки
- Рекомендации по соответствующему уровню тестирования (модульное, интеграционное или E2E)
- Приоритезация на основе рисков (P0/P1/P2)
- Требования к тестовым данным и стратегии моделирования
- Стратегии выполнения для интеграции CI/CD

**Пример результата:**

```yaml
test_summary:
  total: 24
  по_уровню:
    по единицам: 15
    интеграция: 7
    e2e: 2
  по_приоритету:
    P0: 8 # Обязательно — связано с критическими рисками
    P1: 10 # Желательно иметь — средние риски
    P2: 6 # Желательно иметь — низкие риски
```

#### 3. Отслеживание требований (`*trace`)

**Когда:** Во время разработки (промежуточная проверка реализации)

Сопоставление требований с охватом тестирования:

- Документы, в которых тесты проверяют каждый критерий приемки
- Использование Given-When-Then для ясности (только в документации, не в коде BDD)
- Определяет пробелы в покрытии с оценкой серьезности
- Создает матрицу отслеживаемости для целей аудита

#### 4. Оценка NFR (`*nfr`)

**Когда:** во время разработки или на ранней стадии проверки (проверка качественных характеристик)

Проверяет нефункциональные требования:

- **Четыре основных**: безопасность, производительность, надежность, удобство обслуживания
- **Основанные на доказательствах**: поиск фактических доказательств реализации
- **Интеграция контрольных точек**: невыполнение NFR напрямую влияет на контрольные точки качества

#### 5. Комплексный обзор архитектуры тестирования (`*review`)

**Когда:** после завершения разработки, история помечена как «Готова к проверке»

Когда вы запускаете `@qa *review {story}`, Quinn выполняет следующие действия:

- **Отслеживаемость требований**: сопоставляет каждый критерий приемки с соответствующими тестами
- **Анализ уровня тестирования**: обеспечивает надлежащее тестирование на уровне модулей, интеграции и E2E
- **Оценка покрытия**: выявляет пробелы и избыточное покрытие тестов
- **Активная рефакторинг**: непосредственно улучшает качество кода, когда это безопасно
- **Решение о качестве**: выдает статус «ПРОЙДЕНО/ПРЕДУПРЕЖДЕНИЕ/НЕДОСТАТОК» на основе результатов проверки

#### 6. Контрольные точки качества (`*gate`)

**Когда:** после исправления ошибок или когда необходимо обновить статус контроля качества

Управляет решениями по контрольному пункту качества:

- **Детерминированные правила**: четкие критерии для PASS/CONCERNS/FAIL
- **Параллельные полномочия**: отдел контроля качества владеет файлами контроля качества в `docs/qa/gates/`
- **Рекомендательный характер**: дает рекомендации, а не блокирует
- **Поддержка отказов**: документирует приемлемые риски, когда это необходимо

**Примечание:** Ворота носят рекомендательный характер; команды сами выбирают уровень качества. Отказ от проверки требует указания причины, лица, утверждающего отказ, и даты истечения срока. Схема приведена в `templates/qa-gate-tmpl.yaml`, правила ворот — в `tasks/review-story.md`, а система оценки — в `tasks/risk-profile.md`.

### Работа с архитектором тестирования

#### Интеграция с BMad Workflow

Test Architect обеспечивает ценность на протяжении всего жизненного цикла разработки. Вот когда и как можно использовать каждую из его возможностей:

| **Этап**          | **Команда** | **Когда использовать**         | **Ценность**                  | **Результат**                                                     |
| ------------------ | ----------- | ----------------------- | -------------------------- | -------------------------------------------------------------- |
| **Составление истории** | `*риск`     | После того, как SM составил историю   | Раннее выявление подводных камней    | `docs/qa/assessments/{этап}.{story}-risk-{YYYYMMDD}.md`        |
|                    | `*design`   | После оценки рисков   | Руководство разработчикам по стратегии тестирования | `docs/qa/assessments/{этап}.{story}-test-design-{YYYYMMDD}.md` |
| **Разработка**    | `*trace`    | В середине реализации      | Проверить охват тестированием       | `docs/qa/assessments/{этап}.{story}-trace-{YYYYMMDD}.md`       |
|                    | `*nfr`      | Во время создания функций | Раннее выявление проблем с качеством | `docs/qa/assessments/{этап}.{story}-nfr-{YYYYMMDD}.md`         |
| **Проверка**         | `*review`   | История помечена как завершенная   | Полная оценка качества    | Результаты QA в истории + файл gate                                |
| **После проверки**    | `*gate`     | После устранения проблем     | Обновление решения о качестве    | Обновленный файл `docs/qa/gates/{этап}.{story}-{slug}.yml`              |

#### Примеры команд

```bash
# Этап планирования — выполните эти команды ПЕРЕД началом разработки
@qa *risk {draft-story}     # Что может пойти не так?
@qa *design {draft-story}   # Какие тесты мы должны написать?

# Этап разработки — выполните эти действия ВО ВРЕМЯ кодирования
@qa *trace {story}          # Мы тестируем все?
@qa *nfr {story}            # Соответствуем ли мы стандартам качества?

# Этап проверки — выполнять по завершении разработки
@qa *review {story}         # Комплексная оценка + рефакторинг

# После проверки — запускается после устранения проблем
@qa *gate {story}           # Обновление статуса
```

### Применяемые стандарты качества

Quinn соблюдает следующие принципы качества тестирования:

- **Отсутствие нестабильных тестов**: обеспечение надежности за счет правильной обработки асинхронных операций
- **Отсутствие длительного ожидания**: только динамические стратегии ожидания
- **Безсостоятельность и параллельная безопасность**: тесты выполняются независимо
- **Самоочистка**: тесты управляют своими собственными тестовыми данными
- **Соответствующие уровни тестирования**: модульное тестирование для логики, интеграционное тестирование для взаимодействий, E2E для путей
- **Явные утверждения**: утверждения должны оставаться в тестах, а не в вспомогательных программах

### Значения статуса шлюза

- **PASS**: все критические требования выполнены, нет блокирующих проблем
- **ПРОБЛЕМЫ**: обнаружены некритические проблемы, команда должна провести проверку
- **FAIL**: Критические проблемы, которые необходимо устранить (риски безопасности, отсутствующие тесты P0)
- **ОТКАЗАНО**: проблемы признаны, но явно приняты командой

### Особые ситуации

**Истории с высоким риском:**

- Всегда запускайте `*risk` и `*design` перед началом разработки
- Рассмотрите возможность использования контрольных точек `*trace` и `*nfr` в середине разработки

**Сложные интеграции:**

- Запустите `*trace` во время разработки, чтобы убедиться, что все точки интеграции протестированы
- Проведите последующую проверку с помощью `*nfr`, чтобы подтвердить производительность всех интеграций

**Критически важно для производительности:**

- Запускайте `*nfr` на ранних этапах и часто во время разработки
- Не ждите до момента проверки, чтобы обнаружить проблемы с производительностью

**Браунфилд/устаревший код:**

- Начните с `*risk`, чтобы выявить опасности регрессии
- Используйте `*review`, уделяя особое внимание обратной совместимости

### Лучшие практики

- **Раннее вовлечение**: запускайте `*design` и `*risk` во время составления чернового варианта истории
- **Ориентация на риски**: приоритезация тестов должна определяться оценками рисков
- **Постепенное совершенствование**: используйте отзывы от отдела контроля качества для улучшения будущих историй
- **Прозрачность контрольных точек**: делитесь решениями по контрольным точкам с командой
- **Непрерывное обучение**: документируйте шаблоны QA для обмена знаниями в команде
- **Уход за существующими системами**: уделяйте особое внимание рискам регрессии в существующих системах

### Справочник по путям вывода

Краткое справочное руководство по местам хранения результатов Test Architect:

```text
risk-profile  → docs/qa/assessments/{этап}.{story}-risk-{YYYYMMDD}.md
*test-design   → docs/qa/assessments/{этап}.{story}-test-design-{YYYYMMDD}.md
*trace         → docs/qa/assessments/{этап}.{story}-trace-{YYYYMMDD}.md
*оценка-nfr    → docs/qa/assessments/{этап}.{story}-nfr-{YYYYMMDD}.md
*review        → раздел «Результаты QA» в истории + ссылка на файл gate
*gate          → docs/qa/gates/{этап}.{story}-{slug}.yml
```

## Система технических предпочтений

BMad включает в себя систему персонализации через файл `technical-preferences.md`, расположенный в `.bmad-core/data/`. Это может помочь PM и архитектору рекомендовать ваши предпочтения в отношении шаблонов проектирования, выбора технологий или всего остального, что вы хотели бы здесь указать.

### Использование с веб-пакетами

При создании пользовательских веб-пакетов или загрузке на платформы искусственного интеллекта включите в них файл `technical-preferences.md`, чтобы агенты с самого начала любого разговора располагали вашими предпочтениями.

## Основная конфигурация

Файл `bmad-core/core-config.yaml` является важной конфигурацией, которая позволяет BMad беспрепятственно работать с различными структурами проектов. В будущем будет доступно больше опций. В настоящее время наиболее важным является раздел списка devLoadAlwaysFiles в yaml.

### Файлы контекста разработчика

Определите, какие файлы агент разработчика должен всегда загружать:

```yaml
devLoadAlwaysFiles:
  - docs/architecture/coding-standards.md
  - docs/architecture/tech-stack.md
  - docs/architecture/project-structure.md
```

Вам необходимо проверить, что эти документы существуют в вашей архитектуре, что они максимально лаконичны и содержат именно ту информацию, которую вы хотите, чтобы ваш агент разработчика ВСЕГДА загружал в свой контекст. Это правила, которым будет следовать агент.

По мере роста вашего проекта и появления в коде последовательных шаблонов, стандарты кодирования должны быть сокращены, чтобы включать только те стандарты, которые агент по-прежнему должен применять. Агент будет анализировать окружающий код в файлах, чтобы определить стандарты кодирования, которые имеют отношение к текущей задаче.

## Получение помощи

- **Сообщество Discord**: [Присоединяйтесь к Discord](https://discord.gg/gk8jAdXWmj)
- **Проблемы GitHub**: [Сообщите об ошибках](https://github.com/bmadcode/bmad-method/issues)
- **Документация**: [Просмотреть документацию](https://github.com/bmadcode/bmad-method/docs)
- **YouTube**: [Канал BMadCode](https://www.youtube.com/@BMadCode)

## Заключение

Помните: BMad предназначен для улучшения процесса разработки, а не для замены вашего опыта. Используйте его как мощный инструмент для ускорения ваших проектов, сохраняя при этом контроль над проектными решениями и деталями реализации.
