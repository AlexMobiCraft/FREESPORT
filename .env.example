# Пример файла переменных окружения для FREESPORT
# Скопируйте этот файл в .env и заполните реальными значениями

# === ОБЩИЕ НАСТРОЙКИ ===
DJANGO_ENVIRONMENT=development
SECRET_KEY=your-super-secret-key-change-this-in-production

# === НАСТРОЙКИ БАЗЫ ДАННЫХ ===
# Для разработки используется SQLite (настройки ниже игнорируются)
# Для staging/production настройте PostgreSQL:
DB_NAME=freesport
DB_USER=postgres
DB_PASSWORD=password123
DB_HOST=localhost
DB_PORT=5432
DB_SSLMODE=prefer

# === REDIS CACHE ===
REDIS_PASSWORD=password123
REDIS_URL=redis://localhost:6379/0

# === CELERY SETTINGS ===
CELERY_BROKER_URL=redis://:password123@redis:6379/0

# === НАСТРОЙКИ ДОМЕНОВ (PRODUCTION) ===
# Список разрешенных хостов через запятую
ALLOWED_HOSTS=localhost,127.0.0.1,api.your-domain.com

# Список разрешенных CORS доменов через запятую
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# === НАСТРОЙКИ EMAIL ===
# Development: по умолчанию используется console backend (письма выводятся в консоль)
# Для тестирования с Mailhog раскомментируйте:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=mailhog
# EMAIL_PORT=1025
# EMAIL_USE_TLS=False

# Production: SMTP настройки
# --------------------------
# Для Gmail:
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-16-digit-app-password
# (Создать App Password: https://support.google.com/accounts/answer/185833)

# Для Yandex (рекомендуется для freesport.ru):
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=noreply@freesport.ru
EMAIL_HOST_PASSWORD=your-yandex-password
DEFAULT_FROM_EMAIL=noreply@freesport.ru
SERVER_EMAIL=noreply@freesport.ru

# Список email администраторов (через запятую)
# Получат уведомления о новых B2B заявках и ошибках сервера
ADMIN_EMAILS=admin1@freesport.ru,admin2@freesport.ru

# === НАСТРОЙКИ БЕЗОПАСНОСТИ (PRODUCTION) ===
DEBUG=False
SECURE_SSL_REDIRECT=True

# === НАСТРОЙКИ ЛОГИРОВАНИЯ СИНХРОНИЗАЦИИ ===
# Период хранения логов (в днях)
SYNC_LOG_RETENTION_DAYS=90
# Email получателей отчетов (через запятую)
SYNC_REPORT_EMAILS=ops@your-domain.com
# Cron расписание отправки отчетов
SYNC_REPORT_SCHEDULE=0 9 * * *
# Размер пакета при пакетной обработке логов
SYNC_LOG_BATCH_SIZE=1000
# Формат логов (text или json для интеграции с ELK)
SYNC_LOG_FORMAT=text

# === НАСТРОЙКИ МОНИТОРИНГА И АЛЕРТОВ ===
# Включить метрики Prometheus (true/false)
PROMETHEUS_METRICS_ENABLED=false
# Webhook для критических алертов (оставьте пустым, если не используется)
WEBHOOK_ALERT_URL=

# === НАСТРОЙКИ ДЛЯ 1С ИНТЕГРАЦИИ (БУДЕТ ДОБАВЛЕНО ПОЗЖЕ) ===
# ONEC_API_URL=http://your-1c-server/api
# ONEС_API_USER=api_user
# ONEС_API_PASSWORD=api_password

# === НАСТРОЙКИ ЮКАССА (БУДЕТ ДОБАВЛЕНО ПОЗЖЕ) ===
# YOOKASSA_SHOP_ID=your-shop-id
# YOOKASSA_SECRET_KEY=your-secret-key

# === URL САЙТА ===
# URL сайта для ссылок в email (админ-панель, фронтенд)
SITE_URL=https://freesport.ru

# === НАСТРОЙКИ FRONTEND (Next.js) ===
# URL API бэкенда
NEXT_PUBLIC_API_URL=http://localhost:8001/api/v1
# Feature flags
NEXT_PUBLIC_PROMO_ENABLED=false

# === НАСТРОЙКИ ТЕМЫ ===
# Выбор активной темы для корневого URL (/)
# Варианты: coming_soon | blue | electric_orange
ACTIVE_THEME=coming_soon