# Story 9.7: Импорт только изображений товаров

## Metadata
- **Epic**: Epic 9 - Интеграция с 1С
- **Story ID**: 9.7
- **Story Type**: Enhancement (Расширение функционала)
- **Priority**: Medium
- **Estimated Effort**: 5 SP
- **Assigned To**: Backend Developer
- **Status**: Requires Rework
- **Created**: 2025-01-11
- **Dependencies**:
  - Story 3.1.2 (Импорт изображений товаров в media storage)
  - Story 9.6 (Страница импорта данных из 1С)

---

## PO Review (2025-11-11)
- **Статус**: Требует доработки (Requires Rework)
- **Ревьюер**: Sarah (Product Owner)

### Резюме
Это черновик истории высокого качества. Однако, для утверждения и передачи в разработку, требуется несколько критических уточнений.

### Необходимые изменения:
1.  **[Критично] Уточнить логику команды (AC4):** Текущая команда `import_catalog_from_1c --file-type=goods` выглядит так, как будто она обрабатывает и XML-файлы, что противоречит цели импорта "только изображений". Пожалуйста, модифицируйте команду (например, добавьте флаг `--images-only`), чтобы гарантировать, что она обрабатывает **только** изображения и не затрагивает данные о товарах.
2.  **[Риск] Рефакторинг задачи Celery:** Функция `run_selective_import_task` использует `return` внутри цикла. Это потенциальный баг, который не позволит выполнить несколько типов импорта одновременно. Логику необходимо переработать для корректной обработки всех выбранных типов за один запуск.
3.  **[Улучшение] Детализировать отчетность (AC5):** Статистику следует сделать более подробной. Пожалуйста, расширьте `report_details`, включив списки имен файлов для пропущенных изображений (например, по причине дублирования или отсутствия товара), чтобы упростить диагностику.
4.  **[Вопрос] Политика обновления главного изображения (AC7):** История утверждает, что `main_image` никогда не обновляется. Пожалуйста, рассмотрите, нужна ли нам опция (например, чекбокс), чтобы позволить администраторам принудительно перезаписывать главное изображение при импорте.

После внесения этих исправлений история будет готова к утверждению.

---

## User Story

**Как** администратор системы
**Я хочу** импортировать только изображения товаров без повторного импорта всего каталога
**Чтобы** быстро обновлять изображения существующих товаров при получении новых фото от поставщиков

---

## Business Context

### Проблема
В настоящее время импорт изображений товаров возможен только при полном импорте каталога (Story 9.6, тип "Полный каталог"). Это приводит к:
- Излишней нагрузке на систему при обработке всего каталога
- Длительному времени выполнения (когда нужно обновить только фото)
- Риску перезаписи данных каталога при необходимости обновить только изображения

### Решение
Добавить отдельный тип импорта "Изображения" на странице импорта данных из 1С, который:
- Обрабатывает только файлы изображений из `goods/import_files/`
- Обновляет только поля `main_image` и `gallery_images` у существующих товаров
- Использует существующую логику импорта изображений из Story 3.1.2
- Валидирует наличие товаров в БД перед импортом

### Бизнес-ценность
- ⚡ Ускорение процесса обновления изображений (минуты вместо часов)
- 🎯 Точечное обновление без риска изменения данных каталога
- 📊 Прозрачная статистика по импорту изображений
- 🔄 Удобство для операторов при работе с фото от поставщиков

---

## Acceptance Criteria

### AC1: Отображение нового типа импорта в UI
**Given** администратор открывает страницу `/admin/integrations/import_1c/`
**When** страница загружается
**Then** отображается новый тип импорта с параметрами:
- **Value**: `images`
- **Label**: "Изображения"
- **Description**: "Обновление изображений товаров"
- **Files**: "Файлы в директории: goods/import_files/ХХ/*.jpg"
- **Warning**: "⚠️ Требуется предварительный импорт каталога"

### AC2: Валидация зависимостей
**Given** администратор выбирает тип "Изображения"
**When** нажимает кнопку "Запустить импорт"
**Then** система проверяет наличие товаров в БД:
- Если `Product.objects.exists() == False` → показать ошибку:
  *"⚠️ Невозможно загрузить изображения: каталог товаров пуст. Сначала импортируйте полный каталог."*
- Если товары существуют → продолжить импорт

### AC3: Создание ImportSession с правильным типом
**Given** валидация пройдена успешно
**When** запускается импорт изображений
**Then** создается `ImportSession` с параметрами:
- `import_type = ImportType.IMAGES`
- `status = Status.PENDING`
- `initiated_by = request.user`
- `celery_task_id` установлен

### AC4: Запуск Celery задачи с правильными параметрами
**Given** создана ImportSession для типа IMAGES
**When** запускается Celery задача `run_selective_import_task`
**Then** задача вызывает команду:
```bash
python manage.py import_catalog_from_1c --file-type=goods
```
**And** команда обрабатывает только `goods/goods_*.xml` и изображения из `goods/import_files/`

### AC5: Сохранение статистики импорта изображений
**Given** импорт изображений выполнен
**When** задача завершается
**Then** `ImportSession.report_details` содержит:
```json
{
  "type": "images",
  "message": "Изображения товаров импортированы",
  "stats": {
    "images_copied": 120,
    "images_skipped": 30,
    "images_errors": 5
  }
}
```

### AC6: Отображение статистики на странице сессий
**Given** импорт завершен со статистикой изображений
**When** администратор открывает детали ImportSession
**Then** отображается статистика:
- "Скопировано изображений: 120"
- "Пропущено изображений: 30"
- "Ошибок при копировании: 5"

### AC7: Поведение при повторном импорте
**Given** у товара уже установлены изображения
**When** запускается импорт изображений повторно
**Then** применяется логика из Story 3.1.2:
- `main_image` **НЕ меняется** (остается первое установленное)
- Новые изображения **добавляются** в `gallery_images`
- Дубликаты (одинаковые имена файлов) **пропускаются**
- Товары без `onec_id` в XML **пропускаются** (не создаются placeholder)

---

## Technical Implementation Details

### Архитектура
Расширение существующего функционала импорта из Story 9.6 путем добавления нового типа импорта "images".

### Изменения в компонентах

#### 1. Модель данных
**Файл**: `backend/apps/products/models.py` (строка ~524)

**Изменение**: Добавить новый выбор в `ImportType` enum:
```python
class ImportType(models.TextChoices):
    CATALOG = "catalog", "Каталог товаров"
    STOCKS = "stocks", "Остатки товаров"
    PRICES = "prices", "Цены товаров"
    CUSTOMERS = "customers", "Клиенты"
    IMAGES = "images", "Изображения товаров"  # НОВОЕ
```

**Миграция**: Создать миграцию Django для добавления нового выбора в поле.

#### 2. View слой
**Файл**: `backend/apps/integrations/views.py`

**Изменение 1**: Добавить новый тип в контекст (строка ~53-82):
```python
import_types = [
    # ... существующие типы ...
    {
        "value": "images",
        "label": "Изображения",
        "description": "Обновление изображений товаров",
        "files": "Файлы в директории: goods/import_files/ХХ/*.jpg",
        "requires_catalog": True,
    },
]
```

**Изменение 2**: Обновить валидацию зависимостей (строка ~143-160):
```python
def _validate_dependencies(selected_types: list[str]) -> tuple[bool, str]:
    """Проверка зависимостей между типами импорта."""
    if "stocks" in selected_types or "prices" in selected_types or "images" in selected_types:
        if not Product.objects.exists():
            return (
                False,
                "⚠️ Невозможно загрузить остатки/цены/изображения: "
                "каталог товаров пуст. Сначала импортируйте полный каталог.",
            )
    return True, ""
```

**Изменение 3**: Добавить маппинг session_type (строка ~229-234):
```python
session_type_mapping = {
    "catalog": ImportType.CATALOG,
    "stocks": ImportType.STOCKS,
    "prices": ImportType.PRICES,
    "customers": ImportType.CUSTOMERS,
    "images": ImportType.IMAGES,  # НОВОЕ
}
```

#### 3. Celery задача
**Файл**: `backend/apps/integrations/tasks.py` (строка ~126-132)

**Изменение**: Добавить обработку типа "images":
```python
@shared_task(bind=True)
def run_selective_import_task(self, import_types: list[str]) -> dict[str, Any]:
    """Асинхронная задача для выборочного импорта."""
    task_id = self.request.id
    logger = get_task_logger(__name__)

    # ... существующая обработка других типов ...

    for import_type in import_types:
        if import_type == "catalog":
            # ... существующая логика ...
        elif import_type == "images":
            logger.info(f"[Task {task_id}] Запуск импорта изображений товаров")
            call_command("import_catalog_from_1c", "--file-type", "goods")
            return {
                "type": "images",
                "message": "Изображения товаров импортированы",
            }
        # ... остальные типы ...
```

#### 4. Отображение статистики в Admin
**Файл**: `backend/apps/integrations/admin.py`

**Изменение**: Добавить отображение статистики изображений в detail view:
```python
@admin.register(ImportSession)
class ImportSessionAdmin(admin.ModelAdmin):
    # ... существующие настройки ...

    def get_readonly_fields(self, request, obj=None):
        if obj and obj.report_details:
            stats = obj.report_details.get("stats", {})
            if "images_copied" in stats:
                # Добавить поля для отображения статистики изображений
                return self.readonly_fields + (
                    "images_copied_count",
                    "images_skipped_count",
                    "images_errors_count",
                )
        return self.readonly_fields

    def images_copied_count(self, obj):
        """Количество скопированных изображений."""
        return obj.report_details.get("stats", {}).get("images_copied", 0)
    images_copied_count.short_description = "Скопировано изображений"

    def images_skipped_count(self, obj):
        """Количество пропущенных изображений."""
        return obj.report_details.get("stats", {}).get("images_skipped", 0)
    images_skipped_count.short_description = "Пропущено изображений"

    def images_errors_count(self, obj):
        """Количество ошибок при копировании изображений."""
        return obj.report_details.get("stats", {}).get("images_errors", 0)
    images_errors_count.short_description = "Ошибок при копировании"
```

### Не требуется изменять

- ❌ **Template HTML** - автоматически отобразит новый тип из context
- ❌ **Command `import_catalog_from_1c`** - уже поддерживает `--file-type=goods`
- ❌ **ProductDataProcessor** - уже имеет метод `import_product_images()`
- ❌ **Логика импорта изображений** - используется существующая из Story 3.1.2

### Последовательность выполнения

1. Создать миграцию для `ImportType.IMAGES`
2. Обновить `models.py` → добавить выбор IMAGES
3. Обновить `views.py` → добавить тип "images" в context и валидацию
4. Обновить `tasks.py` → добавить обработку типа "images"
5. Обновить `admin.py` → добавить отображение статистики
6. Запустить миграции: `python manage.py migrate`
7. Тестирование функционала

---

## Testing Strategy

### Unit Tests

**Файл**: `backend/apps/integrations/tests/test_views.py`

```python
class TestImportFromOneCViewImages(TestCase):
    """Тесты для импорта изображений (Story 9.7)."""

    def test_import_type_images_in_context(self):
        """AC1: Новый тип 'images' отображается в контексте."""
        response = self.client.get(reverse("admin:import_from_1c"))
        import_types = response.context["import_types"]

        images_type = next((t for t in import_types if t["value"] == "images"), None)
        self.assertIsNotNone(images_type)
        self.assertEqual(images_type["label"], "Изображения")
        self.assertIn("goods/import_files", images_type["files"])
        self.assertTrue(images_type["requires_catalog"])

    def test_validate_images_requires_catalog(self):
        """AC2: Валидация требует наличие товаров для импорта изображений."""
        # Без товаров в БД
        Product.objects.all().delete()

        response = self.client.post(
            reverse("admin:import_from_1c"),
            {"import_types": ["images"]},
        )

        self.assertContains(response, "каталог товаров пуст")
        self.assertEqual(ImportSession.objects.count(), 0)

    def test_create_session_for_images_type(self):
        """AC3: Создание ImportSession с типом IMAGES."""
        # Создать тестовый товар
        ProductFactory.create()

        response = self.client.post(
            reverse("admin:import_from_1c"),
            {"import_types": ["images"]},
        )

        session = ImportSession.objects.latest("created_at")
        self.assertEqual(session.import_type, ImportType.IMAGES)
        self.assertEqual(session.status, Status.PENDING)
        self.assertIsNotNone(session.celery_task_id)
```

**Файл**: `backend/apps/integrations/tests/test_tasks.py`

```python
class TestRunSelectiveImportTaskImages(TestCase):
    """Тесты для Celery задачи импорта изображений (Story 9.7)."""

    @patch("apps.integrations.tasks.call_command")
    def test_run_images_import_calls_correct_command(self, mock_call_command):
        """AC4: Задача вызывает команду с правильными параметрами."""
        result = run_selective_import_task.apply(args=[["images"]])

        mock_call_command.assert_called_once_with(
            "import_catalog_from_1c",
            "--file-type",
            "goods",
        )

        self.assertEqual(result.get()["type"], "images")
        self.assertIn("Изображения товаров импортированы", result.get()["message"])
```

### Integration Tests

**Файл**: `backend/apps/integrations/tests/test_integration_images.py`

```python
@pytest.mark.django_db
class TestImagesImportIntegration:
    """Интеграционные тесты для импорта изображений (Story 9.7)."""

    def test_full_images_import_workflow(self):
        """AC5, AC7: Полный workflow импорта изображений с реальными данными."""
        # Подготовка: создать товары с onec_id
        product1 = ProductFactory.create(onec_id="uuid-001")
        product2 = ProductFactory.create(onec_id="uuid-002")

        # Подготовка: скопировать тестовые изображения
        test_images_dir = Path("data/import_1c/goods/import_files/00/")
        test_images_dir.mkdir(parents=True, exist_ok=True)
        shutil.copy("tests/fixtures/test_image1.jpg", test_images_dir / "test1.jpg")
        shutil.copy("tests/fixtures/test_image2.jpg", test_images_dir / "test2.jpg")

        # Действие: запустить импорт через API
        client = APIClient()
        client.force_authenticate(user=AdminUserFactory.create())

        response = client.post(
            "/admin/integrations/import_1c/",
            {"import_types": ["images"]},
        )

        # Проверка: редирект на страницу сессий
        assert response.status_code == 302

        # Дождаться выполнения Celery задачи
        session = ImportSession.objects.latest("created_at")
        assert session.import_type == ImportType.IMAGES

        # Проверка: статистика сохранена
        assert "images_copied" in session.report_details["stats"]
        assert session.report_details["stats"]["images_copied"] > 0

        # Проверка: изображения скопированы
        product1.refresh_from_db()
        assert product1.main_image is not None
        assert len(product1.gallery_images) > 0

    def test_images_import_skips_missing_products(self):
        """AC7: Товары без onec_id в XML пропускаются."""
        # Создать товар без onec_id
        ProductFactory.create(onec_id=None)

        # Запустить импорт
        call_command("import_catalog_from_1c", "--file-type", "goods")

        # Проверка: товар без onec_id не получил изображения
        product = Product.objects.get(onec_id=None)
        assert product.main_image is None
        assert len(product.gallery_images) == 0

    def test_repeated_images_import_preserves_main_image(self):
        """AC7: Повторный импорт не меняет main_image."""
        # Создать товар с изображением
        product = ProductFactory.create(
            onec_id="uuid-001",
            main_image="products/00/original.jpg",
        )

        # Запустить импорт повторно
        call_command("import_catalog_from_1c", "--file-type", "goods")

        # Проверка: main_image не изменился
        product.refresh_from_db()
        assert product.main_image == "products/00/original.jpg"
```

### Manual Testing Checklist

**Перед началом тестирования:**
- [ ] Импортирован полный каталог товаров
- [ ] В директории `data/import_1c/goods/import_files/` есть тестовые изображения
- [ ] Redis сервис запущен
- [ ] Celery worker запущен

**Сценарий 1: Успешный импорт изображений**
1. [ ] Открыть `/admin/integrations/import_1c/`
2. [ ] Выбрать тип "Изображения"
3. [ ] Нажать "Запустить импорт"
4. [ ] Проверить редирект на страницу сессий
5. [ ] Дождаться завершения импорта (статус "Completed")
6. [ ] Проверить статистику: "Скопировано изображений: X"
7. [ ] Открыть товар в admin → проверить наличие изображений

**Сценарий 2: Валидация без каталога**
1. [ ] Удалить все товары из БД: `Product.objects.all().delete()`
2. [ ] Открыть `/admin/integrations/import_1c/`
3. [ ] Выбрать тип "Изображения"
4. [ ] Нажать "Запустить импорт"
5. [ ] Проверить сообщение: "каталог товаров пуст"

**Сценарий 3: Повторный импорт**
1. [ ] Запустить импорт изображений (первый раз)
2. [ ] Проверить `main_image` у товара
3. [ ] Запустить импорт повторно
4. [ ] Проверить что `main_image` **не изменился**
5. [ ] Проверить что новые изображения **добавлены** в `gallery_images`

**Сценарий 4: Redis Lock**
1. [ ] Запустить импорт изображений
2. [ ] Пока выполняется, попытаться запустить повторно
3. [ ] Проверить сообщение: "импорт уже выполняется"

---

## Dependencies

### Upstream Dependencies (Требуются для работы)
- ✅ **Story 3.1.2**: Импорт изображений товаров в media storage
  - Использует метод `ProductDataProcessor.import_product_images()`
  - Использует логику сохранения в `main_image` и `gallery_images`
  - Использует статистику `images_copied/skipped/errors`

- ✅ **Story 9.6**: Страница импорта данных из 1С
  - Расширяет существующий UI и функционал
  - Использует `ImportSession` модель
  - Использует Celery задачу `run_selective_import_task`
  - Использует Redis lock механизм

### Downstream Dependencies (Могут использовать эту историю)
- 🔮 **Future**: Автоматический импорт изображений по расписанию
- 🔮 **Future**: Webhook от 1С при обновлении фото товаров
- 🔮 **Future**: Превью изображений перед импортом

---

## Implementation Notes

### Критические моменты

1. **Поведение при повторном импорте**
   - `main_image` **НЕ меняется** если уже установлен (логика из Story 3.1.2)
   - Новые изображения **добавляются** в `gallery_images`
   - Дубликаты **пропускаются** (проверка по имени файла)

2. **Обработка отсутствующих товаров**
   - Если в `goods.xml` есть товар с `onec_id` которого нет в БД → **пропускается**
   - Placeholder товары **НЕ создаются** при импорте только изображений
   - В логах появляется предупреждение: `"Товар с onec_id={uuid} не найден, изображения пропущены"`

3. **Валидация наличия товаров**
   - Проверка `Product.objects.exists()` выполняется **ДО** запуска Celery задачи
   - Предотвращает создание ImportSession если каталог пуст
   - Улучшает UX - пользователь сразу видит проблему

4. **Redis Lock**
   - Используется существующий механизм из Story 9.6
   - Ключ: `import_1c:lock`
   - TTL: 3600 секунд (1 час)
   - Предотвращает параллельные импорты любых типов

5. **Статистика изображений**
   - Сохраняется в `ImportSession.report_details["stats"]`
   - Поля: `images_copied`, `images_skipped`, `images_errors`
   - Отображается в Django Admin detail view (AC6)

### Рекомендации по разработке

1. **Начать с миграции** - создать `ImportType.IMAGES` первым делом
2. **Использовать существующую логику** - не дублировать код из Story 3.1.2
3. **Тестировать с реальными данными** - использовать файлы из `data/import_1c/`
4. **Проверить все AC** - каждый критерий должен иметь автоматический тест
5. **Документировать изменения** - обновить README если нужно

### Потенциальные проблемы

1. **Большой объем изображений**
   - Решение: добавить progress bar в будущем (Low Priority)
   - Временное решение: показывать статус "In Progress" во время импорта

2. **Файлы изображений отсутствуют**
   - Решение: логика из Story 3.1.2 уже обрабатывает (images_errors)
   - В логах появляется предупреждение с путем к отсутствующему файлу

3. **Недостаточно места на диске**
   - Решение: добавить проверку свободного места перед копированием (Future)
   - Текущее поведение: ошибка при копировании → images_errors++

---

## Definition of Done

- [ ] Код написан и соответствует стандартам проекта
- [ ] Все AC реализованы и проверены
- [ ] Unit тесты написаны и проходят (покрытие >= 90%)
- [ ] Интеграционные тесты с реальными данными проходят
- [ ] Manual testing checklist полностью выполнен
- [ ] Миграция создана и протестирована
- [ ] Статистика изображений отображается в Admin
- [ ] Документация обновлена (если требуется)
- [ ] Code review пройден
- [ ] История задеплоена на staging
- [ ] QA тестирование пройдено
- [ ] Готово к деплою на production

---

## Related Stories

- [Story 3.1.2: Импорт изображений товаров в media storage](../epic-3.1/story-3.1.2-import-images-to-media.md)
- [Story 9.6: Страница импорта данных из 1С](./9.6-import-page-creation.md)
- [Epic 9: Интеграция с 1С](./README.md)

---

## Questions & Answers

**Q1: Почему не использовать отдельную команду для импорта изображений?**
**A1**: Логика импорта изображений уже интегрирована в `import_catalog_from_1c` (Story 3.1.2). Использование `--file-type=goods` позволяет импортировать только метаданные товаров и изображения, без создания новых товаров.

**Q2: Что происходит если товар удален из БД, но его изображения остались на диске?**
**A2**: При импорте изображений товар с `onec_id` не найден → изображения пропускаются. Старые файлы на диске НЕ удаляются (требуется отдельная задача очистки).

**Q3: Можно ли импортировать изображения для конкретного товара?**
**A3**: В текущей реализации - нет. Импортируются все изображения из `goods/import_files/`. Фильтрация по конкретному товару - future enhancement.

**Q4: Как обрабатываются изображения в разных форматах (JPG, PNG, WEBP)?**
**A4**: Логика из Story 3.1.2 копирует любые файлы из `import_files/` без проверки формата. Валидация формата происходит на уровне Django ImageField при сохранении модели.

---

**История создана**: 2025-01-11
**Создатель**: Sarah (Product Owner)
**Версия**: 1.0
