# Story 14.1: Обновление дизайн-системы и CSS токенов

## Status

In Progress

## Metadata

- **Version:** 1.1
- **Last Updated:** 2025-11-25
- **Story Points:** 3
- **Priority:** 🔴 Critical (блокирует Stories 14.2-14.5)
- **Epic:** 14 - Color Scheme Migration
- **Estimated Time:** 4-6 часов

## Dependencies

**Блокирует:**
- ⚠️ **Story 14.2: UI Kit Refactor** - требует обновлённые токены
- ⚠️ **Story 14.3: Page Components** - требует обновлённые токены
- ⚠️ **Story 14.4: Documentation Sync** - требует finalized tokens
- ⚠️ **Story 14.5: Testing** - требует все изменения

**Требует:**
- ✅ Sprint Change Proposal утверждён
- ✅ Epic 11, 12, 13 завершены
- ✅ Design Lead approval новой палитры

## Story

**As a** Frontend Developer,
**I want** обновить источники истины для цветовых токенов (design-system.json и globals.css),
**so that** все компоненты могут автоматически использовать новую сине-голубую палитру через CSS-переменные и Tailwind классы.

## Business Value

**Почему это важно:**
1. **Фундамент миграции:** Обновление токенов — первый критический шаг, блокирующий всю остальную работу
2. **Автоматическое применение:** Компоненты использующие токены получат новые цвета без доп. изменений
3. **Единый источник истины:** Централизованное управление цветовой палитрой упрощает поддержку

**Влияние на пользователей:**
- Минимальное (на этом этапе визуальные изменения частичны)
- Полное влияние проявится после Story 14.2

## Acceptance Criteria

### AC 1: design-system.json обновлён до v2.1.0

**GIVEN** файл `docs/frontend/design-system.json` открыт для редактирования,
**WHEN** я обновляю цветовую палитру согласно [color-scheme-migration.md](../../frontend/color-scheme-migration.md),
**THEN**:
1. ✅ `meta.version` = "2.1.0"
2. ✅ `meta.lastUpdated` = "2025-11-25"
3. ✅ `meta.changeLog` содержит запись:
   ```json
   {
     "version": "2.1.0",
     "date": "2025-11-25",
     "changes": "Миграция на сине-голубую цветовую палитру"
   }
   ```
4. ✅ `brand.colorPhilosophy` обновлён: "Яркие синие акценты для CTA, голубые вторичные тона, семантические цвета для статусов"

### AC 2: Обновлены все цветовые токены в foundations.colors

**Primary colors:**
```json
"primary": {
  "default": "#0060FF",    // было #1F1F1F
  "hover": "#0047CC",      // было #3A3A3A
  "active": "#0037A6",     // было #0D0D0D
  "subtle": "#E7F3FF"      // было #F2F2F2
}
```

**Secondary colors:**
```json
"secondary": {
  "default": "#00B7FF",    // было #3D3D3D
  "hover": "#0095D6",      // было #2E2E2E
  "active": "#0078B3",     // было #1C1C1C
  "subtle": "#E1F5FF"      // было #EAEAEA
}
```

**Accent colors:**
```json
"accent": {
  "success": "#00AA5B",    // было #4D4D4D
  "successBg": "#E0F5E8",  // NEW: фон для success badges
  "warning": "#F5A623",    // было #6A6A6A
  "warningBg": "#FFF1CC",  // NEW: фон для warning badges
  "danger": "#E53935",     // было #2B2B2B
  "dangerBg": "#FFE1E8",   // NEW: фон для danger/sale badges
  "promo": "#FF2E93",      // было #8A8A8A
  "promoBg": "#FFF0F5"     // NEW: фон для promo badges
}
```

**Neutral scale (9 оттенков, 7 изменены):**
```json
"neutral": {
  "100": "#FFFFFF",        // без изменений
  "200": "#F5F7FB",        // было #F5F5F5
  "300": "#E3E8F2",        // было #E0E0E0
  "400": "#B9C3D6",        // было #C7C7C7
  "500": "#8F9BB3",        // было #A3A3A3
  "600": "#6B7A99",        // было #7D7D7D
  "700": "#4B5C7A",        // было #5E5E5E
  "800": "#2D3A52",        // было #3F3F3F
  "900": "#1F2A44"         // было #1F1F1F
}
```

**Text colors:**
```json
"text": {
  "primary": "#1F2A44",    // было #1B1B1B
  "secondary": "#4B5C7A",  // было #4D4D4D
  "muted": "#7F8CA8",      // было #7A7A7A
  "inverse": "#FFFFFF"     // без изменений
}
```

### AC 3: Обновлены component colors в design-system.json

**Button variants:**
```json
"Button": {
  "variants": {
    "primary": {
      "bg": "var(--color-primary)",           // теперь #0060FF
      "bgHover": "var(--color-primary-hover)",
      "text": "#FFFFFF"
    },
    "secondary": {
      "border": "var(--color-primary)",
      "text": "var(--color-primary)"
    },
    "subtle": {
      "bg": "var(--color-primary-subtle)",    // теперь #E7F3FF
      "text": "var(--color-primary)"
    }
  }
}
```

**Badge variants:**
```json
"Badge": {
  "variants": {
    "new": {
      "bg": "#E7F3FF",         // было #E1F0FF
      "text": "#0060FF"        // было #0F5DA3
    },
    "hit": {
      "bg": "#E0F5E8",         // было #E3F6EC
      "text": "#00AA5B"        // было #1F7A4A
    },
    "promo": {
      "bg": "#FFF0F5",         // было #F4EBDC
      "text": "#FF2E93"        // было #8C4C00
    },
    "sale": {
      "bg": "#FFE1E8",         // было #F9E1E1
      "text": "#E53935"        // было #A63232
    }
  }
}
```

**Checkbox, Toggle, Tabs, Toast:** См. детали в [Sprint Change Proposal раздел 3.4](../../sprint-change-proposal-color-migration.md#34-design-system-json)

### AC 4: globals.css обновлён с новыми CSS-переменными

**GIVEN** файл `frontend/src/app/globals.css` открыт,
**WHEN** я обновляю CSS-переменные в `@theme inline` блоке,
**THEN**:

1. ✅ Все `--color-primary-*` переменные обновлены:
   ```css
   --color-primary: #0060FF;           /* было #1f1f1f */
   --color-primary-hover: #0047CC;     /* было #3a3a3a */
   --color-primary-active: #0037A6;    /* было #0d0d0d */
   --color-primary-subtle: #E7F3FF;    /* было #f2f2f2 */
   ```

2. ✅ Все `--color-secondary-*` переменные обновлены

3. ✅ Все `--color-accent-*` переменные обновлены:
   ```css
   --color-accent-success: #00AA5B;    /* было #4d4d4d */
   --color-accent-success-bg: #E0F5E8; /* NEW: фон success badges */
   --color-accent-warning: #F5A623;    /* было #6a6a6a */
   --color-accent-warning-bg: #FFF1CC; /* NEW: фон warning badges */
   --color-accent-danger: #E53935;     /* было #2b2b2b */
   --color-accent-danger-bg: #FFE1E8;  /* NEW: фон danger/sale badges */
   --color-accent-promo: #FF2E93;      /* было #8a8a8a */
   --color-accent-promo-bg: #FFF0F5;   /* NEW: фон promo badges */
   ```

4. ✅ Neutral scale обновлён (7 из 9 оттенков)

5. ✅ Text colors обновлены

6. ✅ Shadows обновлены:
   ```css
   --shadow-primary: 0 4px 12px rgba(0, 96, 255, 0.28);
   --shadow-secondary: 0 2px 8px rgba(0, 96, 255, 0.16);
   --shadow-pressed: inset 0 2px 4px rgba(0, 71, 204, 0.24);
   ```

7. ✅ Focus states обновлены:
   ```css
   --focus-ring: 0 0 0 4px rgba(0, 96, 255, 0.12);
   --focus-outline: 2px solid rgba(0, 96, 255, 0.6);
   ```

8. ✅ Добавлен комментарий о миграции:
   ```css
   /**
    * FREESPORT Design System - Tailwind CSS 4.0 Tokens
    * Based on design-system.json v2.1.0
    *
    * MIGRATION NOTE (2025-11-25):
    * Цветовая схема обновлена с графитово-серой на сине-голубую.
    * Все токены синхронизированы с design-system.json.
    *
    * Контрастность проверена: WCAG AA соблюдён ✅
    * Подробности: docs/frontend/color-scheme-migration.md
    */
   ```

### AC 5: Валидация и проверка

**Validation checks:**
1. ✅ JSON syntax валиден:
   ```bash
   # Должен вернуть "JSON is valid"
   node -e "JSON.parse(require('fs').readFileSync('docs/frontend/design-system.json')); console.log('JSON is valid')"
   ```

2. ✅ CSS компиляция без ошибок:
   ```bash
   cd frontend && npm run build
   # Не должно быть ошибок компиляции Tailwind
   ```

3. ✅ Dev server запускается:
   ```bash
   cd frontend && npm run dev
   # Сервер должен запуститься на http://localhost:3000
   ```

4. ✅ Визуальная проверка автоприменения:
   - Открыть http://localhost:3000
   - Проверить что некоторые элементы уже изменили цвет (те что используют токены)
   - Например: primary buttons, navigation active state

### AC 6: Git commit с детальным changelog

**GIVEN** все изменения завершены и проверены,
**WHEN** я создаю git commit,
**THEN**:

```bash
git add docs/frontend/design-system.json frontend/src/app/globals.css
git commit -m "feat(design-system): migrate to blue color scheme v2.1.0

Updates:
- design-system.json → v2.1.0 with new blue-cyan palette
- globals.css → all CSS variables updated
- Primary: #1F1F1F → #0060FF (blue)
- Secondary: #3D3D3D → #00B7FF (cyan)
- Accent colors: success, warning, danger, promo semantics
- Neutral scale: 7/9 shades updated with blue-gray undertone
- Shadows and focus states: updated to match new palette

Components auto-updated (via tokens):
- Buttons using bg-primary
- Navigation active states
- Focus indicators
- Shadows

WCAG AA contrast verified ✅
Related: docs/sprint-change-proposal-color-migration.md
Story: 14.1
Epic: 14
"
```

## Tasks / Subtasks

### Task 1: Backup текущих файлов (5 мин)

- [x] Создать backup design-system.json → design-system.json.v2.0.0.backup
- [x] Создать backup globals.css → globals.css.backup
- [ ] Зафиксировать commit point для rollback

### Task 2: Обновление design-system.json (1.5 часа)

- [x] Обновить meta section (version, lastUpdated, changeLog)
- [x] Обновить brand.colorPhilosophy
- [x] Обновить foundations.colors.primary (4 значения)
- [x] Обновить foundations.colors.secondary (4 значения)
- [x] Обновить foundations.colors.accent (4 значения)
- [x] Обновить foundations.colors.neutral (7 из 9 значений)
- [x] Обновить foundations.colors.text (3 из 4 значений)
- [x] Обновить components.Button (3 варианта)
- [x] Обновить components.Badge (5 вариантов)
- [x] Обновить components.Checkbox (3 состояния)
- [x] Обновить components.Toggle (2 состояния)
- [x] Обновить components.Tabs (2 состояния)
- [x] Обновить components.Toast (4 варианта)
- [x] Обновить shadows (3 значения)
- [x] Обновить states.focus (2 значения)

### Task 3: Обновление globals.css (1 час)

- [x] Обновить комментарий с информацией о миграции
- [x] Обновить --color-primary-* (4 переменные)
- [x] Обновить --color-secondary-* (4 переменные)
- [x] Обновить --color-accent-* (8 переменных: 4 text + 4 bg)
- [x] Обновить --color-neutral-* (7 переменных)
- [x] Обновить --color-text-* (3 переменные)
- [x] Обновить --color-типography-* (3 переменные)
- [x] Обновить --shadow-* (3 переменные)
- [x] Обновить --focus-* (2 переменные)

### Task 4: Валидация (30 мин)

- [x] JSON validation (node command)
- [x] CSS compilation test (npm run build)
- [ ] Dev server start (npm run dev)
- [ ] Visual check в браузере (localhost:3000)
- [ ] Screenshot до/после для основных элементов

### Task 5: Git commit и push (15 мин)

- [ ] Создать детальный commit message
- [ ] git add файлы
- [ ] git commit
- [ ] git push origin feature/epic-14-color-scheme-migration

### Task 6: Handoff к Story 14.2 (15 мин)

- [ ] Уведомить команду о завершении Story 14.1
- [ ] Подтвердить что dev server работает с новыми токенами
- [ ] Создать list компонентов для Story 14.2 refactor

## Dev Notes

### Testing

**Test Requirements для Story 14.1:**

Story 14.1 обновляет **только конфигурационные файлы** (JSON, CSS), поэтому:

✅ **Тесты НЕ требуются** для:

- design-system.json (статическая конфигурация)
- globals.css (CSS переменные)

✅ **Валидация требуется** через:

- JSON syntax validation (node команда в AC5)
- CSS compilation test (npm run build)
- Dev server smoke test (npm run dev)
- Visual inspection (браузер)

**Testing Standards:**

- Следовать Frontend Testing Standards из [docs/architecture/coding-standards.md](../../architecture/coding-standards.md)
- Story 14.2 потребует unit тесты для обновлённых компонентов
- Visual regression tests будут обновлены в Story 14.5

**Test Frameworks:**

- Vitest 2.1.5 (для будущих stories)
- React Testing Library 16.3.0 (для компонентов в Story 14.2)
- Manual testing (для visual verification в Story 14.1)

**Test Data:**

- Не требуется для Story 14.1 (только обновление конфигурации)

---

## Technical Notes

### JSON Структура для reference

Полная структура обновляемых разделов:

```json
{
  "meta": {
    "version": "2.1.0",
    "lastUpdated": "2025-11-25",
    "changeLog": [
      {
        "version": "2.1.0",
        "date": "2025-11-25",
        "changes": "Миграция на сине-голубую цветовую палитру"
      }
    ]
  },
  "brand": {
    "tone": "Технологичный, дружелюбный e-commerce в стиле OZON",
    "colorPhilosophy": "Яркие синие акценты для CTA, голубые вторичные тона, семантические цвета для статусов"
  }
}
```

### CSS Variables Checklist

Используйте этот чеклист для проверки всех обновлений:

```markdown
Primary Colors:
- [ ] --color-primary: #0060FF
- [ ] --color-primary-hover: #0047CC
- [ ] --color-primary-active: #0037A6
- [ ] --color-primary-subtle: #E7F3FF

Secondary Colors:
- [ ] --color-secondary: #00B7FF
- [ ] --color-secondary-hover: #0095D6
- [ ] --color-secondary-active: #0078B3
- [ ] --color-secondary-subtle: #E1F5FF

Accent Colors:
- [ ] --color-accent-success: #00AA5B
- [ ] --color-accent-success-bg: #E0F5E8
- [ ] --color-accent-warning: #F5A623
- [ ] --color-accent-warning-bg: #FFF1CC
- [ ] --color-accent-danger: #E53935
- [ ] --color-accent-danger-bg: #FFE1E8
- [ ] --color-accent-promo: #FF2E93
- [ ] --color-accent-promo-bg: #FFF0F5

Neutral Scale (изменены):
- [ ] --color-neutral-200: #F5F7FB
- [ ] --color-neutral-300: #E3E8F2
- [ ] --color-neutral-400: #B9C3D6
- [ ] --color-neutral-500: #8F9BB3
- [ ] --color-neutral-600: #6B7A99
- [ ] --color-neutral-700: #4B5C7A
- [ ] --color-neutral-800: #2D3A52
- [ ] --color-neutral-900: #1F2A44

Text Colors:
- [ ] --color-text-primary: #1F2A44
- [ ] --color-text-secondary: #4B5C7A
- [ ] --color-text-muted: #7F8CA8

Shadows:
- [ ] --shadow-primary: 0 4px 12px rgba(0, 96, 255, 0.28)
- [ ] --shadow-secondary: 0 2px 8px rgba(0, 96, 255, 0.16)
- [ ] --shadow-pressed: inset 0 2px 4px rgba(0, 71, 204, 0.24)

Focus States:
- [ ] --focus-ring: 0 0 0 4px rgba(0, 96, 255, 0.12)
- [ ] --focus-outline: 2px solid rgba(0, 96, 255, 0.6)
```

### Validation Commands

```bash
# JSON validation
node -e "JSON.parse(require('fs').readFileSync('docs/frontend/design-system.json')); console.log('✅ JSON is valid')"

# CSS compilation
cd frontend && npm run build 2>&1 | grep -i "error"
# Не должно быть output

# Dev server (background)
cd frontend && npm run dev &
sleep 5
curl http://localhost:3000 > /dev/null && echo "✅ Dev server running" || echo "❌ Dev server failed"
```

### Visual Check Checklist

Открыть http://localhost:3000 и проверить:

- [ ] **Header navigation active link:**
  - URL: http://localhost:3000
  - Элемент: `<nav>` → `<a class="active">`
  - Ожидается: `color: #0060FF` (было `#1F1F1F`)
  - Проверка: DevTools → Computed → color

- [ ] **Primary button на главной:**
  - Элемент: Hero CTA button
  - Ожидается: `background-color: #0060FF` (было `#1F1F1F`)
  - Hover: `background-color: #0047CC`
  - Проверка: DevTools → Computed → background-color

- [ ] **Focus states (любой input):**
  - Элемент: любое input поле
  - Ожидается: `outline: 2px solid rgba(0, 96, 255, 0.6)`
  - Проверка: Кликнуть на input → проверить outline в DevTools

- [ ] **Shadows (на карточках товаров):**
  - Элемент: product card
  - Ожидается: синий оттенок в тени (едва заметно)
  - Проверка: визуальный осмотр

**Ожидается частичное изменение** — компоненты с хардкодом цветов останутся прежними до Story 14.2.

### Tailwind Config Considerations

**Проверка необходимости обновления:**

Story 14.1 обновляет ТОЛЬКО CSS-переменные в `@theme inline` блоке `frontend/src/app/globals.css`.

✅ **Tailwind config НЕ требует изменений** если:

- Проект использует Tailwind CSS 4.0 с `@theme inline`
- Токены определены через CSS-переменные (`var(--color-primary)`)
- Config использует эти переменные без хардкода

⚠️ **Если config содержит хардкод цветов:**

```javascript
// frontend/tailwind.config.ts
theme: {
  extend: {
    colors: {
      primary: '#1F1F1F', // ❌ ХАРДКОД - требует обновления на #0060FF
    }
  }
}
```

**Действие:** Удалить хардкод из config, использовать CSS-переменные.

**Проверка config:**

```bash
# Открыть файл для проверки
cat frontend/tailwind.config.ts | grep -i "primary\|secondary\|#[0-9A-F]"

# Если найдены HEX коды - требуется обновление config
```

## Edge Cases

### Edge Case 1: JSON файл поврежден после редактирования
**Ситуация:** Ошибка синтаксиса в JSON
**Решение:**
1. Откатиться к backup: `cp design-system.json.v2.0.0.backup design-system.json`
2. Использовать JSON linter (VS Code extension)
3. Применить изменения заново

### Edge Case 2: CSS компиляция Tailwind ломается
**Ситуация:** Ошибка после обновления CSS-переменных
**Решение:**
1. Проверить синтаксис: все переменные должны начинаться с `--`
2. Проверить значения: HEX коды должны быть валидными
3. Откатиться к backup globals.css если нужно

### Edge Case 3: Dev server не запускается
**Ситуация:** Ошибка при npm run dev
**Решение:**
1. Проверить Tailwind config: `frontend/tailwind.config.ts`
2. Очистить cache: `rm -rf .next && npm run dev`
3. Проверить node_modules: `npm install`

### Rollback Strategy

Если после обновления обнаружены критические проблемы:

**План А: Rollback через backup (5 минут)**

```bash
# Откат JSON
cp design-system.json.v2.0.0.backup docs/frontend/design-system.json

# Откат CSS
cp globals.css.backup frontend/src/app/globals.css

# Проверка
cd frontend && npm run dev
```

**План Б: Git revert (10 минут)**

```bash
# Найти commit миграции
git log --oneline | grep "feat(design-system)"

# Откатить изменения
git revert <commit-hash>

# Push изменений
git push origin feature/epic-14-color-scheme-migration
```

### Before/After Visual Comparison

**Primary Button:**

- Before: `background: #1F1F1F` (dark gray)
- After: `background: #0060FF` (blue)
- Screenshot location: `docs/stories/epic-14/screenshots/14.1-button-before-after.png` (опционально)

**Badge "new":**

- Before: `bg: #E1F0FF, text: #0F5DA3`
- After: `bg: #E7F3FF, text: #0060FF`

**Navigation Active Link:**

- Before: `color: #1F1F1F` (graphite)
- After: `color: #0060FF` (blue)

### Performance Validation (Optional)

**Baseline check:**

```bash
# Измерение bundle size
npm run build
ls -lh .next/static/css/*.css

# PageSpeed test
# Открыть https://pagespeed.web.dev/
# Проверить localhost:3000 (после деплоя на staging)
```

**Ожидается:**

- CSS bundle size: без изменений (±1-2%)
- PageSpeed score: >70 (без ухудшения)

---

## Definition of Done

Story 14.1 считается завершённой когда:

**Technical:**
- [ ] design-system.json обновлён до v2.1.0 ✅
- [ ] globals.css полностью обновлён ✅
- [ ] JSON validation проходит ✅
- [ ] CSS компиляция без ошибок ✅
- [ ] Dev server запускается ✅
- [ ] Git commit создан с детальным changelog ✅

**Verification:**
- [ ] Visual check: некоторые элементы изменили цвет (те что используют токены)
- [ ] Backup файлы созданы на случай rollback
- [ ] Команда уведомлена о завершении

**Handoff:**
- [ ] Story 14.2 разблокирована
- [ ] List компонентов для рефакторинга передан разработчику Story 14.2

**Time Check:**
- [ ] Фактическое время <= 6 часов (оценка)

---

**Story Owner:** Frontend Lead
**Reviewer:** Design System Owner
**Estimated Start:** День 1 Epic 14
**Estimated Completion:** День 1 (конец дня)
**Блокирует:** Stories 14.2, 14.3, 14.4, 14.5

---

## QA Results

**Gate Decision:** CONCERNS — необходимо доработать критерии валидации и уточнить артефакты перед стартом разработки.

### Findings
1. **Валидация описана только для локальной машины, Docker-процесс не задокументирован.** В AC5 и Dev Notes требуется запуск `node`, `npm run build` и `npm run dev` напрямую на хосте (@docs/stories/epic-14/14.1.design-system-tokens-update.story.md#237-371). Это нарушает принятую практику запуска фронтенда через Docker и оставляет команду без четких инструкций, как выполнить проверки в контейнере.
2. **Непоследовательность по accent-токенам.** AC2 перечисляет 7 новых значений для блока `accent`, включая фоновые цвета (@docs/stories/epic-14/14.1.design-system-tokens-update.story.md#87-99), но Task 2 фиксирует лишь «4 значения» (@docs/stories/epic-14/14.1.design-system-tokens-update.story.md#303-317). Это может привести к частичному обновлению токенов и несинхронности между design-system.json и globals.css.
3. **Компонентные токены (Checkbox/Toggle/Tabs/Toast) завязаны на внешний документ без явных критериев.** AC3 отсылает к sprint-change-proposal, не фиксируя конкретные значения в самой истории (@docs/stories/epic-14/14.1.design-system-tokens-update.story.md#126-175, @docs/stories/epic-14/14.1.design-system-tokens-update.story.md#312-315). При ревью будет сложно подтвердить полноту миграции без копирования условий из внешнего файла.

### Recommendations
- Добавить инструкции (или ссылку на существующий workflow) по запуску `npm run build/dev` и JSON-валидации внутри Docker-контейнера фронтенда, чтобы QA и разработчики выполняли проверки единообразно.
- Синхронизировать формулировки AC и Task 2 для accent-токенов: перечислить полный перечень значений и явно отметить новые bg-токены, чтобы исключить частичное обновление.
- Включить в AC3 минимально необходимый набор ожидаемых значений для Checkbox/Toggle/Tabs/Toast или приложить таблицу значений в приложении к истории, чтобы QA мог сверить изменения без перехода в другой документ.

## References

- [Sprint Change Proposal - Section 3.4](../../sprint-change-proposal-color-migration.md#34-design-system-json)
- [Sprint Change Proposal - Section 3.5](../../sprint-change-proposal-color-migration.md#35-tailwind-css-tokens)
- [Color Scheme Migration Guide](../../frontend/color-scheme-migration.md)
- [Design System JSON (current)](../../frontend/design-system.json)
- [Globals CSS (current)](../../frontend/src/app/globals.css)

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-11-25 | 1.0 | Initial story creation for design system tokens update | Sarah (PO) |
| 2025-11-25 | 1.1 | Added Testing section, Tailwind Config considerations, Rollback Strategy, Before/After comparison, Performance validation | Sarah (PO) |
| 2025-11-25 | 1.2 | Добавлены accent-*-bg токены для фонов badges (success-bg, warning-bg, danger-bg, promo-bg) | Sarah (PO) |
