# Story 12.5: Фильтры и поиск

## Status

Draft

## Prerequisites

- **Story 12.1 (Обновление дизайн-токенов)** - CSS переменные и Tailwind конфигурация должны быть обновлены
- **Story 12.2 (Миграция базовых UI компонентов)** - Button, Input должны быть обновлены под новую палитру

## Story

**As a** Frontend разработчик,
**I want** создать компоненты SidebarFilters, SearchField, Checkbox, Toggle, Select и сортировку товаров согласно Design System v2.0,
**so that** пользователи FREESPORT могли эффективно фильтровать и искать товары в каталоге с единообразным визуальным стилем и качественным UX.

## Acceptance Criteria

1. Компонент SidebarFilters создан согласно Design System v2.0:
   - Ширина: 280px
   - Секции с заголовками (title-s, font-weight: 600)
   - Collapsible группы фильтров с иконкой ChevronDown
   - Секции: Категории, Бренды, Цена (range slider), Размер, Цвет
   - Spacing между секциями: 24px
   - Кнопки "Применить" и "Сбросить" внизу

2. Компонент Checkbox создан и стилизован:
   - Box size: 20px
   - Border radius: 6px
   - Border default: 1.5px solid #B9C3D6
   - Border/Background checked: #1F1F1F
   - Check icon: lucide-check, size 18px, stroke-width 3
   - Label: typography body-m, spacing 12px от checkbox
   - States: default, hover, checked, disabled
   - Focus ring: 2px #0060FF с offset

3. Компонент Toggle создан и стилизован:
   - Track: width 44px, height 24px, radius 999
   - Track colors: off #E3E8F2, on #0060FF
   - Thumb: size 20px, color #FFFFFF
   - Thumb shadow: 0 2px 8px rgba(0, 0, 0, 0.16)
   - Smooth transition: 180ms cubic-bezier(0.4, 0, 0.2, 1)
   - States: off, on, disabled
   - Focus ring для accessibility

4. Компонент Select/Dropdown создан:
   - Height: 40px
   - Padding: 16px
   - Border radius: 6px
   - Border: 1px solid #D0D7E6
   - ChevronDown icon справа
   - Focus: border-primary 1.5px (#0060FF)
   - Dropdown menu с опциями
   - States: default, focus, open, disabled
   - Keyboard navigation (Arrow keys, Enter, Escape)

5. Компонент SearchField создан:
   - Height: 40px
   - Border radius: 6px
   - Border: 1px solid #E3E8F2
   - Search icon слева (lucide-search)
   - Clear button (X) при наличии текста
   - Placeholder: "Поиск"
   - Focus: border-primary 1.5px
   - Автодополнение dropdown с suggestions и товарами
   - Debounce 300ms для поиска

6. Компонент PriceRangeSlider создан:
   - Dual-thumb slider для диапазона цен
   - Input fields для точного ввода (От/До)
   - Формат цены: с разделителями тысяч и "₽"
   - Track: height 4px, bg #E3E8F2
   - Active track: bg #0060FF
   - Thumb: size 20px, bg #FFFFFF, border 2px #0060FF
   - Min/Max значения из API

7. Компонент SortSelect (Сортировка) создан:
   - Опции B2B/B2C сортировки:
     - Цена по возрастанию/убыванию
     - По наличию
     - По бренду (А-Я)
     - По названию (А-Я)
     - Новинки
   - Текущая сортировка отображается в dropdown trigger
   - Иконка ChevronDown

8. Компонент Chip (для активных фильтров) создан:
   - Default: bg #FFFFFF, border 1px #D0D7E6, text #4B5C7A
   - Selected: bg #0060FF, text #FFFFFF
   - Remove button (X) для удаления фильтра
   - Radius: 16px
   - Gap между chips: 8px

9. Все компоненты проходят accessibility проверку:
   - Контраст текста >= 4.5:1 (WCAG 2.1 AA)
   - Focus состояния видимы
   - aria-* атрибуты корректны
   - Keyboard navigation работает
   - Screen reader support

10. Тесты созданы и проходят успешно:
    - Unit тесты для каждого компонента
    - Тесты интерактивности (клики, keyboard)
    - Тесты accessibility атрибутов
    - Покрытие >= 80%

11. Документация компонентов обновлена:
    - JSDoc комментарии
    - Props interfaces с описаниями
    - Примеры использования

## Tasks / Subtasks

- [ ] Task 1: Создание компонента Checkbox (AC: 2, 9)
  - [ ] 1.1 Создать директорию `frontend/src/components/ui/Checkbox/`
  - [ ] 1.2 Создать файл `Checkbox.tsx` с интерфейсом:
    ```typescript
    interface CheckboxProps {
      checked: boolean
      onChange: (checked: boolean) => void
      label?: string
      disabled?: boolean
      id?: string
      name?: string
      className?: string
    }
    ```
  - [ ] 1.3 Стилизовать checkbox box:
    - w-5 h-5 (20px)
    - rounded-md (6px)
    - border-[1.5px] border-[#B9C3D6]
    - checked: bg-[#1F1F1F] border-[#1F1F1F]
    - transition-all duration-[180ms]
  - [ ] 1.4 Добавить Check icon (lucide-react):
    - size: 18px
    - strokeWidth: 3
    - color: white
    - Scale animation on check
  - [ ] 1.5 Стилизовать label:
    - text-body-m
    - ml-3 (12px gap)
  - [ ] 1.6 Добавить states:
    - hover: border-primary-hover
    - focus: ring-2 ring-[#0060FF] ring-offset-2
    - disabled: opacity-50 cursor-not-allowed
  - [ ] 1.7 Добавить accessibility атрибуты:
    - role="checkbox"
    - aria-checked={checked}
    - aria-disabled={disabled}
    - tabIndex для keyboard nav
  - [ ] 1.8 Создать index.ts для экспорта

- [ ] Task 2: Создание компонента Toggle (AC: 3, 9)
  - [ ] 2.1 Создать директорию `frontend/src/components/ui/Toggle/`
  - [ ] 2.2 Создать файл `Toggle.tsx` с интерфейсом:
    ```typescript
    interface ToggleProps {
      checked: boolean
      onChange: (checked: boolean) => void
      label?: string
      disabled?: boolean
      id?: string
      className?: string
    }
    ```
  - [ ] 2.3 Стилизовать track:
    - w-11 h-6 (44x24px)
    - rounded-full
    - off: bg-[#E3E8F2]
    - on: bg-[#0060FF]
    - transition-colors duration-[180ms]
  - [ ] 2.4 Стилизовать thumb:
    - w-5 h-5 (20px)
    - rounded-full
    - bg-white
    - shadow-[0_2px_8px_rgba(0,0,0,0.16)]
    - translateX: off 2px, on 22px
    - transition-transform duration-[180ms]
  - [ ] 2.5 Добавить focus ring:
    - focus:ring-2 ring-[#0060FF] ring-offset-2
  - [ ] 2.6 Добавить accessibility:
    - role="switch"
    - aria-checked={checked}
  - [ ] 2.7 Создать index.ts

- [ ] Task 3: Создание компонента Select (AC: 4, 9)
  - [ ] 3.1 Создать директорию `frontend/src/components/ui/Select/`
  - [ ] 3.2 Создать файл `Select.tsx` с интерфейсом:
    ```typescript
    interface SelectProps {
      options: { value: string; label: string }[]
      value?: string
      placeholder?: string
      label?: string
      onChange: (value: string) => void
      disabled?: boolean
      className?: string
    }
    ```
  - [ ] 3.3 Стилизовать trigger:
    - h-10 (40px)
    - px-4 (16px padding)
    - rounded-md (6px)
    - border border-[#D0D7E6]
    - bg-white
    - flex justify-between items-center
  - [ ] 3.4 Добавить ChevronDown icon:
    - lucide-react ChevronDown
    - Rotate 180deg when open
  - [ ] 3.5 Создать dropdown menu:
    - bg-white
    - rounded-md
    - shadow-lg
    - border border-[#E3E8F2]
    - max-height: 240px, overflow-y-auto
  - [ ] 3.6 Стилизовать options:
    - px-4 py-2
    - hover: bg-[#F5F7FA]
    - selected: bg-[#E1F0FF] text-[#0060FF]
  - [ ] 3.7 Добавить keyboard navigation:
    - Arrow Up/Down для навигации
    - Enter для выбора
    - Escape для закрытия
  - [ ] 3.8 Добавить focus state:
    - focus: border-[#0060FF] border-[1.5px]
  - [ ] 3.9 Accessibility:
    - role="listbox" для dropdown
    - role="option" для опций
    - aria-expanded, aria-selected
  - [ ] 3.10 Создать index.ts

- [ ] Task 4: Создание компонента SearchField (AC: 5, 9)
  - [ ] 4.1 Создать директорию `frontend/src/components/ui/SearchField/`
  - [ ] 4.2 Создать файл `SearchField.tsx` с интерфейсом:
    ```typescript
    interface SearchFieldProps {
      value: string
      onChange: (value: string) => void
      onSearch?: (query: string) => void
      placeholder?: string
      suggestions?: string[]
      products?: Product[]
      debounceMs?: number
      className?: string
    }
    ```
  - [ ] 4.3 Стилизовать input container:
    - h-10 (40px)
    - rounded-md (6px)
    - border border-[#E3E8F2]
    - focus-within: border-[#0060FF] border-[1.5px]
    - pl-10 для иконки поиска
    - pr-10 для clear button
  - [ ] 4.4 Добавить Search icon:
    - lucide-react Search
    - absolute left-3
    - text-[#7A7A7A]
  - [ ] 4.5 Добавить Clear button:
    - lucide-react X
    - absolute right-3
    - Показывать при value.length > 0
    - hover: bg-[#F5F7FA] rounded-full
  - [ ] 4.6 Реализовать debounce:
    - По умолчанию 300ms
    - Использовать useDebounce hook
  - [ ] 4.7 Создать Autocomplete dropdown:
    - bg-white, shadow-lg, rounded-md
    - Секция suggestions (text queries)
    - Секция products (карточки товаров)
    - Keyboard navigation
  - [ ] 4.8 Accessibility:
    - role="combobox"
    - aria-expanded
    - aria-autocomplete="list"
  - [ ] 4.9 Создать index.ts

- [ ] Task 4.5: Создание useDebounce hook (AC: 5)
  - [ ] 4.5.1 Создать `frontend/src/hooks/useDebounce.ts`:
    ```typescript
    import { useState, useEffect } from 'react'

    export function useDebounce<T>(value: T, delay: number): T {
      const [debouncedValue, setDebouncedValue] = useState<T>(value)

      useEffect(() => {
        const timer = setTimeout(() => setDebouncedValue(value), delay)
        return () => clearTimeout(timer)
      }, [value, delay])

      return debouncedValue
    }
    ```
  - [ ] 4.5.2 Экспортировать из `frontend/src/hooks/index.ts`

- [ ] Task 5: Создание компонента PriceRangeSlider (AC: 6, 9)
  - [ ] 5.1 Создать директорию `frontend/src/components/ui/PriceRangeSlider/`
  - [ ] 5.2 Создать файл `PriceRangeSlider.tsx` с интерфейсом:
    ```typescript
    interface PriceRangeSliderProps {
      min: number
      max: number
      value: [number, number]
      onChange: (value: [number, number]) => void
      step?: number
      formatPrice?: (price: number) => string
      className?: string
    }
    ```
  - [ ] 5.3 Создать dual-thumb slider:
    - Track: h-1 bg-[#E3E8F2] rounded-full
    - Active track: bg-[#0060FF]
    - Thumbs: w-5 h-5 bg-white border-2 border-[#0060FF] rounded-full
  - [ ] 5.4 Создать input fields:
    - Два input для От и До
    - Формат: number с разделителями
    - Суффикс "₽"
  - [ ] 5.5 Синхронизация slider и inputs
  - [ ] 5.6 Валидация: min <= от <= до <= max
  - [ ] 5.7 Accessibility для слайдера
  - [ ] 5.8 Создать index.ts

- [ ] Task 6: Создание компонента SortSelect (AC: 7)
  - [ ] 6.1 Создать директорию `frontend/src/components/ui/SortSelect/`
  - [ ] 6.2 Создать файл `SortSelect.tsx`:
    ```typescript
    interface SortSelectProps {
      options: SortOption[]
      value: string
      onChange: (value: string) => void
      mode?: 'b2c' | 'b2b'
      className?: string
    }

    interface SortOption {
      value: string
      label: string
      direction: 'asc' | 'desc'
    }
    ```
  - [ ] 6.3 Определить опции сортировки:
    ```typescript
    const SORT_OPTIONS = [
      { value: 'price_asc', label: 'Цена: по возрастанию', direction: 'asc' },
      { value: 'price_desc', label: 'Цена: по убыванию', direction: 'desc' },
      { value: 'stock_desc', label: 'По наличию', direction: 'desc' },
      { value: 'brand_asc', label: 'По бренду (А-Я)', direction: 'asc' },
      { value: 'name_asc', label: 'По названию (А-Я)', direction: 'asc' },
      { value: 'created_desc', label: 'Новинки', direction: 'desc' }
    ]
    ```
  - [ ] 6.4 Использовать Select компонент как базу
  - [ ] 6.5 Создать index.ts

- [ ] Task 7: Создание компонента Chip (AC: 8)
  - [ ] 7.1 Создать директорию `frontend/src/components/ui/Chip/`
  - [ ] 7.2 Создать файл `Chip.tsx`:
    ```typescript
    interface ChipProps {
      children: string
      selected?: boolean
      onRemove?: () => void
      onClick?: () => void
      icon?: ReactNode
      className?: string
    }
    ```
  - [ ] 7.3 Стилизовать chip:
    - Default: bg-white border border-[#D0D7E6] text-[#4B5C7A]
    - Selected: bg-[#0060FF] text-white
    - rounded-2xl (16px)
    - px-3 py-1
    - gap-2
  - [ ] 7.4 Добавить remove button:
    - lucide-react X, size 14px
    - Показывать если onRemove определен
  - [ ] 7.5 Создать index.ts

- [ ] Task 8: Создание компонента FilterGroup (AC: 1)
  - [ ] 8.1 Создать директорию `frontend/src/components/ui/FilterGroup/`
  - [ ] 8.2 Создать файл `FilterGroup.tsx`:
    ```typescript
    interface FilterGroupProps {
      title: string
      collapsible?: boolean
      defaultExpanded?: boolean
      children: ReactNode
      className?: string
    }
    ```
  - [ ] 8.3 Стилизовать header:
    - title-s, font-weight: 600
    - flex justify-between items-center
    - cursor-pointer если collapsible
  - [ ] 8.4 Добавить ChevronDown icon:
    - Rotate 180deg when expanded
    - transition-transform
  - [ ] 8.5 Реализовать collapse/expand:
    - Animate height
    - Smooth transition
  - [ ] 8.6 Создать index.ts

- [ ] Task 9: Создание компонента SidebarFilters (AC: 1, 9) [Depends: Task 8]
  - [ ] 9.1 Создать директорию `frontend/src/components/business/SidebarFilters/`
  - [ ] 9.2 Создать файл `SidebarFilters.tsx`:
    ```typescript
    interface SidebarFiltersProps {
      categories: Category[]
      brands: Brand[]
      priceRange: { min: number; max: number }
      sizes: string[]
      colors: { name: string; hex: string }[]
      selectedFilters: FilterValues
      onFilterChange: (filters: FilterValues) => void
      onApply: () => void
      onReset: () => void
      className?: string
    }

    interface FilterValues {
      categories: string[]
      brands: string[]
      priceRange: [number, number]
      sizes: string[]
      colors: string[]
      inStock?: boolean
    }
    ```
  - [ ] 9.3 Собрать sidebar структуру:
    - w-[280px]
    - bg-white
    - Spacing между секциями: 24px
  - [ ] 9.4 Реализовать секцию Категории:
    - FilterGroup с Checkbox для каждой категории
    - Вложенные подкатегории
  - [ ] 9.5 Реализовать секцию Бренды:
    - FilterGroup с Checkbox
    - Поиск по брендам (если много)
  - [ ] 9.6 Реализовать секцию Цена:
    - FilterGroup с PriceRangeSlider
  - [ ] 9.7 Реализовать секцию Размер:
    - FilterGroup с Chip или Checkbox
  - [ ] 9.8 Реализовать секцию Цвет:
    - FilterGroup с цветовыми swatch
  - [ ] 9.9 Добавить Toggle "Только в наличии"
  - [ ] 9.10 Добавить кнопки Apply/Reset:
    - Button variant="primary" для Применить
    - Button variant="tertiary" для Сбросить
  - [ ] 9.11 Создать index.ts

- [ ] Task 10: Создание тестов (AC: 10)
  - [ ] 10.1 Создать тесты для Checkbox:
    - `frontend/src/components/ui/Checkbox/__tests__/Checkbox.test.tsx`
    - Тесты: render, click toggle, keyboard, disabled, accessibility
  - [ ] 10.2 Создать тесты для Toggle:
    - `frontend/src/components/ui/Toggle/__tests__/Toggle.test.tsx`
    - Тесты: render, click toggle, keyboard, disabled
  - [ ] 10.3 Создать тесты для Select:
    - `frontend/src/components/ui/Select/__tests__/Select.test.tsx`
    - Тесты: render, open/close, select option, keyboard nav
  - [ ] 10.4 Создать тесты для SearchField:
    - `frontend/src/components/ui/SearchField/__tests__/SearchField.test.tsx`
    - Тесты: render, input, clear, debounce, autocomplete
  - [ ] 10.5 Создать тесты для PriceRangeSlider:
    - `frontend/src/components/ui/PriceRangeSlider/__tests__/PriceRangeSlider.test.tsx`
    - Тесты: render, slider move, input change
  - [ ] 10.6 Создать тесты для SidebarFilters:
    - `frontend/src/components/business/SidebarFilters/__tests__/SidebarFilters.test.tsx`
    - Тесты: render, filter changes, apply/reset
  - [ ] 10.7 Запустить тесты и проверить покрытие >= 80%

- [ ] Task 11: Документация и интеграция (AC: 11)
  - [ ] 11.1 Добавить JSDoc комментарии ко всем компонентам
  - [ ] 11.2 Добавить примеры использования
  - [ ] 11.3 Экспортировать компоненты из `frontend/src/components/ui/index.ts`
  - [ ] 11.4 Экспортировать SidebarFilters из `frontend/src/components/business/index.ts`
  - [ ] 11.5 Визуальная проверка всех компонентов в dev режиме

## Dev Notes

### Расположение компонентов [Source: docs/architecture/04-component-structure.md]

```
frontend/src/components/
├── ui/
│   ├── Checkbox/
│   │   ├── Checkbox.tsx         # СОЗДАТЬ
│   │   ├── index.ts             # СОЗДАТЬ
│   │   └── __tests__/
│   │       └── Checkbox.test.tsx # СОЗДАТЬ
│   ├── Toggle/
│   │   ├── Toggle.tsx           # СОЗДАТЬ
│   │   └── ...
│   ├── Select/
│   │   ├── Select.tsx           # СОЗДАТЬ
│   │   └── ...
│   ├── SearchField/
│   │   ├── SearchField.tsx      # СОЗДАТЬ
│   │   └── ...
│   ├── PriceRangeSlider/
│   │   ├── PriceRangeSlider.tsx # СОЗДАТЬ
│   │   └── ...
│   ├── SortSelect/
│   │   ├── SortSelect.tsx       # СОЗДАТЬ
│   │   └── ...
│   ├── Chip/
│   │   ├── Chip.tsx             # СОЗДАТЬ
│   │   └── ...
│   ├── FilterGroup/
│   │   ├── FilterGroup.tsx      # СОЗДАТЬ
│   │   └── ...
│   └── index.ts                 # Добавить экспорты
├── business/
│   ├── SidebarFilters/
│   │   ├── SidebarFilters.tsx   # СОЗДАТЬ
│   │   ├── index.ts             # СОЗДАТЬ
│   │   └── __tests__/
│   │       └── SidebarFilters.test.tsx # СОЗДАТЬ
│   └── index.ts                 # Добавить экспорт
```

### Ключевые цвета Design System v2.0 [Source: docs/front-end-spec.md, docs/frontend/design-system.json]

```scss
// Primary (основной цвет для кнопок и checked states)
$primary-default: #1F1F1F;   // Для кнопок, checkbox checked
$primary-hover: #333333;

// Interactive/Accent (для интерактивных элементов)
$accent-interactive: #0060FF; // Toggle on, focus rings, selected chips, slider active
$accent-hover: #0050CC;

// Neutral
$neutral-border: #D0D7E6;    // Borders default
$neutral-bg: #E3E8F2;        // Toggle off, slider track
$checkbox-border: #B9C3D6;   // Checkbox border default

// Text
$text-primary: #1B1B1B;
$text-secondary: #4B5C7A;    // Chip default text
$text-muted: #7A7A7A;        // Icons, placeholders

// Surface
$surface-white: #FFFFFF;
$surface-hover: #F5F7FA;     // Option hover
$surface-selected: #E1F0FF;  // Selected option bg
```

**Примечание:** Checkbox использует `$primary-default` (#1F1F1F) для checked state, а Toggle, Chip selected, PriceRangeSlider и focus rings используют `$accent-interactive` (#0060FF).

### Типографика [Source: docs/frontend/design-system.json]

```typescript
// Для компонентов фильтров
title-s: { size: 16, lineHeight: 24, weight: 600 }  // Заголовки секций
body-m: { size: 16, lineHeight: 24, weight: 500 }   // Labels, options
body-s: { size: 14, lineHeight: 20, weight: 500 }   // Secondary text
caption: { size: 12, lineHeight: 16, weight: 500 }  // Chips
```

### Shadows и Motion [Source: docs/frontend/design-system.json]

```typescript
// Shadows
shadow-dropdown: "0 4px 12px rgba(0, 0, 0, 0.15)"
shadow-thumb: "0 2px 8px rgba(0, 0, 0, 0.16)"

// Motion
duration-fast: 120ms
duration-medium: 180ms
easing: "cubic-bezier(0.4, 0, 0.2, 1)"
```

### Спецификация из front-end-spec.md [Source: docs/front-end-spec.md#filters]

Макет боковых фильтров:
```
┌─────────────────┐
│ КАТЕГОРИЯ    [▼]│
│ ☑ Мячи          │
│ ☐ Бутсы         │
│ ☐ Форма         │
│                 │
│ БРЕНД        [▼]│
│ ☑ Nike          │
│ ☐ Adidas        │
│ ☐ Puma          │
│                 │
│ ЦЕНА         [▼]│
│ От: [____] ₽    │
│ До: [____] ₽    │
│ ────────────────│
│ [СБРОСИТЬ]      │
└─────────────────┘
```

Макет поиска с автодополнением:
```
┌───────────────────────────────────────────────────────────────────────────┐
│ ┌─ Автодополнение ───────────────────────────────────────────────────┐  │
│ │ 🔍 кроссовки для бега Nike                                        │  │
│ │ 🔍 кроссовки для бега Adidas                                      │  │
│ │ 🔍 кроссовки для бега женские                                     │  │
│ │ 📦 Nike Air Zoom Pegasus — кроссовки для бега      12 490 ₽       │  │
│ │ 📦 Adidas Ultraboost — беговые кроссовки           14 990 ₽       │  │
│ └───────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────────────┘
```

### Checkbox спецификация [Source: docs/front-end-spec.md#SidebarFilters строки 1315-1322]

```typescript
// Checkbox dimensions
boxSize: 20px
radius: 6px
borderDefault: '1.5px solid #B9C3D6'
borderChecked: '1.5px solid #1F1F1F'
backgroundChecked: '#1F1F1F'
icon: {
  type: 'lucide-check',
  size: 18px,
  strokeWidth: 3
}
label: {
  typography: 'body-m',
  spacing: 12px
}
```

### Toggle спецификация [Source: docs/front-end-spec.md#SidebarFilters строки 1324-1326]

```typescript
// Toggle dimensions
track: {
  width: 44px,
  height: 24px,
  radius: 999,
  colorOff: '#E3E8F2',
  colorOn: '#0060FF'
}
thumb: {
  size: 20px,
  color: '#FFFFFF',
  shadow: '0 2px 8px rgba(0, 0, 0, 0.16)'
}
```

### Select спецификация [Source: docs/front-end-spec.md#Select]

```typescript
// Select dimensions
height: 40px
padding: 16px
radius: 6px
borderDefault: '1px solid #D0D7E6'
borderFocus: '1.5px solid #0060FF'
```

### SearchField спецификация [Source: docs/front-end-spec.md#SearchField, design-system.json]

```typescript
// SearchField dimensions
height: 40px
radius: 6px
borderDefault: '1px solid #E3E8F2'
borderFocus: '1.5px solid #0060FF'
```

### Сортировка опции [Source: docs/front-end-spec.md#Product-Sorting]

```typescript
const SORT_OPTIONS = [
  { value: 'price_asc', label: 'Цена: по возрастанию', direction: 'asc' },
  { value: 'price_desc', label: 'Цена: по убыванию', direction: 'desc' },
  { value: 'stock_desc', label: 'По наличию', direction: 'desc' },
  { value: 'brand_asc', label: 'По бренду (А-Я)', direction: 'asc' },
  { value: 'name_asc', label: 'По названию (А-Я)', direction: 'asc' },
  { value: 'created_desc', label: 'Новинки', direction: 'desc' }
]
```

### Chip спецификация [Source: docs/front-end-spec.md#Chip]

```typescript
// Chip styles
default: {
  bg: '#FFFFFF',
  border: '1px solid #D0D7E6',
  text: '#4B5C7A'
}
selected: {
  bg: '#0060FF',
  text: '#FFFFFF'
}
radius: 16px
gap: 8px
```

### Зависимости

- **lucide-react**: Check, ChevronDown, Search, X
- **React 19.1.0**: forwardRef для компонентов
- **Tailwind CSS 4.0**: Стилизация

### Technical Constraints

- **TypeScript**: Строгая типизация всех props
- **Accessibility**: WCAG 2.1 AA compliance
- **Performance**: Memoization для списков, debounce для поиска
- **Browser Support**: Chrome, Firefox, Safari, Edge

### API Integration Notes

Фильтры будут получать данные из API:
- `/api/categories` - список категорий
- `/api/brands` - список брендов
- `/api/products/price-range` - min/max цены
- `/api/search/suggestions` - автодополнение поиска

## Testing

**Test file locations:**
- `frontend/src/components/ui/Checkbox/__tests__/Checkbox.test.tsx`
- `frontend/src/components/ui/Toggle/__tests__/Toggle.test.tsx`
- `frontend/src/components/ui/Select/__tests__/Select.test.tsx`
- `frontend/src/components/ui/SearchField/__tests__/SearchField.test.tsx`
- `frontend/src/components/ui/PriceRangeSlider/__tests__/PriceRangeSlider.test.tsx`
- `frontend/src/components/business/SidebarFilters/__tests__/SidebarFilters.test.tsx`

**Testing frameworks** [Source: CLAUDE.md]:
- Vitest 2.1.5 для unit тестов
- @testing-library/react 16.3.0 для компонентных тестов

**Test standards:**
- Каждый компонент тестируется отдельно
- Тестировать все interactive states
- Тестировать keyboard navigation
- Тестировать accessibility атрибуты
- Тестировать edge cases

**Coverage requirements:**
- Минимум 80% покрытие для всех компонентов

**Test commands:**
```bash
# Запуск тестов
npm test

# Запуск тестов с покрытием
npm run test:coverage

# Запуск конкретного теста
npx vitest run src/components/ui/Checkbox/__tests__/Checkbox.test.tsx
```

**Пример теста Checkbox:**
```typescript
// Checkbox.test.tsx
import { render, screen, fireEvent } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { Checkbox } from '../Checkbox'

describe('Checkbox', () => {
  it('renders unchecked by default', () => {
    render(<Checkbox checked={false} onChange={() => {}} label="Test" />)

    const checkbox = screen.getByRole('checkbox')
    expect(checkbox).not.toBeChecked()
    expect(screen.getByText('Test')).toBeInTheDocument()
  })

  it('toggles on click', async () => {
    const handleChange = vi.fn()
    render(<Checkbox checked={false} onChange={handleChange} label="Test" />)

    await userEvent.click(screen.getByRole('checkbox'))
    expect(handleChange).toHaveBeenCalledWith(true)
  })

  it('supports keyboard interaction', async () => {
    const handleChange = vi.fn()
    render(<Checkbox checked={false} onChange={handleChange} label="Test" />)

    const checkbox = screen.getByRole('checkbox')
    checkbox.focus()
    await userEvent.keyboard(' ')

    expect(handleChange).toHaveBeenCalledWith(true)
  })

  it('is disabled when disabled prop is true', () => {
    render(<Checkbox checked={false} onChange={() => {}} label="Test" disabled />)

    const checkbox = screen.getByRole('checkbox')
    expect(checkbox).toBeDisabled()
  })

  it('has correct accessibility attributes', () => {
    render(<Checkbox checked={true} onChange={() => {}} label="Test" />)

    const checkbox = screen.getByRole('checkbox')
    expect(checkbox).toHaveAttribute('aria-checked', 'true')
  })
})
```

**Пример теста Select с keyboard navigation:**
```typescript
// Select.test.tsx
import { render, screen, fireEvent } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { Select } from '../Select'

const options = [
  { value: 'opt1', label: 'Option 1' },
  { value: 'opt2', label: 'Option 2' },
  { value: 'opt3', label: 'Option 3' }
]

describe('Select', () => {
  it('opens dropdown on click', async () => {
    render(<Select options={options} onChange={() => {}} />)

    await userEvent.click(screen.getByRole('combobox'))
    expect(screen.getByRole('listbox')).toBeInTheDocument()
  })

  it('selects option on click', async () => {
    const handleChange = vi.fn()
    render(<Select options={options} onChange={handleChange} />)

    await userEvent.click(screen.getByRole('combobox'))
    await userEvent.click(screen.getByText('Option 2'))

    expect(handleChange).toHaveBeenCalledWith('opt2')
  })

  it('navigates with arrow keys', async () => {
    render(<Select options={options} onChange={() => {}} />)

    const trigger = screen.getByRole('combobox')
    trigger.focus()
    await userEvent.keyboard('{ArrowDown}')

    expect(screen.getByRole('listbox')).toBeInTheDocument()

    await userEvent.keyboard('{ArrowDown}')
    await userEvent.keyboard('{Enter}')

    expect(screen.queryByRole('listbox')).not.toBeInTheDocument()
  })

  it('closes on Escape', async () => {
    render(<Select options={options} onChange={() => {}} />)

    await userEvent.click(screen.getByRole('combobox'))
    await userEvent.keyboard('{Escape}')

    expect(screen.queryByRole('listbox')).not.toBeInTheDocument()
  })
})
```

## Change Log

| Date       | Version | Description                              | Author       |
|------------|---------|------------------------------------------|--------------|
| 2025-11-19 | 1.0     | Создание story на основе Epic 12.5       | Bob (SM)     |

## Dev Agent Record

### Agent Model Used

_To be filled by dev agent_

### Debug Log References

_To be filled by dev agent_

### Completion Notes List

_To be filled by dev agent_

### File List

_To be filled by dev agent_

## QA Results

_To be filled by QA agent_
