# Story 19.6: Обновление навигации

<!-- Powered by BMAD™ Core -->

## Status

**Draft**

---

## Story

**As a** пользователь платформы,  
**I want** видеть ссылки на информационные страницы,  
**so that** я могу легко найти нужную информацию.

---

## Acceptance Criteria

1. [ ] Добавлен пункт «Партнёрам» в Header navigation
2. [ ] Добавлены ссылки в Footer: О компании, Партнёрам, Доставка
3. [ ] Навигация работает на mobile (hamburger menu)
4. [ ] Active state для текущей страницы

---

## Dependencies

> [!IMPORTANT]
> **Блокирующая зависимость:** Stories 19.3, 19.4, 19.5 должны быть завершены (страницы созданы).

---

## Tasks / Subtasks

### Task 1: Обновить Header компонент (AC: 1, 4)

- [ ] 1.1 Открыть `frontend/src/components/layout/Header.tsx`
- [ ] 1.2 Добавить пункт «Партнёрам» → `/partners` в навигацию
- [ ] 1.3 Расположить между «Акции» и поиском
- [ ] 1.4 Реализовать active state (подчёркивание текущей страницы)
- [ ] 1.5 Обновить unit-тесты для Header

### Task 2: Обновить Footer компонент (AC: 2)

- [ ] 2.1 Открыть `frontend/src/components/layout/Footer.tsx`
- [ ] 2.2 Добавить колонку или секцию «Информация» с ссылками:
  - О компании → `/about`
  - Партнёрам → `/partners`
  - Доставка → `/delivery`
- [ ] 2.3 Обновить unit-тесты для Footer

### Task 3: Обновить MobileMenu (AC: 3)

- [ ] 3.1 Найти компонент мобильного меню (MobileMenu или MobileNav)
- [ ] 3.2 Добавить ссылку «Партнёрам» → `/partners`
- [ ] 3.3 Проверить работу hamburger menu

### Task 4: Протестировать навигацию (AC: 1, 2, 3, 4)

- [ ] 4.1 Проверить все ссылки на desktop
- [ ] 4.2 Проверить все ссылки на mobile
- [ ] 4.3 Проверить active state на всех страницах

---

## Dev Notes

### Существующие компоненты

[Source: docs/architecture/source-tree.md]

```text
frontend/src/components/layout/
├── Header.tsx        # Шапка сайта
├── Footer.tsx        # Подвал сайта
├── MobileMenu.tsx    # Мобильное меню (если есть)
└── ...
```

### Структура навигации Header

[Source: docs/frontend/info-pages-spec.md#Navigation Updates]

**До изменений:**

```
[Логотип] [Акции] [Поиск...] [♡ Избранное] [🛒 Корзина] [👤 Профиль]
```

**После изменений:**

```
[Логотип] [Акции] [Партнёрам] [Поиск...] [♡] [🛒] [👤]
```

### Структура Footer

```
┌─────────────────────────────────────────────────────────┐
│  FREESPORT                                              │
│                                                         │
│  Каталог        Информация      Контакты               │
│  ├── Товары     ├── О компании  ├── Телефон            │
│  ├── Бренды     ├── Партнёрам   ├── Email              │
│  └── Акции      └── Доставка    └── Адрес              │
│                                                         │
│  © 2024 FREESPORT. Все права защищены.                 │
└─────────────────────────────────────────────────────────┘
```

### Навигационные ссылки

```typescript
const infoLinks = [
  { label: 'О компании', href: '/about' },
  { label: 'Партнёрам', href: '/partners' },
  { label: 'Доставка', href: '/delivery' },
];
```

### Active state

```typescript
'use client';

import { usePathname } from 'next/navigation';
import Link from 'next/link';

const NavLink = ({ href, children }) => {
  const pathname = usePathname();
  const isActive = pathname === href;
  
  return (
    <Link 
      href={href}
      className={`nav-link ${isActive ? 'nav-link--active' : ''}`}
    >
      {children}
    </Link>
  );
};
```

### CSS для active state

```css
.nav-link {
  color: var(--color-text-secondary);
  font-weight: 500;
  transition: color 0.18s ease;
}

.nav-link:hover {
  color: var(--color-primary);
}

.nav-link--active {
  color: var(--color-primary);
  position: relative;
}

.nav-link--active::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 0;
  right: 0;
  height: 3px;
  background: var(--color-primary);
  border-radius: 999px;
}
```

### Mobile меню

```typescript
// Добавить в массив menuItems
const mobileMenuItems = [
  { label: 'Каталог', href: '/catalog' },
  { label: 'Акции', href: '/promo' },
  { label: 'Партнёрам', href: '/partners' }, // НОВЫЙ
  // ...
];
```

---

## Testing

### Ключевые тест-кейсы

**Header:**

```typescript
describe('Header', () => {
  it('renders "Партнёрам" link');
  it('navigates to /partners on click');
  it('shows active state for current page');
});
```

**Footer:**

```typescript
describe('Footer', () => {
  it('renders info section with 3 links');
  it('renders "О компании" link to /about');
  it('renders "Партнёрам" link to /partners');
  it('renders "Доставка" link to /delivery');
});
```

**MobileMenu:**

```typescript
describe('MobileMenu', () => {
  it('renders "Партнёрам" link');
  it('closes menu on link click');
});
```

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-12-22 | 1.0 | Начальный черновик истории | Bob (SM) |

---

## Dev Agent Record

_To be filled by Dev Agent_

---

## QA Results

_To be filled by QA Agent_
