# Story 15.3a: Backend Delivery API

## Status

Done 

## Story

**As a** разработчик платформы FREESPORT,
**I want** создать backend API для управления способами доставки,
**so that** frontend может получить список доступных способов доставки, а администратор может управлять ими через Django Admin.

## Acceptance Criteria

1. ✅ Создан Django app `apps/delivery/` с моделью `DeliveryMethod` и конфигурацией `DeliveryConfig`
2. ✅ API endpoint `GET /api/v1/delivery/methods/` возвращает список способов доставки
3. ✅ Модель содержит поля: id, name, description, icon, is_available, sort_order
4. ✅ Django Admin интерфейс для управления способами доставки
5. ✅ Fixtures с начальными данными (Курьер, Самовывоз, Транспортная компания)
6. ✅ Unit-тесты для API endpoint (включая edge cases: retrieve single, filter by is_available)
7. ✅ OpenAPI документация (drf-spectacular)

## Tasks / Subtasks

### Задача 1: Создание Django app delivery (AC: 1)

- [x] Создать app: `python manage.py startapp delivery apps/delivery`
- [x] Настроить `apps/delivery/apps.py`:

  ```python
  from django.apps import AppConfig

  class DeliveryConfig(AppConfig):
      default_auto_field = 'django.db.models.BigAutoField'
      name = 'apps.delivery'
      verbose_name = 'Доставка'
  ```

- [x] Добавить `apps.delivery` в `INSTALLED_APPS` в `settings/base.py`
- [x] Создать структуру файлов:

  ```text
  apps/delivery/
  ├── __init__.py
  ├── admin.py
  ├── apps.py
  ├── models.py
  ├── serializers.py
  ├── views.py
  ├── urls.py
  ├── fixtures/
  │   └── delivery_methods.json
  └── tests/
      ├── __init__.py
      └── test_delivery_api.py
  ```

### Задача 2: Модель DeliveryMethod (AC: 1, 3)

- [x] Создать модель в `apps/delivery/models.py`:

  ```python
  from django.db import models

  class DeliveryMethod(models.Model):
      """Способ доставки"""
      
      id = models.CharField(max_length=50, primary_key=True)  # courier, pickup, transport
      name = models.CharField("Название", max_length=100)
      description = models.TextField("Описание", blank=True)
      icon = models.CharField("Иконка", max_length=50, blank=True, help_text="CSS класс иконки или emoji для UI")
      is_available = models.BooleanField("Доступен", default=True)
      sort_order = models.PositiveIntegerField("Порядок сортировки", default=0)
      created_at = models.DateTimeField(auto_now_add=True)
      updated_at = models.DateTimeField(auto_now=True)

      class Meta:
          verbose_name = "Способ доставки"
          verbose_name_plural = "Способы доставки"
          ordering = ["sort_order", "name"]

      def __str__(self):
          return self.name
  ```

- [x] Создать и применить миграции: `python manage.py makemigrations delivery && python manage.py migrate`

### Задача 3: Serializer и ViewSet (AC: 2, 7)

- [x] Создать `apps/delivery/serializers.py`:

  ```python
  from rest_framework import serializers
  from .models import DeliveryMethod

  class DeliveryMethodSerializer(serializers.ModelSerializer):
      class Meta:
          model = DeliveryMethod
          fields = ["id", "name", "description", "icon", "is_available"]
          read_only_fields = fields
  ```

- [x] Создать `apps/delivery/views.py`:

  ```python
  from drf_spectacular.utils import extend_schema, extend_schema_view
  from rest_framework import viewsets, permissions
  
  from .models import DeliveryMethod
  from .serializers import DeliveryMethodSerializer

  @extend_schema_view(
      list=extend_schema(
          summary="Список способов доставки",
          description="Возвращает список доступных способов доставки",
          tags=["Delivery"]
      ),
      retrieve=extend_schema(
          summary="Получить способ доставки",
          description="Возвращает детали конкретного способа доставки по ID",
          tags=["Delivery"]
      )
  )
  class DeliveryMethodViewSet(viewsets.ReadOnlyModelViewSet):
      """
      ViewSet для получения списка способов доставки.
      Только чтение - управление через Django Admin.
      """
      queryset = DeliveryMethod.objects.filter(is_available=True)
      serializer_class = DeliveryMethodSerializer
      permission_classes = [permissions.AllowAny]  # Доступно без авторизации
  ```

### Задача 4: URL routing (AC: 2)

- [x] Создать `apps/delivery/urls.py`:

  ```python
  from django.urls import include, path
  from rest_framework.routers import DefaultRouter
  from .views import DeliveryMethodViewSet

  router = DefaultRouter()
  router.register(r"methods", DeliveryMethodViewSet, basename="delivery-method")

  app_name = "delivery"

  urlpatterns = [
      path("", include(router.urls)),
  ]
  ```

- [x] Подключить в `freesport/urls.py`:

  ```python
  urlpatterns = [
      # ... существующие urls
      path("api/v1/delivery/", include("apps.delivery.urls")),
  ]
  ```

### Задача 5: Django Admin (AC: 4)

- [x] Создать `apps/delivery/admin.py`:

  ```python
  from django.contrib import admin
  from .models import DeliveryMethod

  @admin.register(DeliveryMethod)
  class DeliveryMethodAdmin(admin.ModelAdmin):
      list_display = ["id", "name", "icon", "is_available", "sort_order"]
      list_editable = ["is_available", "sort_order"]
      list_filter = ["is_available"]
      search_fields = ["name", "description"]
      ordering = ["sort_order"]
      fieldsets = (
          (None, {
              'fields': ('id', 'name', 'description', 'icon')
          }),
          ('Настройки', {
              'fields': ('is_available', 'sort_order'),
          }),
      )
  ```

### Задача 6: Fixtures с начальными данными (AC: 5)

- [x] Создать `apps/delivery/fixtures/delivery_methods.json`:

  ```json
  [
    {
      "model": "delivery.deliverymethod",
      "pk": "courier",
      "fields": {
        "name": "Курьер",
        "description": "Доставка курьером до двери. Стоимость уточняется администратором.",
        "icon": "🚚",
        "is_available": true,
        "sort_order": 1
      }
    },
    {
      "model": "delivery.deliverymethod",
      "pk": "pickup",
      "fields": {
        "name": "Самовывоз",
        "description": "Забрать заказ самостоятельно из пункта выдачи.",
        "icon": "🏪",
        "is_available": true,
        "sort_order": 2
      }
    },
    {
      "model": "delivery.deliverymethod",
      "pk": "transport_company",
      "fields": {
        "name": "Транспортная компания",
        "description": "Доставка через CDEK, Boxberry и другие службы. Стоимость уточняется.",
        "icon": "📦",
        "is_available": true,
        "sort_order": 3
      }
    }
  ]
  ```

- [x] Загрузить fixtures: `python manage.py loaddata delivery_methods`

### Задача 7: Unit-тесты API (AC: 6)

- [x] Создать `apps/delivery/tests/test_delivery_api.py`:

  ```python
  import pytest
  from rest_framework.test import APIClient
  from apps.delivery.models import DeliveryMethod

  @pytest.fixture
  def api_client():
      return APIClient()

  @pytest.fixture
  def delivery_methods():
      return [
          DeliveryMethod.objects.create(
              id="courier", name="Курьер", description="До двери", 
              icon="🚚", is_available=True, sort_order=1
          ),
          DeliveryMethod.objects.create(
              id="pickup", name="Самовывоз", description="Забрать", 
              icon="🏪", is_available=True, sort_order=2
          ),
          DeliveryMethod.objects.create(
              id="disabled", name="Отключён", description="", 
              icon="❌", is_available=False, sort_order=3
          ),
      ]

  @pytest.mark.django_db
  class TestDeliveryMethodsListAPI:
      """Тесты для GET /api/v1/delivery/methods/"""

      def test_list_returns_available_methods(self, api_client, delivery_methods):
          """Проверяет, что API возвращает только доступные способы доставки"""
          response = api_client.get("/api/v1/delivery/methods/")
          assert response.status_code == 200
          assert len(response.data) == 2  # Только is_available=True

      def test_list_excludes_unavailable(self, api_client, delivery_methods):
          """Проверяет, что недоступные способы исключаются из списка"""
          response = api_client.get("/api/v1/delivery/methods/")
          ids = [m["id"] for m in response.data]
          assert "disabled" not in ids

      def test_response_structure(self, api_client, delivery_methods):
          """Проверяет структуру ответа API"""
          response = api_client.get("/api/v1/delivery/methods/")
          method = response.data[0]
          assert "id" in method
          assert "name" in method
          assert "description" in method
          assert "icon" in method
          assert "is_available" in method

      def test_empty_list_returns_200(self, api_client):
          """Проверяет, что пустой список возвращает 200"""
          response = api_client.get("/api/v1/delivery/methods/")
          assert response.status_code == 200
          assert response.data == []

      def test_ordering_by_sort_order(self, api_client, delivery_methods):
          """Проверяет сортировку по sort_order"""
          response = api_client.get("/api/v1/delivery/methods/")
          ids = [m["id"] for m in response.data]
          assert ids == ["courier", "pickup"]  # Сортировка по sort_order

  @pytest.mark.django_db
  class TestDeliveryMethodRetrieveAPI:
      """Тесты для GET /api/v1/delivery/methods/{id}/"""

      def test_retrieve_single_method(self, api_client, delivery_methods):
          """Проверяет получение одного способа доставки по ID"""
          response = api_client.get("/api/v1/delivery/methods/courier/")
          assert response.status_code == 200
          assert response.data["id"] == "courier"
          assert response.data["name"] == "Курьер"
          assert response.data["icon"] == "🚚"

      def test_retrieve_unavailable_method_returns_404(self, api_client, delivery_methods):
          """Проверяет, что недоступный способ возвращает 404"""
          response = api_client.get("/api/v1/delivery/methods/disabled/")
          assert response.status_code == 404

      def test_retrieve_nonexistent_method_returns_404(self, api_client):
          """Проверяет, что несуществующий ID возвращает 404"""
          response = api_client.get("/api/v1/delivery/methods/nonexistent/")
          assert response.status_code == 404
  ```

- [x] Запустить тесты: `pytest apps/delivery/tests/ -v`

## Dev Notes

### Архитектурный контекст

**Эта история создаёт backend часть для Story 15.3b (Frontend).**

**Технологический стек (Backend):**

- Django 4.2 LTS
- Django REST Framework 3.14+
- drf-spectacular для OpenAPI
- pytest для тестирования

[Source: architecture/tech-stack.md#Backend]

### API Response

**GET /api/v1/delivery/methods/**

```json
[
  {
    "id": "courier",
    "name": "Курьер",
    "description": "Доставка курьером до двери. Стоимость уточняется администратором.",
    "icon": "🚚",
    "is_available": true
  },
  {
    "id": "pickup",
    "name": "Самовывоз",
    "description": "Забрать заказ самостоятельно из пункта выдачи.",
    "icon": "🏪",
    "is_available": true
  },
  {
    "id": "transport_company",
    "name": "Транспортная компания",
    "description": "Доставка через CDEK, Boxberry и другие службы. Стоимость уточняется.",
    "icon": "📦",
    "is_available": true
  }
]
```

**GET /api/v1/delivery/methods/{id}/**

```json
{
  "id": "courier",
  "name": "Курьер",
  "description": "Доставка курьером до двери. Стоимость уточняется администратором.",
  "icon": "🚚",
  "is_available": true
}
```

**⚠️ ВАЖНО:** Поле `price` отсутствует - стоимость НЕ рассчитывается на данном этапе.

### Testing

**Расположение тестов:** `apps/delivery/tests/`

**Стандарты тестирования:**

- Использовать pytest-django
- Marker `@pytest.mark.django_db` для тестов с БД
- Factory Boy для создания тестовых данных (опционально)
- Docstrings для каждого теста с описанием проверяемой функциональности

**Команды запуска:**

```bash
# Запуск всех тестов delivery app
pytest apps/delivery/tests/ -v

# С покрытием кода
pytest apps/delivery/tests/ --cov=apps.delivery --cov-report=term-missing

# Запуск конкретного класса тестов
pytest apps/delivery/tests/test_delivery_api.py::TestDeliveryMethodsListAPI -v
```

**Требования к покрытию:** ≥ 80%

### Зависимости

- **Блокирует:** Story 15.3b (Frontend DeliveryOptions)
- **Зависит от:** Ничего (независимая история)

## Change Log

| Date       | Version | Description                                              | Author          |
|------------|---------|----------------------------------------------------------|-----------------|
| 2025-12-14 | 1.0.0   | Создание backend story для delivery API                  | Sarah (PO Agent)|
| 2025-12-14 | 1.1.0   | Добавлены: icon поле, apps.py, edge case тесты, Testing  | Sarah (PO Agent)|
| 2025-12-14 | 1.2.0   | Реализация: Django app, модель, API, тесты (100% coverage)| James (Dev)     |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References

Нет debug логов - реализация прошла без критических ошибок.

### Completion Notes

- Все 7 задач выполнены успешно
- 11 unit-тестов пройдено с 100% покрытием кода
- API endpoints работают корректно (GET /api/v1/delivery/methods/, GET /api/v1/delivery/methods/{id}/)
- Fixtures загружены (3 способа доставки: courier, pickup, transport_company)
- Использованы текстовые идентификаторы для иконок (truck, store, package) вместо emoji для избежания проблем с кодировкой

### File List

**Созданные файлы:**
- `backend/apps/delivery/__init__.py`
- `backend/apps/delivery/apps.py`
- `backend/apps/delivery/models.py`
- `backend/apps/delivery/serializers.py`
- `backend/apps/delivery/views.py`
- `backend/apps/delivery/urls.py`
- `backend/apps/delivery/admin.py`
- `backend/apps/delivery/fixtures/delivery_methods.json`
- `backend/apps/delivery/tests/__init__.py`
- `backend/apps/delivery/tests/test_delivery_api.py`
- `backend/apps/delivery/migrations/0001_initial.py`

**Модифицированные файлы:**
- `backend/freesport/settings/base.py` (добавлен apps.delivery в INSTALLED_APPS)
- `backend/freesport/urls.py` (добавлен маршрут api/v1/delivery/)

## QA Results

### Review Date: 2025-12-14

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Отлично.** Реализация delivery API выполнена качественно и полностью соответствует требованиям истории. Код чистый, хорошо структурирован, с полной типизацией и документацией. Все 11 тестов прошли с 100% покрытием кода.

**Сильные стороны:**
- Чёткая структура Django app согласно стандартам проекта
- Полная типизация с использованием `TYPE_CHECKING` для избежания circular imports
- Docstrings на всех классах и методах
- OpenAPI документация через `@extend_schema_view` декораторы
- Тесты покрывают все edge cases: пустой список, недоступные методы, несуществующие ID

### Refactoring Performed

Рефакторинг не требуется - код уже соответствует стандартам.

### Compliance Check

- Coding Standards: ✓ Соответствует `docs/architecture/coding-standards.md`
- Project Structure: ✓ Структура `apps/delivery/` корректна
- Testing Strategy: ✓ 11 unit-тестов, 100% покрытие (требование ≥70%)
- All ACs Met: ✓ Все 7 критериев приёмки выполнены

### Requirements Traceability

| AC# | Описание | Тесты | Статус |
|-----|----------|-------|--------|
| 1 | Django app `apps/delivery/` с моделью | `TestDeliveryMethodModel` (3 теста) | ✅ |
| 2 | GET /api/v1/delivery/methods/ | `TestDeliveryMethodsListAPI` (5 тестов) | ✅ |
| 3 | Поля модели: id, name, description, icon, is_available, sort_order | `test_response_structure`, `test_default_values` | ✅ |
| 4 | Django Admin интерфейс | Код в `admin.py` проверен | ✅ |
| 5 | Fixtures с начальными данными | `delivery_methods.json` (3 записи) | ✅ |
| 6 | Unit-тесты для API | 11 тестов, 100% coverage | ✅ |
| 7 | OpenAPI документация | `@extend_schema_view` декораторы | ✅ |

### Improvements Checklist

- [x] Все AC выполнены
- [x] Тесты покрывают все edge cases
- [x] Код соответствует стандартам
- [ ] *Опционально*: в `views.py` queryset дублируется (на уровне класса и в `get_queryset()`) - не критично, но можно упростить

### Security Review

✅ **Без замечаний.**
- `permission_classes = [permissions.AllowAny]` корректно для публичного read-only API
- Нет записи данных, только чтение
- Фильтрация `is_available=True` на уровне queryset исключает служебные данные

### Performance Considerations

✅ **Без замечаний.**
- Простые SELECT запросы без JOIN
- Ordering по индексируемым полям (`sort_order`, `name`)
- Нет N+1 проблем

### Files Modified During Review

Нет - код не требовал изменений.

### Gate Status

**Gate: PASS** → `docs/qa/gates/15.3a-delivery-backend-api.yml`

### Recommended Status

✅ **Ready for Done** - все критерии приёмки выполнены, тесты проходят с 100% покрытием
