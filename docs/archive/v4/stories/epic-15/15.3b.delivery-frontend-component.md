# Story 15.3b: Frontend DeliveryOptions Component

## Status

Done

## Story

**As a** пользователь платформы FREESPORT,
**I want** выбрать способ доставки из предустановленного списка,
**so that** я могу указать предпочтительный способ получения заказа.

## Acceptance Criteria

1. ✅ Компонент DeliveryOptions получает список методов через deliveryService
2. ✅ Radio group для выбора способа доставки (с role="radiogroup" и aria-label)
3. ✅ Отображение icon, name и description каждого метода
4. ✅ Стоимость отображается как "Уточняется администратором"
5. ✅ Интеграция с React Hook Form (регистрация поля deliveryMethod)
6. ✅ Обработка loading и error состояний (с aria-label для skeleton)
7. ✅ Unit-тесты для deliveryService и DeliveryOptions (Vitest + MSW)

## Tasks / Subtasks

### Задача 1: Создание deliveryService (AC: 1)

- [x] Создать `src/services/deliveryService.ts`:

  ```typescript
  import { apiClient } from "./api-client";
  import type { DeliveryMethod } from "@/types/delivery";

  export const deliveryService = {
    async getDeliveryMethods(): Promise<DeliveryMethod[]> {
      const response = await apiClient.get<DeliveryMethod[]>(
        "/api/v1/delivery/methods/",
      );
      return response.data;
    },
  };

  export default deliveryService;
  ```

### Задача 2: Создание типов TypeScript (AC: 1, 3)

- [x] Создать `src/types/delivery.ts`:

  ```typescript
  /**
   * Способ доставки, получаемый от API
   * Синхронизировано с Story 15.3a (Backend Delivery API)
   */
  export interface DeliveryMethod {
    id: string;
    name: string;
    description: string;
    icon: string; // Emoji или CSS класс иконки для UI отображения
    is_available: boolean;
  }
  ```

### Задача 3: Компонент DeliveryOptions (AC: 2, 3, 4, 5, 6)

- [x] Создать `src/components/checkout/DeliveryOptions.tsx`:

  ```typescript
  'use client'

  import { useEffect, useState } from 'react'
  import { UseFormReturn } from 'react-hook-form'
  import { CheckoutFormData } from '@/schemas/checkoutSchema'
  import deliveryService from '@/services/deliveryService'
  import type { DeliveryMethod } from '@/types/delivery'

  interface DeliveryOptionsProps {
    form: UseFormReturn<CheckoutFormData>
  }

  export const DeliveryOptions: React.FC<DeliveryOptionsProps> = ({ form }) => {
    const [methods, setMethods] = useState<DeliveryMethod[]>([])
    const [isLoading, setIsLoading] = useState(true)
    const [error, setError] = useState<string | null>(null)

    useEffect(() => {
      const fetchMethods = async () => {
        try {
          setIsLoading(true)
          const data = await deliveryService.getDeliveryMethods()
          setMethods(data)
          setError(null)
        } catch (err) {
          setError('Не удалось загрузить способы доставки')
          console.error('Failed to fetch delivery methods:', err)
        } finally {
          setIsLoading(false)
        }
      }

      fetchMethods()
    }, [])

    if (isLoading) {
      return (
        <div
          className="animate-pulse"
          role="status"
          aria-label="Загрузка способов доставки"
        >
          <div className="h-4 bg-gray-200 rounded w-1/3 mb-4"></div>
          <div className="space-y-3">
            <div className="h-16 bg-gray-200 rounded"></div>
            <div className="h-16 bg-gray-200 rounded"></div>
            <div className="h-16 bg-gray-200 rounded"></div>
          </div>
        </div>
      )
    }

    if (error) {
      return (
        <div
          className="text-red-600 p-4 border border-red-300 rounded-lg"
          role="alert"
        >
          {error}
        </div>
      )
    }

    const errorId = 'delivery-method-error'

    return (
      <fieldset className="border p-4 rounded-lg">
        <legend className="text-lg font-semibold">Способ доставки</legend>

        <div
          className="space-y-3 mt-4"
          role="radiogroup"
          aria-label="Выбор способа доставки"
          aria-describedby={form.formState.errors.deliveryMethod ? errorId : undefined}
        >
          {methods.map((method) => (
            <label
              key={method.id}
              className="flex items-start gap-3 cursor-pointer p-3 border rounded-lg hover:bg-gray-50 transition-colors"
            >
              <input
                type="radio"
                value={method.id}
                {...form.register('deliveryMethod')}
                className="mt-1"
                disabled={!method.is_available}
                aria-describedby={`${method.id}-description`}
              />
              <div className="flex-1">
                <p className="font-medium">
                  {method.icon && <span className="mr-2">{method.icon}</span>}
                  {method.name}
                </p>
                <p
                  id={`${method.id}-description`}
                  className="text-sm text-gray-600"
                >
                  {method.description}
                </p>
                <p className="text-sm text-gray-500 mt-1">
                  Стоимость: <span className="font-medium">Уточняется администратором</span>
                </p>
              </div>
            </label>
          ))}
        </div>

        {form.formState.errors.deliveryMethod && (
          <p id={errorId} className="text-red-600 text-sm mt-2" role="alert">
            {form.formState.errors.deliveryMethod.message}
          </p>
        )}
      </fieldset>
    )
  }

  export default DeliveryOptions
  ```

### Задача 4: Интеграция в CheckoutForm (AC: 5)

- [x] В `src/components/checkout/CheckoutForm.tsx`:
  - Удалить placeholder для DeliveryOptions
  - Импортировать: `import { DeliveryOptions } from './DeliveryOptions'`
  - Использовать: `<DeliveryOptions form={form} />`

### Задача 5: Unit-тесты deliveryService (AC: 7)

- [x] Создать `src/services/__tests__/deliveryService.test.ts`:

  ```typescript
  import { describe, it, expect, beforeAll, afterEach, afterAll } from "vitest";
  import { http, HttpResponse } from "msw";
  import { setupServer } from "msw/node";
  import deliveryService from "../deliveryService";

  const mockMethods = [
    {
      id: "courier",
      name: "Курьер",
      description: "До двери",
      icon: "🚚",
      is_available: true,
    },
    {
      id: "pickup",
      name: "Самовывоз",
      description: "Забрать",
      icon: "🏪",
      is_available: true,
    },
  ];

  const server = setupServer(
    http.get("/api/v1/delivery/methods/", () => {
      return HttpResponse.json(mockMethods);
    }),
  );

  beforeAll(() => server.listen());
  afterEach(() => server.resetHandlers());
  afterAll(() => server.close());

  describe("deliveryService", () => {
    it("returns list of delivery methods", async () => {
      const methods = await deliveryService.getDeliveryMethods();
      expect(methods).toHaveLength(2);
      expect(methods[0].id).toBe("courier");
      expect(methods[0].icon).toBe("🚚");
    });

    it("handles empty response", async () => {
      server.use(
        http.get("/api/v1/delivery/methods/", () => {
          return HttpResponse.json([]);
        }),
      );
      const methods = await deliveryService.getDeliveryMethods();
      expect(methods).toEqual([]);
    });

    it("throws on network error", async () => {
      server.use(
        http.get("/api/v1/delivery/methods/", () => {
          return HttpResponse.error();
        }),
      );
      await expect(deliveryService.getDeliveryMethods()).rejects.toThrow();
    });
  });
  ```

### Задача 6: Unit-тесты DeliveryOptions (AC: 7)

- [x] Создать `src/components/checkout/__tests__/DeliveryOptions.test.tsx`:

  ```typescript
  import { describe, it, expect, beforeEach, vi } from 'vitest'
  import { render, screen, waitFor } from '@testing-library/react'
  import userEvent from '@testing-library/user-event'
  import { useForm } from 'react-hook-form'
  import { DeliveryOptions } from '../DeliveryOptions'
  import deliveryService from '@/services/deliveryService'

  // Mock deliveryService with Vitest
  vi.mock('@/services/deliveryService')

  const mockMethods = [
    { id: 'courier', name: 'Курьер', description: 'До двери', icon: '🚚', is_available: true },
    { id: 'pickup', name: 'Самовывоз', description: 'Забрать', icon: '🏪', is_available: true }
  ]

  // Test wrapper component
  const TestWrapper = () => {
    const form = useForm({ defaultValues: { deliveryMethod: '' } })
    return <DeliveryOptions form={form} />
  }

  describe('DeliveryOptions', () => {
    beforeEach(() => {
      vi.resetAllMocks()
      vi.mocked(deliveryService.getDeliveryMethods).mockResolvedValue(mockMethods)
    })

    describe('Loading State', () => {
      it('renders loading skeleton with aria-label', () => {
        render(<TestWrapper />)
        const loadingElement = screen.getByRole('status')
        expect(loadingElement).toHaveAttribute('aria-label', 'Загрузка способов доставки')
      })

      it('shows animated skeleton placeholders', () => {
        render(<TestWrapper />)
        const loadingElement = screen.getByRole('status')
        expect(loadingElement).toHaveClass('animate-pulse')
      })
    })

    describe('Success State', () => {
      it('renders all delivery methods after loading', async () => {
        render(<TestWrapper />)

        await waitFor(() => {
          expect(screen.getByText('Курьер')).toBeInTheDocument()
          expect(screen.getByText('Самовывоз')).toBeInTheDocument()
        })
      })

      it('displays icons for each delivery method', async () => {
        render(<TestWrapper />)

        await waitFor(() => {
          expect(screen.getByText('🚚')).toBeInTheDocument()
          expect(screen.getByText('🏪')).toBeInTheDocument()
        })
      })

      it('shows "Уточняется администратором" for each method', async () => {
        render(<TestWrapper />)

        await waitFor(() => {
          const priceTexts = screen.getAllByText(/уточняется администратором/i)
          expect(priceTexts).toHaveLength(2)
        })
      })

      it('does NOT show numeric price (₽ or руб)', async () => {
        render(<TestWrapper />)

        await waitFor(() => {
          expect(screen.queryByText(/₽/)).not.toBeInTheDocument()
          expect(screen.queryByText(/руб/i)).not.toBeInTheDocument()
        })
      })

      it('renders radiogroup with correct aria attributes', async () => {
        render(<TestWrapper />)

        await waitFor(() => {
          const radiogroup = screen.getByRole('radiogroup')
          expect(radiogroup).toHaveAttribute('aria-label', 'Выбор способа доставки')
        })
      })

      it('allows selecting a delivery method', async () => {
        const user = userEvent.setup()
        render(<TestWrapper />)

        await waitFor(() => {
          expect(screen.getByText('Курьер')).toBeInTheDocument()
        })

        const courierRadio = screen.getByRole('radio', { name: /курьер/i })
        await user.click(courierRadio)

        expect(courierRadio).toBeChecked()
      })
    })

    describe('Error State', () => {
      it('shows error message on fetch failure', async () => {
        vi.mocked(deliveryService.getDeliveryMethods).mockRejectedValue(new Error('Network'))

        render(<TestWrapper />)

        await waitFor(() => {
          expect(screen.getByText(/не удалось загрузить/i)).toBeInTheDocument()
        })
      })

      it('renders error with alert role', async () => {
        vi.mocked(deliveryService.getDeliveryMethods).mockRejectedValue(new Error('Network'))

        render(<TestWrapper />)

        await waitFor(() => {
          const errorElement = screen.getByRole('alert')
          expect(errorElement).toBeInTheDocument()
        })
      })
    })
  })
  ```

## Dev Notes

### Зависимости

- **Зависит от:** Story 15.3a (Backend Delivery API) - **БЛОКИРУЮЩАЯ**
- **Зависит от:** Story 15.1 (CheckoutForm компонент)
- **Блокирует:** Ничего

### Структура файлов

```
frontend/src/
├── services/
│   ├── deliveryService.ts           # [NEW]
│   └── __tests__/
│       └── deliveryService.test.ts  # [NEW]
├── types/
│   └── delivery.ts                  # [NEW]
└── components/
    └── checkout/
        ├── DeliveryOptions.tsx      # [NEW]
        ├── CheckoutForm.tsx         # [MODIFY] - интеграция DeliveryOptions
        └── __tests__/
            └── DeliveryOptions.test.tsx  # [NEW]
```

### API Endpoint

**GET /api/v1/delivery/methods/** (создаётся в Story 15.3a)

```json
[
  {
    "id": "courier",
    "name": "Курьер",
    "description": "...",
    "icon": "🚚",
    "is_available": true
  },
  {
    "id": "pickup",
    "name": "Самовывоз",
    "description": "...",
    "icon": "🏪",
    "is_available": true
  },
  {
    "id": "transport_company",
    "name": "Транспортная компания",
    "description": "...",
    "icon": "📦",
    "is_available": true
  }
]
```

### React Hook Form интеграция

Компонент получает `form` как prop и использует `form.register('deliveryMethod')` для интеграции с формой checkout.

[Source: architecture/coding-standards.md#React Hook Form]

### Testing

**Расположение тестов:**

- `src/services/__tests__/deliveryService.test.ts`
- `src/components/checkout/__tests__/DeliveryOptions.test.tsx`

**Стандарты тестирования:**

- **Vitest 2.1.5** — тестовый раннер (НЕ Jest!)
- **React Testing Library 16.3.0** — тестирование компонентов
- **MSW 2.12.2** — мокирование API запросов
- **`vi.mock()`** вместо `jest.mock()` для модульных моков
- **`vi.fn()`** вместо `jest.fn()` для функций-моков

**Команды запуска:**

```bash
# Запуск всех тестов
npm run test

# Watch mode для разработки
npm run test:watch

# С покрытием кода
npm run test:coverage

# Запуск конкретного файла
npx vitest run src/components/checkout/__tests__/DeliveryOptions.test.tsx
```

**Требования к покрытию:** ≥ 80%

### Accessibility (a11y)

Компонент соответствует WCAG 2.1 AA:

- `role="radiogroup"` с `aria-label` для группы радиокнопок
- `role="status"` с `aria-label` для loading state
- `role="alert"` для сообщений об ошибках
- `aria-describedby` для связи ошибок с полями
- Semantic HTML: `<fieldset>`, `<legend>`, `<label>`

## Change Log

| Date       | Version | Description                                             | Author            |
| ---------- | ------- | ------------------------------------------------------- | ----------------- |
| 2025-12-14 | 1.0.0   | Создание frontend story для DeliveryOptions             | Sarah (PO Agent)  |
| 2025-12-14 | 1.1.0   | Добавлены: icon field, Vitest API, a11y, Testing секция | Sarah (PO Agent)  |
| 2025-12-14 | 1.2.0   | Реализация: все 6 задач выполнены, 27 тестов проходят   | James (Dev Agent) |

## Dev Agent Record

### Agent Model Used

Claude Opus 4 (claude-opus-4-5-20251101)

### Debug Log References

N/A

### Completion Notes

**Реализация завершена успешно:**

1. Создан `deliveryService.ts` - сервис для получения способов доставки через API
2. Создан `delivery.ts` - TypeScript типы для DeliveryMethod
3. Создан `DeliveryOptions.tsx` - компонент с:
   - Loading state (skeleton с aria-label)
   - Error state (alert role)
   - Radio group с WCAG 2.1 AA доступностью
   - Интеграция с React Hook Form
   - Отображение "Уточняется администратором" для стоимости
4. Интегрирован в CheckoutForm (заменён DeliveryOptionsPlaceholder)
5. Созданы MSW handlers для мокирования API в тестах
6. Написаны тесты: 5 для deliveryService + 22 для DeliveryOptions

**Особенности реализации:**

- URL эндпоинта `/delivery/methods/` (baseURL уже содержит `/api/v1`)
- MSW handlers добавлены в глобальный handlers.ts
- Иконки в моках используют текстовые идентификаторы ('truck', 'store', 'package')

**Все 27 тестов проходят успешно.**

### File List

**Созданные файлы:**

- `frontend/src/types/delivery.ts`
- `frontend/src/services/deliveryService.ts`
- `frontend/src/components/checkout/DeliveryOptions.tsx`
- `frontend/src/__mocks__/handlers/deliveryHandlers.ts`
- `frontend/src/services/__tests__/deliveryService.test.ts`
- `frontend/src/components/checkout/__tests__/DeliveryOptions.test.tsx`

**Модифицированные файлы:**

- `frontend/src/components/checkout/CheckoutForm.tsx` (импорт и использование DeliveryOptions)
- `frontend/src/__mocks__/api/handlers.ts` (добавлены deliveryHandlers)

## QA Results

### Review Date: 2025-12-14

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Отлично.** Реализация DeliveryOptions компонента выполнена качественно и полностью соответствует требованиям истории. Код чистый, хорошо документирован, с полной WCAG 2.1 AA accessibility поддержкой.

**Сильные стороны:**

- Чёткая структура компонента с разделением на loading/error/success состояния
- Полная accessibility: `role="radiogroup"`, `role="status"`, `role="alert"`, `aria-label`, `aria-describedby`
- Корректная интеграция с React Hook Form через `register('deliveryMethod')`
- Информативное сообщение для пользователя о расчёте стоимости
- Comprehensive тесты: 27 тестов (5 service + 22 component)
- MSW handlers для мокирования API в тестах

### Refactoring Performed

Рефакторинг не требуется - код уже соответствует стандартам проекта.

### Compliance Check

- Coding Standards: ✓ Соответствует `docs/architecture/coding-standards.md`
- Project Structure: ✓ Файлы размещены корректно в `src/services/`, `src/types/`, `src/components/checkout/`
- Testing Strategy: ✓ 27 тестов с использованием Vitest + MSW + React Testing Library
- All ACs Met: ✓ Все 7 критериев приёмки выполнены

### Requirements Traceability

| AC# | Описание                                         | Тесты                                                                         | Статус |
| --- | ------------------------------------------------ | ----------------------------------------------------------------------------- | ------ |
| 1   | deliveryService получает список методов          | `deliveryService.test.ts` (5 тестов)                                          | ✅     |
| 2   | Radio group с role="radiogroup" и aria-label     | `рендерит radiogroup с правильными aria атрибутами`                           | ✅     |
| 3   | Отображение icon, name, description              | `отображает иконки`, `отображает описания`                                    | ✅     |
| 4   | Стоимость "Уточняется администратором"           | `показывает "Уточняется администратором"`, `НЕ показывает числовую стоимость` | ✅     |
| 5   | Интеграция с React Hook Form                     | `DeliveryOptionsWrapper` в тестах с `useForm`                                 | ✅     |
| 6   | Loading и error состояния                        | Loading State (3 теста), Error State (3 теста)                                | ✅     |
| 7   | Unit-тесты для deliveryService и DeliveryOptions | 5 + 22 = 27 тестов                                                            | ✅     |

### Improvements Checklist

- [x] Все AC выполнены
- [x] Тесты покрывают все основные сценарии
- [x] Accessibility полностью реализована
- [x] Интеграция в CheckoutForm выполнена
- [ ] _Опционально_: Добавить retry кнопку при ошибке загрузки
- [ ] _Опционально_: Добавить сообщение для пустого списка методов доставки

### Security Review

✅ **Без замечаний.**

- Компонент только читает данные, нет записи
- Нет обработки чувствительных данных
- API endpoint публичный (AllowAny) - корректно для списка способов доставки

### Performance Considerations

✅ **Без замечаний.**

- Один GET запрос при монтировании компонента
- useEffect с пустым dependency array предотвращает повторные запросы
- Skeleton UI обеспечивает хороший UX при загрузке

### Files Modified During Review

Нет - код не требовал изменений.

### Gate Status

**Gate: PASS** → `docs/qa/gates/15.3b-delivery-frontend-component.yml`

### Recommended Status

✅ **Ready for Done** - все критерии приёмки выполнены, 27 тестов проходят
