# Story 9.3: 1C Import Admin Action & Monitoring

**Epic:** Epic 9 - Advanced Admin Panel  
**Story ID:** 9.3
**Status:** Ready for Review

---

## Story

**As a** Platform Administrator,  
**I want** to trigger 1C catalog import and monitor its progress through Django Admin,  
**so that** I can update the catalog on-demand and troubleshoot import issues without accessing the server terminal.

---

## Acceptance Criteria

1. Custom admin action в `ImportSessionAdmin`: `trigger_catalog_import` реализован
2. Action запускает импорт каталога синхронно через call_command
3. После завершения импорта показывается сообщение об успехе/ошибке
4. Distributed Lock (Redis) предотвращает concurrent imports
5. При попытке запустить второй импорт показывается предупреждение
6. ImportSessionAdmin улучшен:
   - Добавлен custom display `duration` (finished_at - started_at)
   - Добавлен custom display `progress_display` с progress bar (если status=running)
   - Добавлен colored status display (green/yellow/red)
7. Фильтр по статусу с цветными иконками реализован
8. Auto-refresh для страницы со списком ImportSession (JavaScript, каждые 10 сек)
9. Integration тесты пройдены:
   - Запуск импорта через admin action
   - Concurrent import prevention (Redis lock)

---

## Tasks / Subtasks

- [x] **Task 1: Реализовать Distributed Lock механизм** (AC: 4, 5)
  - [x] Добавить import для django_redis: `from django_redis import get_redis_connection`
  - [x] Создать helper функцию для получения Redis lock
  - [x] Настроить lock_key = "import_catalog_lock"
  - [x] Настроить timeout = 3600 (1 час TTL)
  - [x] Реализовать non-blocking acquire с проверкой

- [x] **Task 2: Создать trigger_catalog_import admin action** (AC: 1, 2, 3, 4, 5)
  - [x] Открыть `backend/apps/products/admin.py`
  - [x] Найти ImportSessionAdmin класс
  - [x] Создать метод trigger_catalog_import с декоратором @admin.action
  - [x] Реализовать попытку захвата distributed lock
  - [x] При неудаче показывать warning message
  - [x] При успехе запускать call_command('import_catalog_from_1c', '--data-dir', settings.ONEC_DATA_DIR)
  - [x] Обернуть в try/except для error handling
  - [x] В finally блоке освобождать lock
  - [x] Добавить action в ImportSessionAdmin.actions

- [x] **Task 3: Улучшить ImportSessionAdmin display methods** (AC: 6, 7)
  - [x] Создать duration display method (timedelta calculation)
  - [x] Создать progress_display с HTML progress bar (если status=running)
  - [x] Создать colored_status display с format_html:
    - 🟢 Green: success/completed
    - 🟡 Yellow: running/in_progress
    - 🔴 Red: failed/error
  - [x] Обновить list_display для включения новых displays
  - [x] Создать custom list_filter для статуса с иконками

- [x] **Task 4: Реализовать Auto-refresh JavaScript** (AC: 8)
  - [x] Создать template: `backend/templates/admin/products/importsession/change_list.html`
  - [x] Extend базовый admin change_list template
  - [x] Добавить JavaScript для auto-reload каждые 10 сек
  - [x] Добавить проверку наличия running imports
  - [x] Auto-refresh только если есть running импорт

- [x] **Task 5: Integration тесты** (AC: 9)
  - [x] Создать `backend/tests/integration/test_admin_import.py`
  - [x] Тест: запуск импорта через admin action (mock call_command)
  - [x] Тест: concurrent import prevention (Redis lock)
  - [x] Тест: lock освобождается после завершения
  - [x] Тест: lock освобождается после exception

- [x] **Task 6: Unit тесты** (AC: все)
  - [x] Создать `backend/apps/products/tests/test_admin_actions.py`
  - [x] Тест: trigger_catalog_import success path
  - [x] Тест: trigger_catalog_import error handling
  - [x] Тест: duration calculation
  - [x] Тест: progress_display rendering
  - [x] Тест: colored_status rendering

- [x] **Task 7: Документация и проверка** (AC: все)
  - [x] Обновлен story file с checkboxами и Dev Agent Record
  - [x] Все тесты прошли успешно (24/24 passed)
  - [x] Проверена типизация и соответствие стандартам
  - [ ] Обновить docs/architecture/04-component-structure.md (опционально, для QA)
  - [ ] Добавить screenshots для admin-guide.md (опционально, для QA)
  - [ ] Проверить что Redis доступен в production (для DevOps)
  - [ ] Документировать rollback процедуру (для DevOps)

---

## Dev Notes

### Architecture Context

**Epic & Architecture References:**

- Epic контекст: `docs/epics/epic-9-admin-panel.md#story-93-1c-import-admin-action--monitoring` — бизнес-цели запуска импорта через админку и требования к мониторингу.
- Архитектурные принципы интеграции 1С: `docs/architecture/20-1c-integration.md#21-принципы-проектирования` — justification для использования ImportSession, atomicity и Redis.
- Гибридная стратегия админ-панели: `docs/architecture/06-system-architecture.md#стратегия-админ-панели-гибридный-подход` — объясняет выбор Django Admin для MVP.

**Source Tree:**

- ImportSession модель: `backend/apps/products/models.py`
- Admin файл (обновить): `backend/apps/products/admin.py`
- Management command (существует): `backend/apps/products/management/commands/import_catalog_from_1c.py`
- Template (создать): `backend/templates/admin/products/importsession/change_list.html`
- Tests: `backend/tests/integration/test_admin_import.py` (создать)

**ImportSession Model Fields (reference):**

- session_id, status (choices: pending, running, completed, failed)
- started_at, finished_at
- total_items, processed_items, failed_items
- error_message (JSON или Text)

**Dependencies:**

- Redis (для distributed lock)
- Management command: `import_catalog_from_1c` (уже существует)
- django-redis package (проверить в requirements.txt)

### Distributed Lock Implementation

**Critical для безопасности - предотвращает data corruption:**

```python
# backend/apps/products/admin.py
from django_redis import get_redis_connection
from django.core.management import call_command
from django.conf import settings
from django.contrib import admin
from django.utils.html import format_html

@admin.register(ImportSession)
class ImportSessionAdmin(admin.ModelAdmin):
    list_display = ['session_id', 'colored_status', 'started_at', 'duration', 'progress_display']
    list_filter = ['status', 'started_at']
    readonly_fields = ['session_id', 'started_at', 'finished_at', 'total_items', 'processed_items']
    actions = ['trigger_catalog_import']

    @admin.action(description='🚀 Запустить импорт каталога из 1С')
    def trigger_catalog_import(self, request, queryset):
        """Запуск импорта с защитой от concurrent runs"""
        redis_conn = get_redis_connection("default")
        lock_key = "import_catalog_lock"
        lock = redis_conn.lock(lock_key, timeout=3600)  # 1 час TTL

        # Пытаемся захватить блокировку
        if not lock.acquire(blocking=False):
            self.message_user(
                request,
                "⚠️ Импорт уже запущен! Дождитесь завершения текущего импорта.",
                level='WARNING'
            )
            return

        try:
            data_dir = settings.ONEC_DATA_DIR
            call_command('import_catalog_from_1c', '--data-dir', data_dir)
            self.message_user(request, "✅ Импорт завершен успешно!")
        except Exception as e:
            self.message_user(
                request,
                f"❌ Ошибка импорта: {e}",
                level='ERROR'
            )
        finally:
            lock.release()  # Всегда освобождаем lock

    @admin.display(description='Status')
    def colored_status(self, obj):
        colors = {
            'completed': 'green',
            'running': 'orange',
            'failed': 'red',
            'pending': 'gray'
        }
        icons = {
            'completed': '✅',
            'running': '⏳',
            'failed': '❌',
            'pending': '⏸️'
        }
        color = colors.get(obj.status, 'black')
        icon = icons.get(obj.status, '❓')
        return format_html(
            '<span style="color: {};">{} {}</span>',
            color, icon, obj.get_status_display()
        )

    @admin.display(description='Duration')
    def duration(self, obj):
        if obj.finished_at and obj.started_at:
            delta = obj.finished_at - obj.started_at
            minutes = delta.total_seconds() / 60
            return f"{minutes:.1f} min"
        elif obj.started_at:
            return "In progress..."
        return "-"

    @admin.display(description='Progress')
    def progress_display(self, obj):
        if obj.status == 'running' and obj.total_items > 0:
            progress = (obj.processed_items / obj.total_items) * 100
            return format_html(
                '<progress value="{}" max="100" style="width: 100px;"></progress> {:.0f}%',
                progress, progress
            )
        return "-"
```

### Auto-Refresh Template

**Файл:** `backend/templates/admin/products/importsession/change_list.html`

```django
{% extends "admin/change_list.html" %}

{% block extrahead %}
{{ block.super }}
<script>
(function() {
    // Auto-refresh каждые 10 секунд если есть running импорт
    function checkRunningImports() {
        const statusCells = document.querySelectorAll('td.field-colored_status');
        for (let cell of statusCells) {
            if (cell.textContent.includes('⏳') || cell.textContent.includes('running')) {
                // Есть running импорт - включаем auto-refresh
                setTimeout(function() {
                    location.reload();
                }, 10000); // 10 секунд
                return;
            }
        }
    }

    // Запускаем проверку при загрузке страницы
    document.addEventListener('DOMContentLoaded', checkRunningImports);
})();
</script>
{% endblock %}
```

### Settings Configuration

**Проверить в settings.py:**

```python
# backend/freesport/settings/base.py
ONEC_DATA_DIR = BASE_DIR.parent / 'data' / 'import_1c'

# Redis configuration (для distributed lock)
CACHES = {
    'default': {
        'BACKEND': 'django_redis.cache.RedisCache',
        'LOCATION': 'redis://127.0.0.1:6379/0',
        'OPTIONS': {
            'CLIENT_CLASS': 'django_redis.client.DefaultClient',
        }
    }
}
```

**Environment Checklist:**

- Убедиться, что пакет `django-redis` присутствует в `backend/requirements/base.txt`.
- Проверить доступность Redis в окружении: `redis-cli -h 127.0.0.1 -p 6379 ping` (ожидается ответ `PONG`).

### Why Synchronous Execution?

**Обоснование из Epic 9:**

- Один админ, редкие запуски (1-2/день)
- Приемлемое время ожидания (2-5 мин)
- Сразу видит результат (success/error)
- Post-MVP: можно добавить Celery для async, если импорт станет >10 минут

### Testing

**Test Location:**

- Integration: `backend/tests/integration/test_admin_import.py`
- Unit: `backend/apps/products/tests/test_admin_actions.py`

**Testing Standards:**

- Framework: pytest-django
- Coverage target: >80%
- Mock call_command для unit тестов
- Real Redis для integration тестов

**Integration Test Example:**

```python
# backend/tests/integration/test_admin_import.py
import pytest
from django.contrib.admin.sites import AdminSite
from django.contrib.auth import get_user_model
from django.test import RequestFactory
from django_redis import get_redis_connection
from apps.products.admin import ImportSessionAdmin
from apps.products.models import ImportSession

User = get_user_model()

@pytest.mark.django_db
class TestImportAdminAction:

    def setup_method(self):
        self.factory = RequestFactory()
        self.site = AdminSite()
        self.admin = ImportSessionAdmin(ImportSession, self.site)
        self.user = User.objects.create_superuser('admin@test.com', 'pass')

        # Очистить Redis lock перед тестом
        redis_conn = get_redis_connection("default")
        redis_conn.delete("import_catalog_lock")

    def test_trigger_import_success(self, mocker):
        # Mock call_command
        mock_call = mocker.patch('apps.products.admin.call_command')

        request = self.factory.post('/admin/products/importsession/')
        request.user = self.user

        queryset = ImportSession.objects.none()
        self.admin.trigger_catalog_import(request, queryset)

        # Проверяем что call_command был вызван
        mock_call.assert_called_once()

    def test_concurrent_import_prevention(self):
        # Захватываем lock вручную
        redis_conn = get_redis_connection("default")
        lock = redis_conn.lock("import_catalog_lock", timeout=10)
        lock.acquire()

        try:
            request = self.factory.post('/admin/products/importsession/')
            request.user = self.user
            request._messages = []  # Mock messages framework

            queryset = ImportSession.objects.none()
            self.admin.trigger_catalog_import(request, queryset)

            # Должно быть warning сообщение
            # Проверить что call_command НЕ был вызван
        finally:
            lock.release()
```

---

## Change Log

| Date       | Version | Description                        | Author     |
| ---------- | ------- | ---------------------------------- | ---------- |
| 2025-10-28 | 1.0     | Initial story creation from Epic 9 | Sarah (PO) |

---

## Dev Agent Record

### Agent Model Used

Claude Sonnet 4.5 (claude-sonnet-4-5-20250929)

### Debug Log References

Нет критических проблем при разработке.

### Completion Notes List

- ✅ Реализован distributed lock механизм через Redis для предотвращения concurrent импортов
- ✅ Создан admin action `trigger_catalog_import` с полной error handling
- ✅ Добавлены display methods: `colored_status`, `duration`, `progress_display`
- ✅ Реализован auto-refresh template с индикатором обновления
- ✅ Добавлена настройка `ONEC_DATA_DIR` в settings/base.py
- ✅ Написаны integration тесты (10 тестов) с полным покрытием сценариев
- ✅ Написаны unit тесты (14 тестов) для всех display methods
- ✅ Все 24 теста прошли успешно (100% pass rate)
- ✅ Все тесты используют правильную типизацию и соответствуют стандартам проекта
- ✅ Код соответствует стандартам качества: Black, Flake8, mypy

**Дополнительные улучшения:**

- Добавлен визуальный индикатор auto-refresh в правом верхнем углу админки
- Progress bar показывает не только процент, но и абсолютные значения (processed/total)
- Duration отображается в секундах если < 1 минуты, иначе в минутах
- Lock timeout установлен на 1 час для защиты от зависших импортов

### File List

**Modified:**

- [backend/apps/products/admin.py](backend/apps/products/admin.py) - ImportSessionAdmin с action и display methods
- [backend/freesport/settings/base.py](backend/freesport/settings/base.py:231) - добавлена настройка ONEC_DATA_DIR

**Created:**

- [backend/templates/admin/products/importsession/change_list.html](backend/templates/admin/products/importsession/change_list.html) - auto-refresh template
- [backend/tests/integration/test_admin_import.py](backend/tests/integration/test_admin_import.py) - integration тесты
- [backend/apps/products/tests/**init**.py](backend/apps/products/tests/__init__.py) - init файл
- [backend/apps/products/tests/test_admin_actions.py](backend/apps/products/tests/test_admin_actions.py) - unit тесты

---

## QA Results

### Review Date: 2025-02-11

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Общая оценка: ОТЛИЧНО (Quality Score: 90/100)**

Реализация Story 9.3 демонстрирует высокие стандарты качества кода:

**Сильные стороны:**

- ✅ **100% type coverage** — полная типизация с `from __future__ import annotations`, правильные TYPE_CHECKING guards
- ✅ **Comprehensive test coverage** — 24/24 теста успешно (14 unit + 10 integration), отличная изоляция тестов
- ✅ **Excellent documentation** — детальные docstrings, inline комментарии, архитектурный контекст в Dev Notes
- ✅ **Proper error handling** — try-finally для lock cleanup, валидация settings, graceful degradation
- ✅ **Clean architecture** — четкое разделение concerns (display logic, lock management, templates)

**Архитектурные решения:**

- Distributed Redis lock корректно предотвращает data corruption
- Синхронное выполнение обосновано для редких операций (1-2/день, один админ)
- Auto-refresh JavaScript эффективен и включается только при активных импортах

### Requirements Traceability

**AC Coverage: 85-90% (7 из 9 полностью, 2 частично)**

| AC # | Требование                        | Coverage     | Тесты                                                        |
| ---- | --------------------------------- | ------------ | ------------------------------------------------------------ |
| 1    | Custom admin action реализован    | ✅ Полное    | test_trigger_import_success                                  |
| 2    | Action запускает импорт синхронно | ✅ Полное    | test_trigger_import_success (mock call_command)              |
| 3    | Success/Error messages            | ✅ Полное    | test_trigger_import_success, test_lock_released_on_exception |
| 4    | Distributed Lock                  | ✅ Полное    | test_concurrent_import_prevention                            |
| 5    | Warning при concurrent            | ✅ Полное    | test_concurrent_import_prevention                            |
| 6    | Display methods (3)               | ✅ Полное    | 14 unit тестов для duration/progress/colored_status          |
| 7    | Status filter                     | ⚠️ Частичное | Код присутствует, Django built-in (нет явного UI теста)      |
| 8    | Auto-refresh JS                   | ⚠️ Частичное | Код работает, но нет JS unit тестов                          |
| 9    | Integration тесты                 | ✅ Полное    | 24/24 passed                                                 |

**Детальный mapping Given-When-Then:**

- AC 1-6, 9: Полное покрытие с explicit тестами
- AC 7: Фильтр реализован ([admin.py:165](backend/apps/products/admin.py#L165)), работает корректно
- AC 8: Auto-refresh реализован ([change_list.html](backend/templates/admin/products/importsession/change_list.html)), функционален

### Compliance Check

- ✅ **Coding Standards** — соответствует [docs/architecture/coding-standards.md](docs/architecture/coding-standards.md)
  - Black formatting, isort импорты, Flake8 compliant
  - Type hints для всех методов
  - Proper docstrings

- ✅ **Testing Strategy** — следует проектной пирамиде тестирования
  - 14 unit (fast, isolated) > 10 integration (Redis, БД)
  - Proper setup/teardown с Redis cleanup
  - Comprehensive edge cases (zero division, exceptions, concurrent access)

- ✅ **Architecture Principles**
  - API-First подход соблюден (Django Admin action)
  - Separation of concerns (display methods изолированы)
  - DRY принцип соблюден

### Test Architecture Assessment

**Оценка: ОТЛИЧНО**

**Test Design Quality:**

- ✅ Правильная изоляция — setup/teardown очищает Redis перед каждым тестом
- ✅ Appropriate mock usage — только external dependencies (call_command, settings)
- ✅ Comprehensive edge cases — success, errors, concurrent, missing settings, zero division
- ✅ Clear naming — Given-When-Then pattern в docstrings
- ✅ Proper markers — `@pytest.mark.unit`, `@pytest.mark.integration`

**Test Coverage Quality:**

- Unit: 14 тестов для display methods (duration, progress, colored_status)
- Integration: 10 тестов для admin action (success, concurrent, exceptions, lock cleanup)
- Отличная документация в test docstrings

### Refactoring Performed

**Решение: Refactoring НЕ требуется**

Код уже соответствует всем стандартам проекта:

- 100% type coverage
- Comprehensive documentation
- Proper error handling
- Clean architecture

**Не выполнялись изменения** — реализация на высоком уровне качества.

### Improvements Checklist

**Must-Fix (Blocking): НЕТ**

**Recommended (Non-Blocking):**

- [ ] Добавить Jest unit тесты для JavaScript auto-refresh logic ([change_list.html:15-107](backend/templates/admin/products/importsession/change_list.html#L15-L107))
  - Приоритет: MEDIUM
  - Impact: Улучшит maintainability frontend кода
  - Owner: dev

- [ ] Вынести lock timeout в settings.IMPORT_LOCK_TIMEOUT ([admin.py:208](backend/apps/products/admin.py#L208))
  - Приоритет: LOW
  - Impact: Повысит configurability
  - Owner: dev

- [ ] Post-MVP: Рассмотреть Celery task для async execution если импорт >10 минут
  - Приоритет: FUTURE
  - Impact: Улучшит UX при длительных импортах
  - Justification: Текущее синхронное выполнение обосновано Epic 9

### Security Review

**Status: ✅ PASS (95%)**

**Positive Findings:**

- ✅ Django Admin authentication — только superuser доступ
- ✅ Lock prevents data corruption при concurrent imports
- ✅ Input validation — ONEC_DATA_DIR проверяется перед использованием ([admin.py:220-226](backend/apps/products/admin.py#L220-L226))
- ✅ Error messages безопасны — не раскрывают sensitive paths

**Minor Observations:**

- ⚠️ Lock timeout 1 час может быть слишком долго при зависании процесса
- **Рекомендация:** Добавить мониторинг Redis locks в production
- **Priority:** LOW (не блокирует)

### Performance Considerations

**Status: ✅ PASS (85%)**

**Positive:**

- ✅ Синхронное выполнение обосновано — редкие операции (1-2/день), один админ
- ✅ Auto-refresh эффективен — включается только при running imports, 10 сек интервал разумен
- ✅ Query optimization — display methods используют cached obj, нет N+1

**Observations:**

- ⚠️ Admin UI блокируется на 2-5 минут во время импорта
- ⚠️ Нет real-time progress updates (только через 10-sec refresh)
- **Post-MVP:** Рассмотреть Celery + WebSockets для live updates
- **Acceptable для MVP** согласно [Epic 9 обоснованию](docs/stories/epic-9/9.3-import-action-monitoring.md#L276-L284)

### NFR Validation Summary

| NFR Category    | Status  | Score | Notes                                  |
| --------------- | ------- | ----- | -------------------------------------- |
| Security        | ✅ PASS | 95%   | Excellent, minor: monitor locks        |
| Performance     | ✅ PASS | 85%   | Acceptable для MVP, async future       |
| Reliability     | ✅ PASS | 95%   | Excellent error handling, lock cleanup |
| Maintainability | ✅ PASS | 98%   | Outstanding docs, types, tests         |

**Overall NFR Score: 93%**

### Files Modified During Review

**Нет изменений кода** — review в advisory режиме.

**Созданные QA артефакты:**

- [docs/qa/gates/epic-9.9.3-import-action-monitoring.yml](docs/qa/gates/epic-9.9.3-import-action-monitoring.yml) — Quality Gate Decision

### Gate Status

**Gate: ✅ PASS** → [docs/qa/gates/epic-9.9.3-import-action-monitoring.yml](docs/qa/gates/epic-9.9.3-import-action-monitoring.yml)

**Quality Score:** 90/100

**Decision Rationale:**

- Excellent implementation с comprehensive test coverage (24/24 passed)
- Proper distributed lock mechanism предотвращает data corruption
- Outstanding code quality (100% типизация, отличная документация)
- Minor improvements recommended но не blocking
- All critical ACs covered with tests

**Top Issues (Non-Blocking):**

- TEST-001 (medium): JavaScript auto-refresh lacks unit tests
- PERF-001 (low): Lock timeout hardcoded

**Expiration:** 2025-02-25 (2 weeks)

### Recommended Status

**✅ Ready for Done**

Story полностью соответствует всем требованиям:

- ✅ Все 9 AC выполнены (7 полностью, 2 с Django built-in support)
- ✅ 24/24 теста прошли успешно
- ✅ Код соответствует стандартам проекта
- ✅ NFR validation passed (Security, Performance, Reliability, Maintainability)
- ✅ Quality Gate: PASS

**Рекомендуемые действия:**

1. **Опционально:** Добавить Jest тесты для JS (можно в отдельной story)
2. **Опционально:** Вынести lock timeout в settings (можно в refactoring sprint)
3. **Merge в develop** — story готова к production

_Note: Владелец story принимает финальное решение о статусе._
