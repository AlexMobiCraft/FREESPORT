# Story 26.3: Cart Summary & Checkout CTA

## Status

Ready for Review

## Story

**Как** пользователь,  
**Я хочу** видеть итоговую сумму заказа и перейти к оформлению,  
**Чтобы** завершить покупку.

## Dependencies

> [!NOTE]
> **Зависимости:**
>
> - [Story 26.1: CartPageLayout](26.1.cart-page-layout.md#relevant-source-tree): Cart Page Layout
> - [Story 26.2: CartItemCard](26.2.cart-item-card.md#cartitemcard-layout): Cart Item Card
> - [Story 26.4: PromoCodeIntegration](26.4.promo-code-integration.md#cartstore-promo-state-example): Promo Code (плацехолдер в этой стори)

## Acceptance Criteria

1. Отображается заголовок "Итоги заказа"
2. Отображается "Товары на сумму" с общей стоимостью товаров (`totalPrice` из cartStore)
3. Отображается "Скидка по промокоду" (если применён промокод) — **placeholder для Story 26.4**
4. Отображается "Итого к оплате" с финальной суммой
5. Поле ввода промокода с кнопкой "Применить" (placeholder для Story 26.4)
6. Кнопка "Оформить заказ" (primary, height: 56px)
7. Кнопка неактивна если корзина пуста (`items.length === 0`)
8. Sticky позиционирование на desktop (top: 96px)
9. На mobile блок не sticky, располагается под списком товаров
10. Hydration: корректная работа с Zustand persist (паттерн `mounted`)
11. Accessibility: `aria-live="polite"` для динамических сумм

## Tasks / Subtasks

- [x] Создать компонент CartSummary (AC: 1-7, 10-11)
  - [x] Создать `frontend/src/components/cart/CartSummary.tsx`
  - [x] Добавить директиву `'use client'`
  - [x] Использовать cartStore напрямую: `items`, `totalPrice`, `totalItems`
  - [x] Обработка hydration: использовать паттерн `mounted` state
  - [x] Layout: white card с shadow
  - [x] Заголовок "Итоги заказа" (title-m)
  - [x] Добавить `aria-live="polite"` для блока с суммами
  - [x] Добавить экспорт в `frontend/src/components/cart/index.ts` (barrel export)

- [x] Реализовать Price Summary (AC: 2-4)
  - [x] Строка "Товары на сумму" + `totalPrice` (из cartStore)
  - [x] Строка "Скидка по промокоду" — **placeholder, скрыта до Story 26.4**
  - [x] Разделитель (divider)
  - [x] Строка "Итого к оплате" (headline-m, bold) — равна `totalPrice` (без скидки до 26.4)
  - [x] Использовать `formatPrice` из `@/utils/pricing`

- [x] Добавить Promo Code Placeholder (AC: 5)
  - [x] Создать `frontend/src/components/cart/PromoCodeInput.tsx`
  - [x] Добавить директиву `'use client'`
  - [x] Input + Button для промокода (UI готов)
  - [x] Логика — **stub/TODO** до Story 26.4
  - [x] Добавить экспорт в `frontend/src/components/cart/index.ts`

- [x] Реализовать Checkout CTA (AC: 6-7)
  - [x] Кнопка "Оформить заказ" primary large (height: 56px)
  - [x] ⚠️ **ВАЖНО**: `<Link>` не поддерживает `disabled` — использовать условный рендеринг:
    - Если `items.length > 0`: `<Link href="/checkout">`
    - Если `items.length === 0`: `<button disabled>`
  - [x] `aria-disabled={isEmpty}` для accessibility

- [x] Реализовать Sticky Positioning (AC: 8-9)
  - [x] Desktop: `lg:sticky lg:top-24` (96px = 6rem)
  - [x] Mobile: обычный flow (без sticky)

- [x] Обновить persist в cartStore для promo state (MEDIUM)
  - [x] Добавлено `promoCode`, `discountType`, `discountValue` в `partialize` — реализовано в cartStore.ts
  - [x] Promo state сохраняется в localStorage через Zustand persist middleware

- [x] Написать unit тесты
  - [x] Тест отображения итогов
  - [x] Тест disabled state кнопки при пустой корзине
  - [x] Тест навигации на /checkout
  - [x] Тест hydration (нет mismatch warnings)
  - [x] Тест promo code placeholder

## Dev Notes

### Relevant Source Tree

```
frontend/src/
├── components/
│   └── cart/
│       ├── CartSummary.tsx             (NEW)
│       ├── PromoCodeInput.tsx          (NEW - placeholder/stub)
│       ├── index.ts                    (UPDATE - barrel export)
│       └── __tests__/
│           └── CartSummary.test.tsx    (NEW)
├── stores/
│   └── cartStore.ts                    (EXISTS - items, totalPrice, totalItems)
└── utils/
    └── pricing.ts                      (EXISTS - formatPrice)
```

> [!IMPORTANT]
> **В cartStore НЕТ `promoDiscount`!**
> Скидка по промокоду будет добавлена в Story 26.4.
> В этой истории строка "Скидка" скрыта (`promoDiscount = 0`).

> [!WARNING]
> **Архитектурное замечание: пересчёт скидки при изменении корзины**
>
> При процентном типе скидки `promoDiscount` фиксируется в момент применения.
> Если `totalPrice` изменится (добавление/удаление товаров), скидка не пересчитается.
>
> **Решение для Story 26.4:**
>
> - Хранить `discountType` + `discountValue` вместо вычисленного `promoDiscount`
> - Вычислять скидку динамически в getter/selector
> - Пересчитывать `promoDiscount` при каждом изменении `items`

### CartSummary Layout

```tsx
// CartSummary.tsx - используйте CSS переменные из globals.css
"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import { useCartStore } from "@/stores/cartStore";
import { formatPrice } from "@/utils/pricing";
import { cn } from "@/utils/cn";
import PromoCodeInput from "./PromoCodeInput";

export const CartSummary = () => {
  // Hydration паттерн
  const [mounted, setMounted] = useState(false);
  const { items, totalPrice } = useCartStore();

  useEffect(() => {
    setMounted(true);
  }, []);

  const isEmpty = !mounted || items.length === 0;

  // Placeholder для Story 26.4
  const promoDiscount = 0;
  const finalTotal = totalPrice - promoDiscount;

  return (
    <div
      className="bg-[var(--bg-panel)] rounded-[var(--radius-lg)] p-6
                 shadow-[0_18px_40px_rgba(0,55,166,0.12)] lg:sticky lg:top-24"
      data-testid="cart-summary"
    >
      <h2 className="text-title-m font-semibold text-[var(--color-text-primary)] mb-6">
        Итоги заказа
      </h2>

      {/* Price Breakdown - с aria-live для динамических обновлений */}
      <div className="space-y-3 mb-6" aria-live="polite">
        <div className="flex justify-between text-body-m">
          <span className="text-[var(--color-text-secondary)]">
            Товары на сумму
          </span>
          <span
            className="text-[var(--color-text-primary)] font-medium"
            data-testid="subtotal-amount"
          >
            {mounted ? formatPrice(totalPrice) : "—"}
          </span>
        </div>

        {/* Скидка - скрыта до Story 26.4 */}
        {promoDiscount > 0 && (
          <div className="flex justify-between text-body-m">
            <span className="text-[var(--color-accent-success)]">
              Скидка по промокоду
            </span>
            <span
              className="text-[var(--color-accent-success)] font-medium"
              data-testid="promo-discount-amount"
            >
              -{formatPrice(promoDiscount)}
            </span>
          </div>
        )}
      </div>

      {/* Divider */}
      <div className="border-t border-[var(--color-neutral-300)] my-4" />

      {/* Total */}
      <div className="flex justify-between items-center mb-6">
        <span className="text-title-m font-semibold text-[var(--color-text-primary)]">
          Итого к оплате
        </span>
        <span
          className="text-headline-m font-bold text-[var(--color-text-primary)]"
          data-testid="total-amount"
        >
          {mounted ? formatPrice(finalTotal) : "—"}
        </span>
      </div>

      {/* Promo Code (placeholder для Story 26.4) */}
      <PromoCodeInput />

      {/* Checkout Button - условный рендеринг Link/button */}
      {isEmpty ? (
        <button
          disabled
          className={cn(
            "w-full h-14 flex items-center justify-center",
            "bg-[var(--color-primary)] text-[var(--color-text-inverse)]",
            "rounded-[var(--radius-md)] text-body-l font-medium",
            "opacity-50 cursor-not-allowed",
          )}
          data-testid="checkout-button"
          aria-disabled="true"
        >
          🛒 Оформить заказ
        </button>
      ) : (
        <Link
          href="/checkout"
          className={cn(
            "w-full h-14 flex items-center justify-center",
            "bg-[var(--color-primary)] text-[var(--color-text-inverse)]",
            "rounded-[var(--radius-md)] text-body-l font-medium",
            "hover:bg-[var(--color-primary-hover)] transition-colors",
          )}
          data-testid="checkout-button"
        >
          🛒 Оформить заказ
        </Link>
      )}
    </div>
  );
};
```

### PromoCodeInput Placeholder

```tsx
// PromoCodeInput.tsx - placeholder для Story 26.4
// используйте CSS переменные из globals.css
const PromoCodeInput = () => {
  const [code, setCode] = useState("");
  const [isLoading, setIsLoading] = useState(false);

  const handleApply = async () => {
    // Placeholder - будет реализовано в Story 26.4
    setIsLoading(true);
    // TODO: Implement promo code validation
    setTimeout(() => setIsLoading(false), 500);
  };

  return (
    <div className="flex gap-2 mb-4" data-testid="promo-code-section">
      <input
        type="text"
        value={code}
        onChange={(e) => setCode(e.target.value)}
        placeholder="Введите промокод"
        className="flex-1 h-10 px-3 border border-[var(--color-neutral-400)] rounded-[var(--radius-sm)]
                   text-body-m focus:outline-none focus:ring-[var(--focus-ring)] 
                   focus:border-[var(--color-primary)] bg-[var(--bg-panel)]"
        data-testid="promo-code-input"
      />
      <button
        onClick={handleApply}
        disabled={!code || isLoading}
        className="px-4 h-10 bg-[var(--color-primary-subtle)] text-[var(--color-primary)] 
                   rounded-[var(--radius-sm)] text-body-m font-medium 
                   hover:bg-[var(--color-primary)] hover:text-[var(--color-text-inverse)]
                   disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
        data-testid="apply-promo-button"
      >
        {isLoading ? "Проверка..." : "Применить"}
      </button>
    </div>
  );
};

export default PromoCodeInput;
```

### Data Test IDs

| Элемент            | data-testid             | Описание           |
| ------------------ | ----------------------- | ------------------ |
| Блок итогов        | `cart-summary`          | Контейнер блока    |
| Сумма товаров      | `subtotal-amount`       | Сумма до скидки    |
| Скидка промокода   | `promo-discount-amount` | Сумма скидки       |
| Итого              | `total-amount`          | Финальная сумма    |
| Поле промокода     | `promo-code-input`      | Input для кода     |
| Кнопка "Применить" | `apply-promo-button`    | Применить промокод |
| Кнопка "Оформить"  | `checkout-button`       | CTA оформления     |

### Design System References

```json
// Из design-system.json
{
  "components": {
    "ProductSummary": {
      "priceBlock": {
        "background": "#FFFFFF",
        "radius": 20,
        "shadow": "0 18px 40px rgba(0, 55, 166, 0.12)",
        "padding": 24
      },
      "ctaPrimary": {
        "background": "#0060FF",
        "hover": "#0047CC",
        "textColor": "#FFFFFF",
        "radius": 16,
        "height": 56,
        "font": "body-l"
      }
    }
  }
}
```

### Sticky Positioning

```css
/* Desktop: sticky */
@media (min-width: 1024px) {
  .cart-summary {
    position: sticky;
    top: 96px; /* Header height (60px) + padding (36px) */
  }
}

/* Mobile: normal flow */
@media (max-width: 1023px) {
  .cart-summary {
    position: relative;
    top: auto;
  }
}
```

### Price Formatting Utility

```typescript
// utils/formatPrice.ts
export const formatPrice = (price: number): string => {
  return new Intl.NumberFormat("ru-RU", {
    style: "currency",
    currency: "RUB",
    minimumFractionDigits: 0,
    maximumFractionDigits: 0,
  }).format(price);
};

// Пример: formatPrice(15732) → "15 732 ₽"
```

## Testing

**Framework:** Vitest + React Testing Library

**Тестовые сценарии:**

1. **Render with items:** Отображение всех строк итогов
2. **Empty cart:** Кнопка "Оформить заказ" disabled
3. **With promo:** Отображение строки скидки
4. **Price formatting:** Корректное форматирование сумм
5. **Checkout navigation:** Клик на кнопку → redirect to `/checkout`

### Mock Cart Data

```typescript
// Пример mock данных для тестирования CartSummary
const mockCartItems = [
  {
    id: 1,
    variant_id: 101,
    product: {
      id: 10,
      name: "Мяч футбольный Nike",
      slug: "myach-futbolniy-nike",
      image: "/images/products/ball.jpg",
    },
    variant: {
      sku: "BALL-001",
      color_name: "Белый",
      size_value: "5",
    },
    quantity: 2,
    unit_price: "2500",
    total_price: "5000",
    added_at: "2025-12-07T10:00:00Z",
  },
  {
    id: 2,
    variant_id: 102,
    product: {
      id: 11,
      name: "Бутсы Adidas Predator",
      slug: "butsy-adidas-predator",
      image: "/images/products/boots.jpg",
    },
    variant: {
      sku: "BOOTS-002",
      color_name: "Чёрный",
      size_value: "42",
    },
    quantity: 1,
    unit_price: "8990",
    total_price: "8990",
    added_at: "2025-12-07T10:05:00Z",
  },
];

// Mock cartStore state
const mockCartStore = {
  items: mockCartItems,
  totalItems: 3, // 2 + 1
  totalPrice: 13990, // 5000 + 8990
  isLoading: false,
  error: null,
};

// Mock для пустой корзины
const mockEmptyCartStore = {
  items: [],
  totalItems: 0,
  totalPrice: 0,
  isLoading: false,
  error: null,
};

// Использование в тестах
vi.mock("@/stores/cartStore", () => ({
  useCartStore: vi.fn(() => mockCartStore),
}));
```

## Definition of Done

- [x] Компонент рендерится корректно
- [x] Итоги подсчитываются правильно (используя cartStore.totalPrice)
- [x] Hydration работает без mismatch warnings
- [x] Sticky работает на desktop (lg:sticky lg:top-24)
- [x] Кнопка disabled при пустой корзине (условный рендеринг Link/button)
- [x] PromoCodeInput placeholder готов для Story 26.4
- [x] Barrel exports добавлены в index.ts
- [x] Unit тесты написаны (36 тестов, 100% pass rate)
- [x] Accessibility: aria-live="polite", семантическая разметка

## Dev Agent Record

### Agent Model Used

Claude Sonnet 4 (Cascade)

### Debug Log References

N/A - реализация прошла без критических проблем

### Completion Notes

- ✅ CartSummary.tsx создан с полной поддержкой hydration (mounted pattern)
- ✅ PromoCodeInput.tsx создан как placeholder для Story 26.4
- ✅ Условный рендеринг Link/button для корректной работы disabled state
- ✅ Sticky positioning через Tailwind классы lg:sticky lg:top-24
- ✅ Accessibility: aria-live="polite" для динамических сумм, aria-disabled для кнопки
- ✅ 36 unit тестов (19 для CartSummary, 17 для PromoCodeInput) - все проходят
- ✅ Регрессия: все 157 тестов корзины проходят
- ✅ Persist для promo state реализован (promoCode, discountType, discountValue в partialize)

### File List

| File                                                             | Action   | Description                     |
| ---------------------------------------------------------------- | -------- | ------------------------------- |
| `frontend/src/components/cart/CartSummary.tsx`                   | NEW      | Компонент итогов корзины        |
| `frontend/src/components/cart/PromoCodeInput.tsx`                | NEW      | Placeholder для ввода промокода |
| `frontend/src/components/cart/index.ts`                          | MODIFIED | Добавлены barrel exports        |
| `frontend/src/components/cart/__tests__/CartSummary.test.tsx`    | NEW      | Unit тесты CartSummary          |
| `frontend/src/components/cart/__tests__/PromoCodeInput.test.tsx` | NEW      | Unit тесты PromoCodeInput       |

## Change Log

| Date       | Version | Description                                                                                                                      | Author      |
| ---------- | ------- | -------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 2025-12-07 | 1.0     | Initial story creation                                                                                                           | Sarah (PO)  |
| 2025-12-07 | 1.1     | Добавлены: hydration паттерн, условный рендеринг Link/button, barrel exports, aria-live, исправлен пример кода                   | Bob (SM)    |
| 2025-12-07 | 1.2     | Исправлено: добавлены ссылки на секции в Dependencies, требование persist для promo state, архитектурное TODO о пересчёте скидки | Sarah (PO)  |
| 2025-12-07 | 1.3     | Добавлены mock cart data в секцию Testing                                                                                        | Sarah (PO)  |
| 2025-12-07 | 1.4     | **IMPLEMENTATION COMPLETE** - CartSummary, PromoCodeInput созданы, 36 тестов, все AC выполнены                                   | James (Dev) |
| 2025-12-07 | 1.5     | Подтверждение реализации persist для promo state, обновлены чекбоксы подзадач [N/A] → [x]                                        | James (Dev) |

## QA Results

### Review Date: 2025-12-07

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Overall: EXCELLENT** — Реализация соответствует высоким стандартам качества.

**Сильные стороны:**

- ✅ Отличная документация компонентов (JSDoc комментарии)
- ✅ Правильная реализация hydration паттерна через `mounted` state
- ✅ Условный рендеринг Link/button — корректное решение проблемы disabled Link
- ✅ Грамотное использование CSS переменных из design-system
- ✅ Полное покрытие тестами (19 тестов CartSummary + 17 тестов PromoCodeInput)
- ✅ Accessibility: `aria-live="polite"`, `aria-label`, `aria-disabled`
- ✅ Keyboard navigation (Enter для применения промокода)
- ✅ Правильная интеграция с cartStore (items, totalPrice)

**Архитектурные решения:**

- ✅ `getPromoDiscount()` в cartStore динамически пересчитывает скидку — предотвращает стейл данные при изменении корзины
- ✅ Persist middleware корректно сохраняет promo state (promoCode, discountType, discountValue)
- ✅ Barrel exports в index.ts обеспечивают чистые импорты

### Refactoring Performed

Рефакторинг не требуется — код высокого качества.

### Compliance Check

- Coding Standards: ✓ Соответствует стандартам проекта
- Project Structure: ✓ Файлы размещены по source tree из story
- Testing Strategy: ✓ Unit тесты с Vitest + RTL, proper mocking
- All ACs Met: ✓ Все 11 acceptance criteria выполнены

### Improvements Checklist

- [x] Hydration паттерн реализован корректно
- [x] Условный рендеринг Link/button для disabled state
- [x] Accessibility: aria-live, aria-label, aria-disabled
- [x] Sticky positioning: lg:sticky lg:top-24
- [x] PromoCodeInput placeholder готов для Story 26.4
- [x] Barrel exports добавлены

### Security Review

**Status: PASS** — Нет security concerns для UI компонента.

### Performance Considerations

**Status: PASS**

- Минимальный re-render благодаря правильной структуре компонентов
- Нет тяжёлых вычислений
- formatPrice использует встроенный Intl.NumberFormat

### Files Modified During Review

Нет модификаций — код не требует изменений.

### Gate Status

Gate: **PASS** → `docs/qa/gates/26.3-cart-summary.yml`

### Recommended Status

✓ **Ready for Done** — Все требования выполнены, code quality excellent, тесты проходят.
