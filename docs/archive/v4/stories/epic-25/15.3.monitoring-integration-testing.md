# Story 15.3: Интеграция с системой мониторинга и тестирование

> **Epic**: [Epic 15 - Обновление изображений товаров через админ-панель](../../epics/epic-15/epic-15.admin-image-update.md)
> **Создано**: 2025-11-26
> **Статус**: 📋 Approved
> **Зависимости**: Story 15.1 и Story 15.2 должны быть завершены

---

## Status

**Current Status**: Approved

---

## Story

**As a** администратор системы,
**I want** видеть импорт изображений в списке сессий `ImportSessionAdmin` с корректными статусами и прогрессом,
**so that** я могу отслеживать выполнение импорта в реальном времени и диагностировать проблемы при необходимости.

---

## Acceptance Criteria

1. Импорт изображений отображается в списке сессий с корректными статусами
2. Прогресс-бар работает корректно (если применимо)
3. Тесты покрывают: успешный импорт, отсутствие товаров, отсутствие директории
4. Coverage >= 80% для нового кода
5. Документация обновлена с примерами использования
6. Все существующие типы импорта продолжают работать (regression tests)
7. Проверка директории `import_files/` перед запуском импорта
8. E2E тест полного flow: форма → Celery → мониторинг

---

## Tasks / Subtasks

- [ ] **Task 1: Добавить проверку директории import_files в \_create_and_run_import()** (AC: 7)
  - [ ] Расширить `_create_and_run_import()` ([views.py:164-258](../../../backend/apps/integrations/views.py#L164-L258))
  - [ ] Добавить ветку для типа "images":
    ```python
    elif import_type == "images":
        if not (data_path / "goods" / "import_files").exists():
            raise FileNotFoundError(
                f"Директория изображений не найдена: {data_path / 'goods' / 'import_files'}. "
                f"Убедитесь, что данные выгружены из 1С."
            )
    ```
  - [ ] Убедиться что проверка выполняется ДО создания ImportSession
  - [ ] Добавить unit-тест для этой валидации

- [ ] **Task 2: Проверить интеграцию с ImportSessionAdmin** (AC: 1, 2)
  - [ ] Убедиться что `ImportType.IMAGES` корректно отображается в списке
  - [ ] Проверить работу `colored_status()` для сессий типа "images"
  - [ ] Проверить работу `celery_task_status()` для отслеживания Celery задачи
  - [ ] Проверить расчет `duration()` для завершенных импортов
  - [ ] **Опционально**: Адаптировать `progress_display()` для типа "images":
    - [ ] Использовать `report_details["total_products"]` вместо `total_items`
    - [ ] Использовать `report_details["processed"]` вместо `processed_items`
    - [ ] Если не адаптируется - прогресс-бар не будет показываться (допустимо)

- [ ] **Task 3: Создать интеграционные тесты для валидаций** (AC: 3)
  - [ ] **Тест: успешный импорт изображений**
    - [ ] Создать товары с `onec_id`
    - [ ] Мокировать директорию `import_files/` с тестовыми файлами
    - [ ] POST запрос с `import_type=images`
    - [ ] Проверить создание ImportSession со статусом STARTED
    - [ ] Проверить вызов Celery задачи
    - [ ] Мокировать выполнение задачи
    - [ ] Проверить финальный статус COMPLETED

  - [ ] **Тест: отсутствие товаров в БД**
    - [ ] Очистить таблицу Product
    - [ ] POST запрос с `import_type=images`
    - [ ] Проверить редирект с ошибкой
    - [ ] Проверить что ImportSession НЕ создана

  - [ ] **Тест: отсутствие директории import_files**
    - [ ] Создать товары
    - [ ] Мокировать отсутствие директории `import_files/`
    - [ ] POST запрос с `import_type=images`
    - [ ] Проверить `FileNotFoundError` с понятным сообщением
    - [ ] Проверить что ImportSession НЕ создана

- [ ] **Task 4: Создать E2E тесты полного flow** (AC: 8)
  - [ ] **E2E тест: полный цикл импорта изображений**
    - [ ] Создать товары и тестовые изображения
    - [ ] GET запрос на `/admin/integrations/import_1c/`
    - [ ] Проверить наличие типа "images" в форме
    - [ ] POST запрос с выбором "images"
    - [ ] Проверить создание ImportSession
    - [ ] Запустить Celery задачу синхронно (eager mode)
    - [ ] Проверить обновление ImportSession с результатами
    - [ ] Проверить GET `/admin/integrations/session/` - сессия отображается
    - [ ] Проверить финальный статус COMPLETED

  - [ ] **E2E тест: обработка ошибки в Celery задаче**
    - [ ] Мокировать `ProductDataProcessor` для вызова исключения
    - [ ] Запустить импорт
    - [ ] Проверить статус ImportSession = FAILED
    - [ ] Проверить наличие `error_message` в сессии

- [ ] **Task 5: Regression тесты для существующих типов** (AC: 6)
  - [ ] **Regression тест: catalog тип работает как прежде**
    - [ ] POST запрос с `import_type=catalog`
    - [ ] Проверить создание сессии с типом CATALOG
    - [ ] Проверить что валидация для "images" НЕ применяется к catalog

  - [ ] **Regression тест: stocks тип требует товары**
    - [ ] Очистить таблицу Product
    - [ ] POST запрос с `import_type=stocks`
    - [ ] Проверить ошибку валидации
    - [ ] Создать товары
    - [ ] POST запрос с `import_type=stocks`
    - [ ] Проверить успешное создание сессии

  - [ ] **Regression тест: prices тип работает**
    - [ ] Создать товары
    - [ ] POST запрос с `import_type=prices`
    - [ ] Проверить создание сессии с типом PRICES

  - [ ] **Regression тест: customers тип работает**
    - [ ] POST запрос с `import_type=customers`
    - [ ] Проверить создание сессии с типом CUSTOMERS

- [ ] **Task 6: Coverage анализ и gap filling** (AC: 4)
  - [ ] Запустить `pytest --cov=apps/integrations --cov=apps/products`
  - [ ] Проверить coverage для нового кода:
    - [ ] `views.py` - проверка директории import_files
    - [ ] `tasks.py` - функция `_run_image_import()`
    - [ ] `tasks.py` - ветка "images" в `_execute_import_type()`
    - [ ] `models.py` - новый enum `ImportType.IMAGES`
  - [ ] Если coverage < 80% - добавить недостающие тесты
  - [ ] Сгенерировать HTML отчет: `pytest --cov-report=html`

- [ ] **Task 7: Обновить документацию** (AC: 5)
  - [ ] **Обновить CLAUDE.md**:
    - [ ] Добавить пример использования импорта изображений
    - [ ] Документировать формат `report_details` для типа "images"
    - [ ] Добавить troubleshooting секцию
  - [ ] **Обновить Epic 15**:
    - [ ] Отметить завершение всех 3 stories
    - [ ] Обновить Definition of Done
  - [ ] **Создать README для администраторов** (опционально):
    - [ ] Пошаговая инструкция по импорту изображений
    - [ ] Частые ошибки и их решения
    - [ ] Требования к структуре директорий

- [ ] **Task 8: Финальная проверка и cleanup**
  - [ ] Запустить все тесты: `make test`
  - [ ] Запустить линтеры: `make lint`
  - [ ] Проверить type hints: `mypy apps/`
  - [ ] Проверить миграции: `python manage.py makemigrations --check`
  - [ ] Запустить форматирование: `make format`
  - [ ] Проверить отсутствие TODO/FIXME в новом коде
  - [ ] Code review checklist пройден

---

## Dev Notes

### Relevant Source Tree

**Основные файлы для изменения:**

```
backend/
├── apps/
│   ├── integrations/
│   │   ├── views.py:164-258           # _create_and_run_import() - добавить проверку директории
│   │   ├── admin.py:207-235           # progress_display() - опционально адаптировать
│   │   └── tests/
│   │       ├── test_views.py          # Интеграционные тесты views
│   │       └── test_tasks.py          # Интеграционные тесты tasks
│   └── products/
│       └── tests/
│           └── test_models.py         # Тесты модели ImportSession
└── docs/
    ├── CLAUDE.md                       # Обновить с примерами
    └── epics/epic-15/
        └── epic-15.admin-image-update.md  # Отметить завершение
```

**Файлы для обновления:**

- Документация в `CLAUDE.md`
- Epic 15 статус
- Coverage отчет

---

### Existing System Integration

**1. ImportSessionAdmin для мониторинга** ([admin.py:14-236](../../../backend/apps/integrations/admin.py#L14-L236)):

**Ключевые методы**:

- `colored_status()` - автоматически работает для всех типов ImportSession
- `celery_task_status()` - отслеживает Celery задачи через AsyncResult
- `duration()` - рассчитывает длительность импорта
- `progress_display()` - показывает прогресс-бар (требует адаптации)

**Progress display текущая логика** ([admin.py:207-235](../../../backend/apps/integrations/admin.py#L207-L235)):

```python
def progress_display(self, obj: Session) -> str:
    if obj.status == "in_progress" and obj.report_details:
        total = obj.report_details.get("total_items", 0)      # ← НЕ совпадает
        processed = obj.report_details.get("processed_items", 0)  # ← НЕ совпадает
        # ...
```

**Story 15.2 использует другие ключи**:

```python
report_details = {
    "total_products": count,     # ← Вместо "total_items"
    "processed": processed_count, # ← Вместо "processed_items"
    # ...
}
```

**Варианты решения**:

**Вариант A (рекомендуется)**: Адаптировать `progress_display()` для поддержки обоих форматов:

```python
def progress_display(self, obj: Session) -> str:
    if obj.status == "in_progress" and obj.report_details:
        # Попытка получить данные в разных форматах
        total = obj.report_details.get("total_items") or obj.report_details.get("total_products", 0)
        processed = obj.report_details.get("processed_items") or obj.report_details.get("processed", 0)

        if total > 0:
            # ... логика прогресс-бара
```

**Вариант B**: Не показывать прогресс-бар для типа "images" (допустимо, статус и duration достаточно)

---

**2. Проверка директорий в \_create_and_run_import()** ([views.py:198-227](../../../backend/apps/integrations/views.py#L198-L227)):

**Существующий паттерн**:

```python
# Проверяем наличие необходимых поддиректорий в зависимости от типа импорта
if import_type == "catalog":
    required_subdirs = ["goods", "offers", "prices", "rests"]
    missing_subdirs = [
        subdir for subdir in required_subdirs if not (data_path / subdir).exists()
    ]
    if missing_subdirs:
        raise FileNotFoundError(
            f"Отсутствуют обязательные поддиректории в {data_dir}: "
            f"{', '.join(missing_subdirs)}. "
            f"Убедитесь, что данные выгружены из 1С в правильной структуре."
        )
elif import_type == "stocks":
    if not (data_path / "rests").exists():
        raise FileNotFoundError(...)
```

**Добавить для "images"**:

```python
elif import_type == "images":
    import_files_dir = data_path / "goods" / "import_files"
    if not import_files_dir.exists():
        raise FileNotFoundError(
            f"Директория изображений не найдена: {import_files_dir}. "
            f"Убедитесь, что данные выгружены из 1С с изображениями товаров."
        )
```

---

**3. Формат report_details для разных типов**:

**Catalog**:

```json
{
  "total_items": 1000,
  "processed_items": 1000,
  "created": 500,
  "updated": 400,
  "skipped": 100,
  "errors": 0
}
```

**Images** (из Story 15.2):

```json
{
  "total_products": 1000,
  "processed": 1000,
  "copied": 2500,
  "skipped": 100,
  "errors": 5,
  "last_updated": "2025-11-26T12:34:56"
}
```

**Customers**:

```json
{
  "total_customers": 500,
  "created": 300,
  "updated": 150,
  "skipped": 50,
  "errors": 0
}
```

**Вывод**: Каждый тип использует свой формат, но ключевая информация всегда присутствует.

---

### Существующие паттерны для следования

#### 1. E2E тестирование с Celery eager mode

**Паттерн**:

```python
import pytest
from django.test import TestCase, override_settings
from django.contrib.auth import get_user_model
from apps.products.models import ImportSession


@pytest.mark.integration
@override_settings(CELERY_TASK_ALWAYS_EAGER=True)
class TestImageImportE2E(TestCase):
    """E2E тесты полного flow импорта изображений."""

    def setUp(self):
        """Настройка тестовых данных."""
        self.client = Client()
        self.admin_user = get_user_model().objects.create_superuser(
            email="admin@test.com",
            password="testpass123"
        )
        self.client.force_login(self.admin_user)

    @patch("pathlib.Path.exists", return_value=True)
    @patch("apps.integrations.tasks.ProductDataProcessor")
    def test_full_image_import_flow(self, mock_processor_class, mock_path_exists):
        """
        Полный E2E тест: форма → POST → Celery → ImportSession обновление.
        """
        # 1. Создать товары
        from apps.products.tests.factories import ProductFactory
        ProductFactory.create_batch(3)

        # 2. Mock processor
        mock_processor = mock_processor_class.return_value
        mock_processor.import_product_images.return_value = {
            "copied": 2,
            "skipped": 0,
            "errors": 0,
        }

        # 3. GET форму импорта
        response = self.client.get("/admin/integrations/import_1c/")
        assert response.status_code == 200
        assert "images" in str(response.content)

        # 4. POST запрос на импорт
        response = self.client.post(
            "/admin/integrations/import_1c/",
            {"import_type": "images"}
        )

        # 5. Проверить редирект
        assert response.status_code == 302

        # 6. Проверить создание сессии
        session = ImportSession.objects.filter(
            import_type=ImportSession.ImportType.IMAGES
        ).first()

        assert session is not None
        assert session.celery_task_id is not None

        # 7. Celery задача выполнилась синхронно (eager mode)
        # Перезагрузить сессию для получения обновленных данных
        session.refresh_from_db()

        # 8. Проверить финальный статус
        assert session.status == ImportSession.ImportStatus.COMPLETED
        assert session.finished_at is not None
        assert session.report_details["total_products"] == 3
        assert session.report_details["processed"] == 3
        assert session.report_details["copied"] == 6  # 3 товара * 2 изображения
```

---

#### 2. Regression тестирование

**Паттерн**:

```python
@pytest.mark.integration
@override_settings(CELERY_TASK_ALWAYS_EAGER=True)
class TestExistingImportTypesRegression(TestCase):
    """Regression тесты: новый тип не ломает существующие."""

    def setUp(self):
        """Настройка тестовых данных."""
        self.client = Client()
        self.admin_user = get_user_model().objects.create_superuser(
            email="admin@test.com",
            password="testpass123"
        )
        self.client.force_login(self.admin_user)

    @patch("apps.integrations.tasks.call_command")
    def test_catalog_import_still_works(self, mock_call_command):
        """Проверка: импорт catalog работает после добавления типа images."""
        # POST запрос для catalog
        response = self.client.post(
            "/admin/integrations/import_1c/",
            {"import_type": "catalog"}
        )

        # Проверить создание сессии
        session = ImportSession.objects.filter(
            import_type=ImportSession.ImportType.CATALOG
        ).first()

        assert session is not None
        assert session.celery_task_id is not None

        # Проверить что call_command был вызван для catalog
        mock_call_command.assert_called_with(
            "import_catalog_from_1c",
            "--file-type",
            "all"
        )
```

---

#### 3. Coverage анализ

**Команды**:

```bash
# Запуск тестов с покрытием для конкретных app
pytest --cov=apps/integrations --cov=apps/products \
       --cov-report=term-missing \
       --cov-report=html

# Проверка coverage для конкретных файлов
pytest --cov=apps/integrations/views \
       --cov=apps/integrations/tasks \
       --cov-report=term-missing

# HTML отчет
pytest --cov=apps/integrations --cov-report=html
# Открыть htmlcov/index.html
```

**Анализ gaps**:

```python
# Найти неохваченные строки в coverage отчете
# Добавить тесты для:
# - Error handling paths
# - Edge cases (пустые списки, None значения)
# - Валидация входных данных
```

---

### Важные замечания

1. **Progress display - опциональная задача**:
   - Если не адаптировать - прогресс-бар не будет показываться для "images"
   - Статус, Celery task status и duration будут работать
   - Рекомендуется адаптировать для полноты feature

2. **Проверка директории import_files**:
   - ДОЛЖНА выполняться в `_create_and_run_import()` ДО создания сессии
   - Иначе сессия создастся, а Celery задача упадет с ошибкой
   - Лучше показать ошибку пользователю сразу

3. **E2E тесты с Celery**:
   - Использовать `CELERY_TASK_ALWAYS_EAGER=True` для синхронного выполнения
   - Это позволяет тестировать полный flow без реального Celery worker
   - Мокировать тяжелые операции (файловая система, процессинг изображений)

4. **Regression тесты критичны**:
   - Epic 15 добавляет новый функционал к существующей системе
   - ОБЯЗАТЕЛЬНО проверить что catalog, stocks, prices, customers не сломались
   - Запустить все старые тесты после изменений

5. **Coverage требования**:
   - > = 80% для НОВОГО кода (views.py проверка, tasks.py функция)
   - Допустимо если ОБЩИЙ coverage немного снизился из-за нового кода
   - Главное - новый код хорошо покрыт тестами

---

### Testing

#### Testing Standards

**Framework**: Pytest 7.4.3 + pytest-django 4.7.0

**Coverage target**: >= 80% для нового кода

**Test file locations**:

- `backend/apps/integrations/tests/test_views.py` - интеграционные тесты views
- `backend/apps/integrations/tests/test_tasks.py` - интеграционные тесты tasks
- `backend/apps/integrations/tests/test_e2e.py` - E2E тесты (новый файл)

**Pytest markers**:

- `@pytest.mark.unit` - unit-тесты
- `@pytest.mark.integration` - интеграционные тесты
- `@pytest.mark.slow` - медленные тесты (E2E)

---

#### Testing Patterns

**1. Интеграционный тест проверки директории**

```python
import pytest
from django.test import TestCase
from unittest.mock import patch
from apps.integrations.views import _create_and_run_import


@pytest.mark.integration
class TestImageImportDirectoryValidation(TestCase):
    """Тесты валидации директории import_files."""

    @patch("pathlib.Path.exists")
    @patch("apps.integrations.views.run_selective_import_task")
    def test_images_import_fails_if_directory_missing(
        self, mock_task, mock_path_exists
    ):
        """Проверка FileNotFoundError при отсутствии import_files."""
        # Mock: основная директория есть, но import_files нет
        def path_exists_side_effect(path_obj):
            path_str = str(path_obj)
            if "import_files" in path_str:
                return False
            return True

        mock_path_exists.side_effect = path_exists_side_effect

        # Попытка запустить импорт
        with pytest.raises(FileNotFoundError) as exc_info:
            _create_and_run_import("images")

        # Проверить сообщение об ошибке
        assert "import_files" in str(exc_info.value)
        assert "изображений не найдена" in str(exc_info.value)

        # Проверить что Celery задача НЕ запускалась
        mock_task.delay.assert_not_called()

    @patch("pathlib.Path.exists", return_value=True)
    @patch("apps.integrations.views.run_selective_import_task")
    def test_images_import_succeeds_if_directory_exists(
        self, mock_task, mock_path_exists
    ):
        """Проверка успешного запуска при наличии import_files."""
        from apps.products.tests.factories import ProductFactory

        # Создать товары для прохождения валидации
        ProductFactory.create()

        mock_task.delay.return_value.id = "test-task-id"

        # Запустить импорт
        session = _create_and_run_import("images")

        # Проверить что сессия создана
        assert session is not None
        assert session.import_type == "images"

        # Проверить что Celery задача запущена
        mock_task.delay.assert_called_once_with(["images"])
```

---

**2. E2E тест полного flow с мониторингом**

```python
import pytest
from django.test import TestCase, Client, override_settings
from django.contrib.auth import get_user_model
from django.urls import reverse
from unittest.mock import patch
from apps.products.models import Product, ImportSession
from apps.products.tests.factories import ProductFactory


@pytest.mark.slow
@pytest.mark.integration
@override_settings(CELERY_TASK_ALWAYS_EAGER=True)
class TestImageImportE2EWithMonitoring(TestCase):
    """E2E тесты импорта изображений с проверкой мониторинга."""

    def setUp(self):
        """Настройка тестовых данных."""
        self.client = Client()
        User = get_user_model()
        self.admin_user = User.objects.create_superuser(
            email="admin@test.com",
            password="testpass123"
        )
        self.client.force_login(self.admin_user)

    @patch("pathlib.Path.exists", return_value=True)
    @patch("pathlib.Path.glob")
    @patch("apps.integrations.tasks.ProductDataProcessor")
    def test_e2e_image_import_with_monitoring(
        self, mock_processor_class, mock_glob, mock_path_exists
    ):
        """
        E2E: форма → POST → Celery → ImportSession → admin list.
        """
        # 1. Создать товары
        products = ProductFactory.create_batch(5)

        # 2. Mock glob для возврата изображений
        from unittest.mock import MagicMock
        mock_glob.return_value = [
            MagicMock(name="img1.jpg"),
            MagicMock(name="img2.jpg"),
        ]

        # 3. Mock processor
        mock_processor = mock_processor_class.return_value
        mock_processor.import_product_images.return_value = {
            "copied": 2,
            "skipped": 0,
            "errors": 0,
        }

        # 4. GET форму импорта
        url_form = reverse("admin:integrations_import_from_1c")
        response = self.client.get(url_form)

        assert response.status_code == 200
        import_types = response.context["import_types"]
        images_type = next((t for t in import_types if t["value"] == "images"), None)
        assert images_type is not None

        # 5. POST запрос на импорт
        response = self.client.post(url_form, {"import_type": "images"})

        # 6. Проверить редирект на список сессий
        assert response.status_code == 302
        assert "integrations/session" in response.url

        # 7. Проверить создание ImportSession
        session = ImportSession.objects.filter(
            import_type=ImportSession.ImportType.IMAGES
        ).first()

        assert session is not None
        assert session.status == ImportSession.ImportStatus.STARTED

        # 8. Celery задача выполнилась (eager mode)
        # Перезагрузить сессию
        session.refresh_from_db()

        # 9. Проверить финальный статус
        assert session.status == ImportSession.ImportStatus.COMPLETED
        assert session.finished_at is not None
        assert session.report_details["total_products"] == 5
        assert session.report_details["processed"] == 5
        assert session.report_details["copied"] == 10  # 5 * 2 images

        # 10. GET список сессий через admin
        url_list = reverse("admin:integrations_session_changelist")
        response = self.client.get(url_list)

        assert response.status_code == 200
        # Проверить что сессия отображается
        assert str(session.id) in str(response.content)
        assert "Изображения товаров" in str(response.content)
```

---

**3. Regression тесты для всех типов импорта**

```python
@pytest.mark.integration
@override_settings(CELERY_TASK_ALWAYS_EAGER=True)
class TestAllImportTypesRegression(TestCase):
    """Comprehensive regression тесты для всех типов импорта."""

    def setUp(self):
        """Настройка тестовых данных."""
        self.client = Client()
        User = get_user_model()
        self.admin_user = User.objects.create_superuser(
            email="admin@test.com",
            password="testpass123"
        )
        self.client.force_login(self.admin_user)

    @patch("apps.integrations.tasks.call_command")
    def test_all_import_types_create_sessions(self, mock_call_command):
        """Проверка создания сессий для всех типов импорта."""
        from apps.products.tests.factories import ProductFactory

        # Создать товары для типов требующих catalog
        ProductFactory.create()

        import_types = ["catalog", "stocks", "prices", "customers", "images"]

        for import_type in import_types:
            with self.subTest(import_type=import_type):
                # Мокировать специфичные для типа зависимости
                if import_type == "images":
                    with patch("pathlib.Path.exists", return_value=True), \
                         patch("apps.integrations.tasks.ProductDataProcessor"):
                        response = self.client.post(
                            "/admin/integrations/import_1c/",
                            {"import_type": import_type}
                        )
                else:
                    response = self.client.post(
                        "/admin/integrations/import_1c/",
                        {"import_type": import_type}
                    )

                # Проверить редирект
                assert response.status_code == 302

                # Проверить создание сессии
                session = ImportSession.objects.filter(
                    import_type=import_type.upper()
                ).order_by("-started_at").first()

                assert session is not None, f"Session не создана для {import_type}"
```

---

**4. Тест адаптации progress_display для images**

```python
@pytest.mark.unit
class TestProgressDisplayAdaptation(TestCase):
    """Тесты адаптации progress_display для типа images."""

    def test_progress_display_works_with_images_format(self):
        """Проверка progress_display с форматом report_details для images."""
        from apps.integrations.admin import ImportSessionAdmin
        from apps.integrations.models import Session

        # Создать сессию с форматом images
        session = Session.objects.create(
            import_type="images",
            status="in_progress",
            report_details={
                "total_products": 100,
                "processed": 50,
                "copied": 120,
                "skipped": 5,
                "errors": 2,
            }
        )

        admin = ImportSessionAdmin(Session, admin.site)
        result = admin.progress_display(session)

        # Если адаптировано - должен быть прогресс-бар
        # Если нет - должен быть "-"
        # (зависит от реализации в Task 2)

        # Пример проверки если адаптировано:
        if "total_products" in result or "50%" in result:
            assert "progress" in result or "50" in result
        else:
            assert result == "-"
```

---

**5. Coverage gap analysis тест**

```python
@pytest.mark.unit
class TestCoverageGaps(TestCase):
    """Тесты для покрытия edge cases и error paths."""

    def test_run_image_import_handles_empty_onec_id(self):
        """Проверка обработки товаров без onec_id."""
        from apps.products.tests.factories import ProductFactory

        # Создать товар без onec_id
        product = ProductFactory.create(onec_id="")

        # ... тест логики пропуска товаров без onec_id

    def test_import_session_with_none_report_details(self):
        """Проверка обработки None в report_details."""
        from apps.integrations.models import Session

        session = Session.objects.create(
            import_type="images",
            status="started",
            report_details=None  # Edge case
        )

        # Проверить что admin методы не падают
        from apps.integrations.admin import ImportSessionAdmin
        admin = ImportSessionAdmin(Session, admin.site)

        # Не должно быть исключений
        admin.progress_display(session)
        admin.colored_status(session)
```

---

## Change Log

| Date       | Version | Description                           | Author     |
| ---------- | ------- | ------------------------------------- | ---------- |
| 2025-11-26 | 1.0     | Создание Story 15.3 на основе Epic 15 | Sarah (PO) |

---

## Dev Agent Record

### Agent Model Used

_Будет заполнено dev agent при реализации_

### Debug Log References

_Будет заполнено dev agent при реализации_

### Completion Notes List

_Будет заполнено dev agent при реализации_

### File List

_Будет заполнено dev agent при реализации_

---

## QA Results

_Будет заполнено QA agent после завершения тестирования_

---

## References

- [Epic 15: Обновление изображений товаров через админ-панель](../../epics/epic-15/epic-15.admin-image-update.md)
- [Story 15.1: Добавление типа импорта "Изображения"](15.1.admin-images-import-type.md)
- [Story 15.2: Celery задача для импорта изображений](15.2.celery-task-image-import.md)
- [ImportSessionAdmin](../../../backend/apps/integrations/admin.py#L14-L236)
- [\_create_and_run_import()](../../../backend/apps/integrations/views.py#L164-L258)
- [Coding Standards - Testing](../../architecture/coding-standards.md#тестирование)
